# AWS Deployment (MVP)

This folder contains starter IaC templates for deploying the forecasting stack on AWS.

Recommended MVP stack:

- ECS Fargate (frontend + backend)
- RDS PostgreSQL
- Application Load Balancer
- CloudWatch Logs

Use Terraform in this folder as a starting point (placeholders are intentionally simple for MVP).
