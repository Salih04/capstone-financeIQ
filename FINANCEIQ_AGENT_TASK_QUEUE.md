# FINANCEIQ_AGENT_TASK_QUEUE.md

Sequenced task queue for future coding agents. Grounded in the 2026-07-08 audit (`FINANCEIQ_MODEL_VALIDITY_AUDIT.md`, `OPERATING_LAYER_VALIDATION.md`) and re-planned 2026-07-12 from direct repo inspection after the Stage-0/Stage-1/Stage-2 completions below. Every agent: read `CLAUDE.md` → `PRD.md` → `REPO_MAP.md` → `FINANCEIQ_SMALL_MODEL_RULES.md` first, then only the task's listed files.

**Universal verification:** current observed suite counts and data-validation output live in `docs/VERIFICATION_BASELINE.md`; re-run the applicable command and compare with that dated baseline rather than copying counts into new task text. After backend edits run `PYTHONPATH=backend python -m pytest backend/tests`; after pipeline/test edits run `PYTHONPATH=. python -m pytest tests/`; after data edits also run `make data-validate`; after any user-facing copy or response-constant change run `make claims-lint`. Rollback for all tasks: `git checkout -- <files>` before commit, `git revert` after — no task below has irreversible side effects unless its rollback note says otherwise.

**Model recommendation key** (used in every spec): **Sol** = small/fast model — docs, copy, ledger edits, verification runs. **Terra** = mid model — standard additive backend/frontend features with existing patterns to copy. **Opus** = strong model — guard-adjacent code, statistics, migrations, new services. **Fable** = frontier model — tasks where pipeline changes and claim surfaces intersect, or where a wrong judgment call creates investment-advice risk. Effort levels: low / medium / high.

---

## Phase 1 — COMPLETE (do not reopen)

All Phase-1 tasks are done and verified. Evidence ledger (commit SHAs from `git log`, states from `TASK_STATE.md` 2026-07-12 verification row and current artifacts):

| Task | Status | Evidence |
|---|---|---|
| OPS-01 stale `call_local_llm` refs | DONE | zero `call_local_llm` references remain in `tests/`/`research_agent_training/` (grep 2026-07-12); root suite 97/97 per TASK_STATE verification row |
| OPS-02 dead `unnecessary/` link | DONE | `a93bb766` |
| OPS-03 stale ledger test-count line | DONE (superseded) | VER-01 baseline row in TASK_STATE (2026-07-12); note: ledger "Last updated" header is still stale — folded into R2-GOV-01 |
| OPS-04 PRD ambiguities | DONE | `558e115e` |
| OPS-05 Settings tolerates unknown `.env` keys | DONE | `843a02ef`; backend suite now 54/54 |
| DATA-01 dictionary cross-check | DONE | `438e113e`, `DATA_01_DATA_DICTIONARY_AUDIT.md` (61/61 columns) |
| DATA-02 missing-features-reduce-confidence tests | DONE | `33697229` |
| DATA-03 malformed-CSV failure messages | DONE | `dd5f774b` |
| DATA-04 conditional DEGENERATE caveat | DONE | `854332a6` + follow-up `0ef21bf4` (generated `experiment_summary.md` path) |
| DATA-05 re-run experiments, refresh summary | DONE | `fe185e06` (environment-qualified; see `.agent/memory/02-…`) |
| DATA-06 sector audit | DONE | `0822a8ea`; METHODOLOGY §"Sector-label provenance": **`sector` is unpopulated in all trusted datasets** — no per-sector features or checks are currently possible |
| MOD-01 IC dispersion in UI | DONE | `4a9266c3` |
| MOD-02 serving heuristic documented | DONE | `c10f6b79`, METHODOLOGY §"Serving-side experimental ranking heuristic" |
| UI-01 disclaimer coverage | DONE | `5822de8e`; now contract-enforced by `make claims-lint` |
| UI-02 dataset-scope lines | DONE | `f70009c4` |
| BE-01 demo smoke check | DONE | `97e9be01` → `scripts/demo_smoke.py`, `make demo-check` |
| BE-02 fresh-DB bootstrap verification | DONE | `5db00f8d` + `773f5548`, `docs/FRESH_DATABASE_BOOTSTRAP_VERIFICATION.md` |
| VER-01 verification baseline | DONE | `ad016f64`, TASK_STATE row (root 97, backend 51→54, data VALID, build green) |
| VER-02 frontend build/e2e status | DONE | `149d8d9c` (build green; e2e not run — no backend at the time) |
| DOC-01 first repo-memory lessons | DONE | `47d1b62b`, two lessons in `.agent/memory/` |

---

# Phase 2 — Roadmap execution queue

Implements `FINANCEIQ_MOONSHOT_ROADMAP.md`. The Stage-0 gate is **satisfied**. Phase 2 is now partially complete; the remaining queue below was re-specified 2026-07-12 against the artifacts the completed tasks actually produced.

## Phase 2 completions (do not reopen)

| Task | Status | Evidence |
|---|---|---|
| R2-REPRO-01 run manifests + one-command reproduction | DONE | `74f35efe`; `experiments/results/runs/<stamp>_<sha>/manifest.json` (2 committed), `scripts/verify_run.py`, `make research-verify-run`, METHODOLOGY §"Reproducibility and run provenance" |
| R2-UNIV-01 universe & survivorship audit | DONE | `26448525`; `docs/universe_audit.md`, METHODOLOGY Limitations (retrospective cohort, 226/240 price coverage, missing years for ASTOR/CANTE/DSTKF/MIATK/PASEU) |
| R2-STAT-01 permutation + bootstrap significance | DONE | `c0c5c1d9`; `experiments/results/predictions_test_{2023,2024,2025}.csv` (ticker,year,model,y_true,y_pred; n=80/split), `experiments/significance.py`, `make research-significance`, `significance_report.{json,md}`. Headline: no ML model survives Bonferroni family-wise correction; smallest raw ML p is random forest at pooled IC **−0.153** (adj p=0.1098); equal-weight baseline pooled IC 0.150 is descriptive context **outside** the correction family |
| R2-STAT-02 power / minimum detectable IC | DONE | `a875bf67`; analytic+simulated power in the significance report: detectable \|IC\| ≈ **0.309** (one 80-row year), **0.182** (three-year design), 0.431/0.260 at public-40 scale; METHODOLOGY §"Power and detectability limits", claims guide §11 |
| R2-CONTRACT-01 Model Confidence Contract v1 + claims lint | DONE | `28ba92b2`; `model_confidence_contract.json` (v1.0.0), `scripts/lint_claims.py`, `make claims-lint`, backend contract test |
| R2-GOV-01 truth sync + experiment-artifact governance | DONE (uncommitted) | 2026-07-12; observed root 106/106, backend 55/55, data VALID; truth-sync and run-directory governance docs updated |
| R2-CONTRACT-02 MCC coverage drift guard + versioning procedure | DONE (uncommitted) | 2026-07-12; `model_confidence_contract.json` v1.1.0, recursive JSX scan, explicit auth exemptions, root route-coverage guard, and MCC versioning procedure; commit deferred by request |
| R2-LINEAGE-01 feature passports | DONE (uncommitted) | 2026-07-12; generated `feature_passports.json` covers 61/61 columns, read-only API passthrough, Score Explorer passport popover; root 111/111, backend 57/57, data VALID, frontend build and claims lint passed; commit deferred by request |
| R2-SKEPTIC-01 skeptic challenge service | DONE (uncommitted) | 2026-07-12; deterministic cached six-check `/research/skeptic/{ticker}` report, structured evidence citations and insufficient-data handling, MCC v1.2.0; root 111/111, backend 67/67, claims lint and live ASELS/ASTOR checks passed; commit deferred by request |
| R2-AUTOPSY-01 Negative Alpha Autopsy | DONE (uncommitted) | 2026-07-12; six artifact-backed exhibits at `/autopsy`, typed `/research/significance/autopsy` passthrough, explicit source/limitation labels, MCC v1.3.0; root 114/114, backend 69/69, frontend build and claims lint passed; live API passed, page visual blocked by missing approved Supabase session; commit deferred by request |
| R2-CAL-01 confidence calibration bench | DONE (uncommitted) | 2026-07-12; deterministic replay over persisted 2023–2025 prediction dumps, `calibration_report.{json,md}` + `calibration_plot.csv`; hybrid confidence is constant at 0.25, so decile calibration and monotonicity are not estimable; root 125/125, backend 69/69, claims lint passed; no tuning or service/model change; commit deferred by request |
| R2-REAL-01 real-terms + USD return targets | DONE (uncommitted) | 2026-07-12; separate CPI-deflated TRY and USD-basis targets for all 321 nominal outcomes, isolated `experiments/results_real_terms/` significance artifacts; real TRY random-forest pooled IC −0.156 (Bonferroni p=0.0984), USD −0.150 (Bonferroni p=0.1278), neither survives correction; two-run checksums identical; canonical datasets/nominal artifacts unchanged; root 136/136, data VALID, claims lint passed; no commit by request |
| R2-REGIME-01 Regime Lens | DONE (uncommitted) | 2026-07-12; sourced effective-dated 2020–2025 macro context, deterministic `experiments/results_regime/regime_context_report.*`, read-only `/research/regime-context`, and shared chart strip; all three test years map to one task-defined period, so regime-conditional diagnostics are explicitly untestable and not computed; two-run checksums identical; root 146/146, backend 71/71, data VALID, frontend build and claims lint passed; canonical nominal and real-terms artifacts unchanged; no commit by request |
| R2-LOOP-01 analyst-in-the-loop dissent ledger | DONE (uncommitted) | 2026-07-13; append-only `analyst_verdicts` migration, authenticated writes, deterministic read-only aggregates, shared Labeling/Validation Lab ledger, score-isolation pin test, and MCC v1.5.0; scratch Postgres migration passed; root 149/149, backend 76/76, frontend build and claims lint passed; protected research/data artifact checksums unchanged; no commit by request |
| R2-COURT-01 Research Courtroom | DONE (uncommitted) | 2026-07-13; deterministic citation-complete Bull/Bear/Skeptic/Risk evidence lenses at `POST /research/courtroom` and `/courtroom`; four items per lens, Risk last, structured `insufficient_data`, and no adjudication field; MCC v1.6.0; root 152/152, backend 83/83, frontend build, claims lint, and live LLM-off API passed; protected-page visual blocked by missing approved Supabase session; protected research/data checksums unchanged; no commit by request |
| R2-FRICTION-01 Friction simulator | DONE (uncommitted) | 2026-07-13; deterministic within-model/year rank-only top-10 nominal TRY baskets with half-L1 turnover and explicit zero/illustrative/deliberately adverse assumed-cost controls; `friction_report.{json,md}` + `friction_plot.csv` and in-drawing stamped Autopsy panel; MCC v1.7.0; two-run checksums identical; root 168/168, backend 85/85, frontend build and claims lint passed; protected experiment/trusted-data checksums unchanged; no commit by request |
| R2-DEMO-01 glass-box demo runbook + reproducibility quickstart | DONE (uncommitted) | 2026-07-13; `docs/DEMO_RUNBOOK.md` scripts the shipped runtime, specimen, seismograph/Instrumented Null, Autopsy, Skeptic, Courtroom, fallback, and claim-tripwire flow; README adds the three-command manifest quickstart; finale failed once under MCC-CLAIM-001 and passed after revert; smoke/API checks passed after rebuilding stale local containers; root 168/168, backend 85/85, frontend build and claims lint passed; protected frontend visuals blocked by missing `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`; protected experiment/trusted-data artifacts unchanged; no commit by request |

### Phase-2 post-commit reconciliation (appended by R3-GOV-01)

The rows above preserve their at-completion state. The owner's later commits supersede every `DONE (uncommitted)` / `commit deferred` note as follows; no historical row was rewritten.

| Task | Committed SHA |
|---|---|
| R2-GOV-01 | `d743e7d2` |
| R2-STAT-UI-01 | `2985a86b` |
| R2-CONTRACT-02 | `253eedc5` |
| R2-LINEAGE-01 | `6af2c5be` |
| R2-SKEPTIC-01 | `53a92a41` |
| R2-AUTOPSY-01 | `a95e1e1c` |
| R2-CAL-01 | `646fdae7` |
| R2-REAL-01 | `7124bdd8` |
| R2-REGIME-01 | `d83741c2` |
| R2-LOOP-01 | `57ea8c05` |
| R2-COURT-01 | `ef6a8030` |
| R2-FRICTION-01 | `b9fe263e` |
| R2-DEMO-01 | `97e4fc33` |

Post-Phase-2 reproducibility state was then recorded by `fbab761f`.

## Universal rules for remaining Phase-2 tasks

- Every new evaluation output goes to a **new, separately named** artifact/directory — never overwrite `experiments/leaderboard.csv`, `experiments/results/significance_report.*`, `experiments/results/predictions_test_*.csv`, or anything in `data/trusted_clean/` except via existing Makefile regeneration.
- Any task that re-runs `make research` must first record `shasum experiments/leaderboard.csv` and confirm it is unchanged afterward; drift = STOP and report (see `.agent/memory/02-environment-qualified-experiment-reruns.md` — byte reproduction is environment-qualified).
- Every new UI surface: (a) its caveat copy is specified **in the task below, verbatim** — wording changes require editing this file first; (b) the new page path must be added to `model_confidence_contract.json` `required_disclaimer.pages` (contract version bump per R2-CONTRACT-02 procedure); (c) `make claims-lint` must pass; (d) any new backend service that ships user-facing response copy must be added to the contract's `scan.backend_response_files`.
- New Makefile targets are additive; renames remain forbidden.
- New data series (CPI, USDTRY, macro) follow the manual-CSV pattern of `data/trusted_raw/shares_outstanding_events.csv`: sourced, dated, shape-validated, null-if-missing, never imputed.
- No new paid dependencies. Python additions must be stdlib/numpy/pandas/scikit-learn (already present); frontend additions must use existing dependencies.
- The prediction dumps evaluate the **81-ticker training universe (n=80 rows per split)**, not the public 40. Any surface built on them must label the universe explicitly; do not describe dump-derived numbers as "the public 40."
- The equal-weight baseline's pooled IC 0.150 (unadjusted p=0.0168) is the single most misquotable number in the repo. It is descriptive context outside the ML correction family. **No task may surface it without the exact qualifier sentence from METHODOLOGY** ("reported as descriptive baseline context outside the six-model ML correction family, not as a validated edge").

---

### R2-GOV-01 — Truth sync + experiment-artifact governance
- **Priority:** P1. **Stage:** 1 (hygiene). **Owner role:** Documentation/Verification. **Risk:** low.
- **Why this matters:** Three docs now lag the repo they describe, which is exactly the drift failure mode this project treats as a cardinal sin. Also, `experiments/results/runs/` will accumulate one directory per `make research` forever with no stated retention or which-manifest-is-of-record policy.
- **Current evidence:** `CLAUDE.md` "Known test-suite state" still says root 95/97 with `call_local_llm` failures — but zero such references remain (grep 2026-07-12) and TASK_STATE's verification row records 97 passed. `TASK_STATE.md` header still says "Last updated: 2026-06-11 (rev 6)" and has **no rows for any R2-\* completion**. Two run dirs are committed under `experiments/results/runs/` with no governance note anywhere.
- **Target files:** `CLAUDE.md` (test-state note only), `TASK_STATE.md` (header + new Phase-2 rows), `METHODOLOGY.md` (≤ 10-line "Run-directory governance" note under the existing Reproducibility section), optionally one `.agent/memory/` lesson.
- **Steps:** (1) Re-run both suites and `make data-validate`; record honest counts. (2) Update CLAUDE.md's "Known test-suite state" block to the observed state (do not delete the `backend/.env` gotcha history — mark it resolved by OPS-05). (3) Add TASK_STATE rows: R2-REPRO-01/UNIV-01/STAT-01/STAT-02/CONTRACT-01 with dates + SHAs from the ledger above; refresh the "Last updated" header. (4) Write the governance note: the manifest whose SHA matches the committed `leaderboard.csv` is the **manifest of record**; superseded run dirs may be deleted only in the same commit that replaces the leaderboard; never hand-edit a manifest. (5) Optional memory lesson if a new trap was confirmed.
- **Acceptance criteria:** No doc claims a test count that a fresh run contradicts; every completed R2 task has a ledger row; the run-retention rule exists in exactly one place.
- **Verification commands:** `PYTHONPATH=. python -m pytest tests/`; `PYTHONPATH=backend python -m pytest backend/tests`; `make data-validate`; `git diff` docs-only.
- **Generated artifacts:** none.
- **Claim-safety:** prose only; no numbers beyond observed test output; `make claims-lint` unaffected but run it anyway (docs are outside its scan — note that in your report, not as a lint pass).
- **Failure modes:** "fixing" a count without running the suite (forbidden — run it); rewriting ledger history instead of appending.
- **Rollback:** revert the doc edits.
- **Demo value:** low. **Research value:** medium (provenance discipline). **CV value:** low directly, but protects everything else.
- **Model/effort:** **Sol, low.** **Commit:** yes (docs). **/clear after:** yes.

### R2-STAT-UI-01 — The Instrumented Null panel (significance + power into the product)
- **Priority:** P1. **Stage:** 2→3 bridge. **Owner role:** Backend/Frontend. **Risk:** medium (claim surface).
- **Why this matters:** The project's strongest new evidence — permutation p-values, bootstrap CIs, power thresholds, null histograms — exists only as committed files and METHODOLOGY prose. `grep -rn "significance\|permutation\|power\|detectable" frontend/src/pages/` returns **nothing** (verified 2026-07-12). The single most persuasive artifact (observed IC inside its null distribution) is invisible in the product. This was the roadmap's whole point ("turn the negative result from a sentence into an instrument") and it is currently half-done: instrumented, not displayed.
- **Current evidence:** `experiments/results/significance_report.json` (keys: `analysis`, `headline`, `limitations`, `models`, `power_analysis`, `schema_version`, `source_artifacts`; METHODOLOGY states null histograms are included). `ExperimentsPage.jsx` already shows per-split IC dispersion (MOD-01, `4a9266c3`) — the natural host.
- **Target files:** new read-only endpoint in `backend/app/routers/research.py` + a small loader in `backend/app/services/research/` (read the subpackage's existing CSV/JSON access + caching patterns first and copy them; both `research.py` and `research_agent.py` mount under `/research` — check prefixes, never assume); new backend test; `frontend/src/pages/ExperimentsPage.jsx` (extend — no new page); `frontend/src/api/researchApi.js`.
- **Steps:** (1) Read `significance_report.json` structure fully; design the endpoint as a filtered passthrough (headline + per-model {pooled IC, CI, raw p, adjusted p, null-histogram bins} + power_analysis + limitations) — no server-side statistics, ever. (2) Backend test pinning response shape and the presence of `limitations`. (3) Frontend panel on ExperimentsPage in the Research Terminal language: per-model strip with pooled IC dot inside its bootstrap CI bar; one null-distribution histogram (canvas/SVG with existing deps) with the observed IC marked; a power footer line. (4) Demo fallback per the page's existing conventions (labeled, fallback-only). (5) Copy (verbatim, changes require editing this spec): headline — "**No ML model is statistically distinguishable from the within-year null after family-wise correction.**"; power footer — "**This design could only detect |IC| ≥ 0.182 (three-year, 80-row design; 0.309 for a single year) at 80% power — a design limit, not an estimate of the true IC.**"; baseline row, if shown, must carry the METHODOLOGY qualifier sentence verbatim; per-split values, if shown, labeled "**exploratory — SE ≈ 0.16 at this n**". (6) Run `make claims-lint`; add contract allowlist entries only for method-description lines, with review reasons.
- **Acceptance criteria:** Panel renders real report data with backend up and labeled fallback without; no number on the panel differs from `significance_report.json`; backend suite green +new tests; `npm run build` green; `make claims-lint` exit 0; the panel never renders a p-value without its adjusted companion.
- **Verification commands:** `PYTHONPATH=backend python -m pytest backend/tests`; `cd frontend && npm run build`; `make claims-lint`; manual `npm run dev` visual check recorded (screenshot in report).
- **Generated artifacts:** none (reads committed reports).
- **Claim-safety:** the raw-vs-adjusted pairing rule above is mandatory; forbidden framings: "statistically significant" applied to any ML model, any presentation of the baseline as an edge, any "proves market efficiency" phrasing.
- **Failure modes:** cherry-rendering the random-forest raw p=0.0183 without the adjusted 0.1098 (this is the exact multiple-comparisons trap the report exists to prevent); recomputing statistics in JS (forbidden — display only); fallback data drifting from real data shape.
- **Rollback:** revert; purely additive.
- **Demo value:** very high — the null histogram is the project's best single slide, now live in the product. **Research value:** high. **CV value:** very high.
- **Model/effort:** **Terra, medium** (patterns exist to copy; the judgment calls are pre-made in this spec). **Commit:** yes. **/clear after:** yes.

### R2-CONTRACT-02 — MCC coverage drift guard + versioning procedure
- **Priority:** P1. **Stage:** 2 hardening. **Owner role:** Backend/Product. **Risk:** low-medium.
- **Why this matters:** MCC v1.0.0 is a tripwire with a blind spot: its `required_disclaimer.pages` list is a hand-maintained snapshot of today's 21 pages, and its scan covers `frontend/src/pages/*.jsx` plus exactly one backend file. Every remaining Phase-2 task adds pages (Autopsy, Courtroom) or copy-bearing services (Skeptic, Courtroom) — without a drift guard, the contract silently stops covering the newest, riskiest surfaces. This task makes the contract self-policing before those surfaces exist.
- **Current evidence:** `model_confidence_contract.json` `required_disclaimer.pages` (17 pages + 2 route aliases); `scan.backend_response_files` = `[forecasting_csv_service.py]`; `frontend/src/components/` contains copy-bearing components (`TerminalFx.jsx` caveat strips, `layout/`) that are entirely outside the scan; contract `limitations` already admit the scope gap.
- **Target files:** new root test `tests/test_contract_coverage.py` (or backend test — pick where App.jsx parsing fits better; justify in report), `model_confidence_contract.json` (version bump to 1.1.0), `scripts/lint_claims.py` (only if the glob extension needs code), `METHODOLOGY.md` §MCC (≤ 8 lines on the version procedure).
- **Steps:** (1) Write the drift test: parse `frontend/src/App.jsx` route elements → every routed page component file must appear in the contract's `pages` or `route_aliases` (allow an explicit `exempt_pages` contract key for non-data pages like `LoginPage.jsx`/`AuthCallbackPage.jsx` — add it, with reasons per entry). The test fails when someone adds a route without registering it. (2) Extend the lint scan glob to `frontend/src/components/**/*.jsx`; run it; report violations as findings and add allowlist entries only for legitimate lines (each with a reason). Fix nothing silently. (3) Document the version procedure in the contract itself and METHODOLOGY: any change to `rules`, `scan`, `pages`, or `evidence_state` bumps minor version + `effective_date`; allowlist-only line-number refreshes bump patch. (4) Bump to 1.1.0.
- **Acceptance criteria:** Deliberately adding a fake route to App.jsx (test-local fixture or temp mutation, reverted) makes the drift test fail; component-dir scan is green or has a committed findings list; both suites green; `make claims-lint` exit 0.
- **Verification commands:** `PYTHONPATH=. python -m pytest tests/` (or backend suite, per placement); `make claims-lint`; mutation check recorded.
- **Generated artifacts:** none.
- **Claim-safety:** this task *is* claim safety; it must not weaken any existing rule or remove allowlist review reasons.
- **Failure modes:** regex-parsing App.jsx too cleverly (keep it dumb: match `element={<X` + import paths; if the file's structure defeats simple parsing, STOP and report rather than building a JSX parser); exempting a data-bearing page.
- **Rollback:** revert; the contract file and test are self-contained.
- **Demo value:** medium (extends the "watch the lint fail" demo). **Research value:** medium. **CV value:** high — "self-policing claims governance" is a genuinely original line.
- **Model/effort:** **Terra, medium.** **Commit:** yes. **/clear after:** yes.

### R2-LINEAGE-01 — Feature Passports (per-column lineage, machine-readable)
- **Priority:** P2. **Stage:** 1 (last Stage-1 item). **Owner role:** Data/Backend/Frontend. **Risk:** medium (touches the dictionary generator).
- **Why this matters:** The pipeline's lineage story (accepted/rejected machinery, frozen evidence, ingestion reports) is scattered across seven report files. A per-column passport makes "every value is real, sourced, and auditable" a clickable fact, and gives DATA-01's drift problem a durable structural fix — the passport is generated from the same load that validation runs on.
- **Current evidence:** `_data_dictionary()` at `scripts/data_collection/build_all.py:30`, called at `:89`; since DATA-01 (`438e113e`) it regenerates in **both** build and `--validate-only` modes from the loaded dataset — the passport generator inherits that property for free. Dataset has **61 columns** (40 features + targets/metadata; `DATA_01_DATA_DICTIONARY_AUDIT.md`). Role/leakage facts come from `scripts.data_collection.validate.feature_registry`; caveat facts from `data_quality_report.md` / `frozen_column_evidence.md`.
- **Target files:** `scripts/data_collection/build_all.py` (extend `_data_dictionary()` to also emit `data/trusted_clean/feature_passports.json`); new root test `tests/test_feature_passports.py`; backend passthrough endpoint (likely `research.py` router + `services/research/` loader — read first); backend test; `frontend/src/pages/ResearchPage.jsx` (Score Explorer popover, first surface only).
- **Steps:** (1) Read `build_all.py` and `validate.py`'s feature_registry fully. (2) Passport schema per column: `{name, registry_role, source_class (vendor_xlsx|corrected_yearly_csv|yahoo_fetch|manual_shares|derived|metadata), transform_chain (short strings), leakage_risk, acceptance_status, caveats[], evidence_files[]}` — every field must be derivable from existing registry/reports; **if a source class cannot be determined from code/reports, emit `"unknown"` — never guess.** (3) Emit inside `_data_dictionary()` so dictionary and passports can never diverge; regenerate via `make data-validate`. (4) Root test: passport count == dataset column count; every passport role matches feature_registry; no `caveats` invented (spot-pin frozen columns cite `frozen_column_evidence`). (5) Backend passthrough endpoint + shape test. (6) Popover on Score Explorer feature rows with footer (verbatim): "**Provenance record — documents source and validation path, not a guarantee of source accuracy.**" (7) `make claims-lint` (popover copy is in a scanned page).
- **Acceptance criteria:** `make data-validate` still VALID with unchanged counts; `feature_passports.json` regenerates deterministically (run twice, identical); dictionary markdown byte-identical to before (or the diff is explained and confined to generation-order artifacts — report it); suites green +new tests; popover renders real passports with backend up.
- **Verification commands:** `make data-validate` ×2 + `shasum data/trusted_clean/feature_passports.json` between runs; `PYTHONPATH=. python -m pytest tests/`; `PYTHONPATH=backend python -m pytest backend/tests`; `cd frontend && npm run build`; `make claims-lint`.
- **Generated artifacts:** `data/trusted_clean/feature_passports.json` (generated — never hand-edit; regenerate via Makefile only).
- **Claim-safety:** the footer above is mandatory; passports must not describe any feature as "predictive" (registry roles are the vocabulary).
- **Failure modes:** inventing source classes for ambiguous columns (use `"unknown"`); the generator accidentally changing the dictionary's existing content; hand-editing the JSON to fix a wrong passport (forbidden — fix the generator).
- **Rollback:** revert code **and** the generated JSON together; dictionary must be re-verified after revert.
- **Demo value:** high (click any feature → its passport). **Research value:** medium-high. **CV value:** high — "feature-store-grade lineage on a no-fabrication pipeline."
- **Model/effort:** **Opus, medium-high** (touches the generator that guards data truth). **Commit:** yes. **/clear after:** yes.

### R2-SKEPTIC-01 — Skeptic Agent service + challenge report endpoint (revised checks)
- **Priority:** P2. **Stage:** 3. **Owner role:** Backend/Agent. **Risk:** medium.
- **Why this matters:** Operationalizes self-skepticism: every ranking gets attacked before a human sees it. **Revision vs the original spec:** the planned sector-concentration check is impossible — DATA-06 established that `sector` is unpopulated in every trusted dataset (METHODOLOGY §Sector-label provenance). It is replaced by a cohort-integrity check built on the universe audit, which is both feasible and sharper.
- **Current evidence:** all six checks below run on committed artifacts that now exist: `data/trusted_clean/data_quality_report.{md,json}`, `frozen_column_evidence.{md,json}`, the modeling CSVs, `experiments/results/predictions_test_*.csv` (per-model per-ticker, n=80/split), `experiments/results/significance_report.json`, `docs/universe_audit.md` (price-coverage gaps: ASTOR 2020-22, CANTE 2020, DSTKF 2020-24, MIATK 2020, PASEU 2020-23).
- **Target files:** new `backend/app/services/skeptic_service.py`; route in `backend/app/routers/research.py` **or** `research_agent.py` (read both first — both mount `/research`; place it beside the closest existing evidence-serving pattern and justify); new `backend/tests/test_skeptic_service.py`; `model_confidence_contract.json` (`scan.backend_response_files` += the new service; version bump per R2-CONTRACT-02). Frontend panel is a **separate later task** — do not build UI here.
- **The six checks** (each a pure function returning `{check_id, verdict: pass|warn|fail|insufficient_data, evidence: [{fact, source_file}], severity}`):
  1. **Staleness/frozen probe** — does any input to this ticker's display derive from a column listed in `frozen_column_evidence.json`? (Should be none — that's the point of proving it per-ticker.)
  2. **Missingness attack** — fraction of the ticker's feature columns populated in its latest row; warn below a threshold **taken from `data_quality_report`'s coverage stats, cited, or explicitly labeled `heuristic` in the response**.
  3. **Instability probe** — from the prediction dumps: does the ticker's within-year predicted rank flip across models/splits by more than half the field? Deterministic, computed once and cached following existing service caching patterns.
  4. **Cohort-integrity challenge** (replaces sector check) — cites `docs/universe_audit.md`: retrospectively fixed cohort, survivorship risk, and per-ticker price-coverage gaps (fail→warn for the five gap tickers listed above).
  5. **Universe-scale reminder** — n=40 public / n=80 evaluated rows per split; any rank difference smaller than the power-analysis detectable band is unresolvable (cite `significance_report.json` power numbers).
  6. **Backtest reminder** — always present, always last: no ML model survives family-wise correction (cite the report's headline verbatim).
- **Fixed footer (verbatim, mandatory):** "**Surviving these checks means *not obviously broken*, not *predictive* — walk-forward IC remains ≈ 0 and no model survives family-wise correction.**"
- **Steps:** (1) Read `services/research_agent.py` + `services/research/` loaders — reuse path/env handling (`RESEARCH_REPO_ROOT`) and caching. (2) Implement checks as pure functions over loaded artifacts; unit-test each on fixtures (a sparse ticker, a coverage-gap ticker like ASTOR, a stable ticker). (3) `GET /research/skeptic/{ticker}` returning `{ticker, checks[], footer}`; structured `insufficient_data` verdicts for missing inputs — never fabricated. (4) Respect `PUBLIC_DEMO_MODE` read-open convention. (5) Response-shape contract test. (6) Contract update + `make claims-lint`.
- **Acceptance criteria:** Backend suite green +new tests; real evidence for a real ticker (record `curl` output for ASELS and one coverage-gap ticker, e.g. ASTOR); every `evidence` item resolves to an actual file; no score computation altered anywhere; lint green after contract update.
- **Verification commands:** `PYTHONPATH=backend python -m pytest backend/tests`; `make claims-lint`; manual `curl localhost:8000/research/skeptic/ASELS` recorded.
- **Generated artifacts:** none.
- **Claim-safety:** the footer is non-negotiable; "pass" verdicts must never be describable as validation (check the response's wording against the contract's forbidden patterns).
- **Failure modes:** inventing thresholds (each is cited or labeled heuristic); heavy per-request recomputation (cache like existing services); resurrecting the sector check (impossible — evidence above).
- **Rollback:** revert; purely additive.
- **Demo value:** very high — the system prosecutes its own ranking. **Research value:** high (the instability probe surfaces real per-ticker noise). **CV value:** very high.
- **Model/effort:** **Opus, high.** **Commit:** yes. **/clear after:** yes.

### R2-AUTOPSY-01 — Negative Alpha Autopsy page (six exhibits, all real numbers)
- **Priority:** P2. **Stage:** 3. **Owner role:** Frontend/Research. **Risk:** medium.
- **Why this matters:** The negative result is displayed but never *explained*. Every exhibit's numbers now exist in committed artifacts — including power and significance, which were "pending" in the original spec. This page converts "it failed" into "here is the anatomy of why," the project's best interview surface.
- **Current evidence:** `experiments/results/feature_stability_by_split.csv`, `feature_stability_summary.csv`, `coverage_impact.csv`, `leaderboard.csv` (trees negative IC in all three splits), `significance_report.json` (incl. power + null histograms), METHODOLOGY regime prose. Build known green (VER-02).
- **Target files:** backend read-only endpoint(s) serving the three stability/coverage CSVs as JSON (reuse/extend the R2-STAT-UI-01 significance endpoint rather than duplicating it); new `frontend/src/pages/AutopsyPage.jsx` + route in `frontend/src/App.jsx` + API functions in `frontend/src/api/researchApi.js`; `model_confidence_contract.json` (add the page to `required_disclaimer.pages`; version bump); backend tests.
- **The six exhibits** (each: one chart + one finding paragraph that distinguishes "consistent with" from "proves"):
  1. **Instability** — feature-weight sign flips across splits (`feature_stability_by_split.csv`).
  2. **Overfit** — baseline-vs-tree IC bars; trees consistently negative (`leaderboard.csv`).
  3. **Sparsity** — coverage impact (`coverage_impact.csv`).
  4. **Significance** — the multiple-comparisons lesson, stated plainly: *the smallest raw p-value in the model family belongs to random forest at pooled IC **−0.153** — the most "significant" model is significantly bad before correction, and nothing survives after it.* (From `significance_report.json`; both raw and adjusted p shown together, always.)
  5. **Power** — minimum detectable |IC| 0.182/0.309 (80-row design) with the design-limit framing sentence from METHODOLOGY verbatim.
  6. **Regime** — single extraordinary macro regime, 2020–2025; regime robustness untestable (statement, no fake chart).
- **Page caveat strip (verbatim, from roadmap §3.5):** "**This page documents evidence consistent with why no reliable signal was found: unstable feature relationships, overfitting under small n, sparse coverage, low statistical power, and a single macro regime. It explains the negative result; it does not promise a positive one under other conditions.**" Plus the standard research-support disclaimer.
- **Steps:** (1) Read `ExperimentsPage.jsx` fully — copy data-fetch/fallback/caveat patterns exactly. (2) Backend CSV→JSON endpoints + tests (no computation server-side beyond parsing). (3) Page in the Research Terminal language; universe labels per the universal rule (dump-derived exhibits say "81-ticker training universe, n=80/split"). (4) Route + nav wiring; contract page registration; `make claims-lint`. (5) `npm run build`; visual check with backend running; screenshot in report.
- **Acceptance criteria:** All six exhibits render real artifact data with backend up, labeled fallback without; no number differs from its source file; no existing page modified beyond nav; suites + build + lint green.
- **Verification commands:** `PYTHONPATH=backend python -m pytest backend/tests`; `cd frontend && npm run build`; `make claims-lint`; visual check recorded.
- **Generated artifacts:** none.
- **Claim-safety:** exhibit 4's raw/adjusted pairing rule is mandatory; no exhibit may claim causal knowledge ("consistent with," never "because").
- **Failure modes:** over-narration; inventing a regime chart (exhibit 6 is deliberately a statement); duplicating the significance endpoint instead of reusing it.
- **Rollback:** revert; additive.
- **Demo value:** very high — likely the strongest 3 minutes of any demo. **Research value:** very high. **CV value:** very high.
- **Model/effort:** **Opus, high** (Terra acceptable if R2-STAT-UI-01's endpoint landed first and is reusable as-is). **Depends:** R2-STAT-UI-01 (endpoint reuse), R2-CONTRACT-02 (registration procedure). **Commit:** yes. **/clear after:** yes.

### R2-CAL-01 — Confidence calibration bench
- **Priority:** P2. **Stage:** 3. **Owner role:** Research. **Risk:** medium.
- **Why this matters:** The hybrid score's 0.20 confidence component is displayed but never evaluated — the one place the project's own honesty standard isn't yet met. Whatever the bench finds, publishing it (rather than tuning it) is the product feature.
- **Current evidence:** per-row confidence is **not persisted** historically (`experiments/results/research_agent_model_outputs.csv` carries `ml_score`/`ml_rank` only — verified in roadmap §3.12); model ranks per ticker-year are now available from `predictions_test_*.csv`; realized returns are in the modeling CSVs (targets only — this is meta-evaluation on past rows, no leakage concern).
- **Target files:** new `experiments/calibration_bench.py`; new outputs `experiments/results/calibration_report.{json,md}`; additive Makefile target (e.g. `research-calibration`); `METHODOLOGY.md` findings paragraph; new root test.
- **Steps:** (1) Read `forecasting_csv_service.py` and `research_agent.py` confidence computation fully; decide which confidence quantity is actually user-facing and bench that one (state the choice + code refs in the report). (2) **Replay** the service on historical rows to obtain per-row confidence; record replay date, git SHA, and code version in the report — replayed confidence describes *today's code* on past rows, and the report must say so. (3) Join with realized returns; rank error = |predicted rank − realized rank| within year; bin by confidence decile; monotonicity check (seeded, deterministic); emit a plot-ready CSV inside the report dir. (4) Commit a plain verdict sentence, whichever way it points: "confidence is / is not informative about rank error at this scale." (5) METHODOLOGY paragraph quoting the report. (6) If uncalibrated: file a follow-up owner-decision task in this queue; **do not tune confidence on the same rows** (meta-overfitting is the named failure).
- **Acceptance criteria:** Report committed; rerun → byte-identical (seeded); verdict sentence matches the numbers; **no service code changed**; root suite green +new test; `make data-validate` untouched.
- **Verification commands:** `PYTHONPATH=. python -m pytest tests/`; run the new target twice + `shasum` both report files; `git diff` shows no `backend/app/services/` change.
- **Generated artifacts:** `experiments/results/calibration_report.{json,md}` + plot CSV (new dir/file names only; never overwrite existing results).
- **Claim-safety:** "calibrated" may appear only if measured so; the METHODOLOGY paragraph must carry "audited as of run X" framing; UI copy changes (if any needed) are a separate task gated by the contract.
- **Failure modes:** replay mismatch presented as historical truth (the replay-date framing is the fix); quiet recalibration (forbidden); binning so coarse the verdict is vacuous (use deciles over the 321 target rows; if bins are too thin, report that as the finding).
- **Rollback:** delete the new files; nothing else touched.
- **Demo value:** high — an *audited* confidence number is rarer than a good one. **Research value:** very high. **CV value:** high.
- **Model/effort:** **Opus, medium-high.** **Commit:** yes. **/clear after:** yes.
- **Follow-up owner decision filed — NOT STARTED:** decide whether the current dataset-state component should be relabeled as a global caution diagnostic or replaced by a separately designed, walk-forward per-row quantity. Any redesign needs new holdout evaluation; do not tune on the 240 audited ticker-year outcomes or silently substitute forecasting coverage. This is an owner-decision queue item only, not authorization to change service or UI behavior.

### R2-REAL-01 — Real-terms & USD return targets (parallel evaluation)
- **Priority:** P2. **Stage:** 2 (last Stage-2 item). **Owner role:** Data/Research. **Risk:** **high** (touches pipeline + creates quotable numbers).
- **Why this matters:** Nominal TRY targets in a hyperinflation window are the biggest interpretability weakness (2022 benchmark ≈ +186% nominal). Whether the null holds in real/USD terms is open and answerable; either answer strengthens the thesis — but only with the significance machinery (now built) applied before anything is quoted.
- **Current evidence:** `build_panel_for_target()` exists at `experiments/run_experiments.py:296` (re-verify — the file grew since the roadmap cited :115); the manual-CSV validation pattern lives in the shares/corrected-balance-sheet ingestion; `make research-significance` machinery is reusable per target basis; Yahoo fetch pattern in `scripts/fetch_yahoo_chart_prices.py`.
- **Target files:** new `data/trusted_raw/macro/cpi_yearly_tr.csv` (≤ 6 rows, TÜİK annual CPI, source + retrieval date in a sidecar `.md`); USDTRY year-end via a new small fetch script mirroring the existing Yahoo pattern; new `scripts/data_collection/derive_alternative_targets.py`; new output `data/trusted_clean/modeling_targets_alternative.csv` + `alternative_targets_report.{json,md}` (**separate CSV — the main dataset stays byte-identical**); additive Makefile targets; harness runs into `experiments/results_real_terms/`; root tests; `METHODOLOGY.md` comparison paragraph.
- **Steps:** (1) Read `build_all.py`, `validate.py`, and the shares ingestion for the validation pattern. (2) CPI CSV with citation; shape-validate like `corrected_balance_sheet_2024.csv`. (3) USDTRY fetch (free Yahoo, same source class). (4) Derivation stage with formulas in the docstring — real = (1+nominal)/(1+CPI)−1; USD = (1+nominal)·FX_T/FX_{T+1}−1 — **verify FX direction against a hand-computed 2022 example pinned in a test**. Missing CPI/FX year → null target, never interpolated. (5) Tests: null propagation, formula correctness, byte-identity of all existing `data/trusted_clean/` outputs. (6) Run the harness per basis via `build_panel_for_target`; if it cannot point at the alternative CSV without touching metric code, **STOP and report the minimal refactor** rather than improvising. (7) Run the significance machinery per basis into the new results dir. (8) METHODOLOGY paragraph quoting per-basis pooled ICs **with their corrected p-values, never without**.
- **Acceptance criteria:** `make data-validate` unchanged (403/40/321); `shasum` of the main modeling CSV identical before/after; `shasum experiments/leaderboard.csv` identical; new report + results dirs exist; root suite green +new tests; no imputation (test-pinned).
- **Verification commands:** `make data-validate`; the two `shasum` checks; `PYTHONPATH=. python -m pytest tests/`; new Makefile targets end-to-end; `make claims-lint` (METHODOLOGY is outside its scan — human-review the paragraph against the do-not-claim register instead, and say so).
- **Generated artifacts:** the alternative-targets CSV + report (generated; regenerate via Makefile only); `experiments/results_real_terms/` (new, separate; never merged into headline outputs).
- **Claim-safety:** roadmap §6 row applies verbatim — a lucky real-terms split must never become "we found signal in real terms"; every quoted basis result carries its significance treatment; the nominal-vs-real 2022 comparison is an inflation illustration, not a performance statement.
- **Failure modes:** FX direction inverted (hand-checked test guards); CPI vintage/base-year error (cite the exact TÜİK series in the sidecar); headline outputs drifting (shasum gates); promotion of an uncorrected per-split IC.
- **Rollback:** revert code + delete new generated dirs; main outputs untouched by design.
- **Demo value:** high ("+186% nominal was not a bull market"). **Research value:** high. **CV value:** high — inflation-aware evaluation shows domain maturity.
- **Model/effort:** **Fable, high** — pipeline guards, new data series, and a new quotable claim surface intersect here; this is the queue's highest-judgment task. **Commit:** yes. **/clear after:** yes.

### R2-REGIME-01 — Regime Lens (macro context strip; honest "untestable" default)
- **Priority:** P3. **Stage:** 3. **Owner role:** Data/Frontend. **Risk:** medium.
- **Why this matters:** Regime homogeneity is a hard limitation currently buried in prose. The lens makes the limitation itself the visible, taught concept — and creates the slot future data plugs into.
- **Current evidence:** audit §8; METHODOLOGY limitations; R2-REAL-01 creates `data/trusted_raw/macro/` and the CPI series this task shares.
- **Target files:** extend `data/trusted_raw/macro/` with `macro_context_yearly.csv` (CPI — shared file with R2-REAL-01, do not duplicate it — plus policy rate, USDTRY, BIST100 yearly return; each value sourced + dated in a sidecar, null if unsourced); backend passthrough endpoint; a regime-strip component used on `BenchmarkPage.jsx` and `ExperimentsPage.jsx` charts; tests.
- **Steps:** (1) After R2-REAL-01 lands, extend (not duplicate) the macro CSV with the extra columns; shape-validate. (2) Passthrough endpoint + test. (3) Strip component rendering the macro series under existing time-axis charts **without altering any chart data**; prominent default statement (verbatim): "**2020–2025 spans a single extraordinary Turkish macro regime (high inflation, deep TRY depreciation). Model behavior across regimes is therefore untested — this lens shows regime context and will only compute regime-conditional diagnostics when regime diversity exists.**" (4) Contract registration for any new copy; lint.
- **Acceptance criteria:** Charts' underlying data unchanged (diff the fetch payloads); builds + suites + lint green; every macro value carries a source or is null.
- **Verification commands:** `cd frontend && npm run build`; backend pytest; `make claims-lint`.
- **Generated artifacts:** the macro CSV (manual raw input, not generated — hand-maintained under `data/trusted_raw/`, which is the one place manual files live).
- **Claim-safety:** no per-regime statistics of any kind while one regime exists (a "per-regime" number would be a re-labeled aggregate — the named trap).
- **Failure modes:** fabricating a policy-rate value to fill a year (null instead); the strip visually implying causation between macro moves and IC.
- **Rollback:** revert; additive.
- **Demo value:** medium-high. **Research value:** high (thesis framing). **CV value:** medium-high.
- **Model/effort:** **Terra, medium.** **Depends:** R2-REAL-01 (shared macro file). **Commit:** yes. **/clear after:** yes.

### R2-LOOP-01 — Analyst-in-the-loop dissent ledger
- **Priority:** P3. **Stage:** 3. **Owner role:** Backend/Frontend. **Risk:** medium-high (only Alembic-touching task).
- **Why this matters:** Converts the under-explained Labeling/Validation Lab pages into a real research workflow, and creates the only ethically clean use of human judgment here: measuring disagreement, never overriding scores.
- **Current evidence:** `LabelingLabPage.jsx` / `ValidationLabPage.jsx` exist with routers `labeling.py` / `validation.py`; both pages carry the standard disclaimer (contract allowlist confirms); no verdict persistence exists.
- **Target files:** read `labeling.py`/`validation.py` + both pages fully first — map what already exists before adding; new Alembic migration (append-only — new table `analyst_verdicts`: ticker, year, verdict agree|disagree|abstain, reason_type enum, free-text note, user, timestamp); new endpoints; minimal UI addition on the existing pages; aggregate dissent view (simple table first); tests including the **pin test: verdicts never enter any score computation** (assert scoring service outputs are identical with and without verdict rows).
- **Steps:** (1) Map existing surface. (2) Migration + model + endpoints (auth per existing conventions; writes require auth even in demo mode — read how other write endpoints gate). (3) UI verdict controls + dissent aggregate. (4) The score-isolation pin test. (5) Ledger copy (verbatim): "**Records disagreement for research; never a score input.**" (6) Contract registration + lint.
- **Acceptance criteria:** Backend suite green +tests; migration applies on a scratch DB (`alembic upgrade head` transcript recorded per BE-02's pattern); pin test proves score isolation; lint green.
- **Verification commands:** `PYTHONPATH=backend python -m pytest backend/tests`; scratch-DB migration transcript; `cd frontend && npm run build`; `make claims-lint`.
- **Generated artifacts:** none (DB schema change).
- **Claim-safety:** aggregated verdicts must never be presented as a crowd signal; the ledger copy above is mandatory on every dissent view.
- **Failure modes:** migration edited after commit (append-only, forever); verdicts leaking into any ranking path (the pin test is the tripwire); building a rich UI before the schema is right.
- **Rollback:** **special** — before commit, `git checkout` + drop the scratch table; after commit, a new *down*-migration via `git revert` is not enough — write a follow-up migration; flag this in the task report.
- **Demo value:** medium. **Research value:** high. **CV value:** medium-high.
- **Model/effort:** **Opus, high** (the migration + auth surface demands it). **Commit:** yes. **/clear after:** yes.

### R2-COURT-01 — Research Courtroom (deterministic core, no verdict slot)
- **Priority:** P3. **Stage:** 4. **Owner role:** Agent/Frontend. **Risk:** medium-high.
- **Why this matters:** The signature demo: four personas debate a ticker using only cited, validated evidence — and there is structurally no verdict slot, so the flashiest feature cannot give advice.
- **Current evidence:** `services/research_agent.py` (explanation-only LLM constraint, deterministic fallback), `services/research/` evidence loaders, `make build-company-contexts` target exists. **Prerequisite check:** verify `data/trusted_clean/company_contexts/` exists in the worktree; if absent, generate via the Makefile target (generated artifact — regeneration is the allowed path) and record it.
- **Target files:** new `backend/app/services/courtroom_service.py` + `POST /research/courtroom` route (router choice per Skeptic's precedent); new `frontend/src/pages/CourtroomPage.jsx` + route + API function; backend tests; contract updates (page + service registration; version bump).
- **Design (binding):** Deterministic mode works with `RESEARCH_LLM_PROVIDER=none`. Personas are pure functions over a ticker's context + quality reports: **Bull** = top-percentile validated features; **Bear** = bottom-percentile; **Skeptic** = R2-SKEPTIC-01's challenge report embedded verbatim; **Risk** = missingness, small-n, nominal-TRY basis, significance evidence — and always renders last. Every sentence carries a citation chip `{field, value, source_file}`. Equal evidence budget per persona (4 items each). Optional LLM mode may only rephrase the same grounded bullets. **The response schema contains no verdict/recommendation/winner field, and a test asserts that key-absence explicitly.** Closing panel (verbatim): "**A structured debate over historical, validated evidence. No persona forecasts returns; no verdict is issued; nothing here is investment advice.**"
- **Steps:** (1) Read the agent service + one company-context JSON to map fields. (2) Persona builders, unit-tested for citation completeness (every sentence resolves to a field). (3) Route + schema + no-verdict-key test + PUBLIC_DEMO_MODE convention. (4) Page after AutopsyPage patterns; Risk panel visually persistent. (5) Contract registration; `make claims-lint` on all template sentences. (6) Manual run with `RESEARCH_LLM_PROVIDER=none` recorded.
- **Acceptance criteria:** Works LLM-off; every rendered sentence citation-resolvable; no verdict field anywhere (test-pinned); suites + build + lint green.
- **Verification commands:** `PYTHONPATH=backend python -m pytest backend/tests`; `cd frontend && npm run build`; `make claims-lint`; the LLM-off transcript.
- **Generated artifacts:** possibly regenerated `company_contexts/` (via Makefile only; note in report).
- **Claim-safety:** never summarize a debate as a conclusion; equal budgets and Risk-last ordering are design requirements, not suggestions; LLM mode must be incapable of introducing uncited sentences (reject, don't repair).
- **Failure modes:** rhetoric outrunning evidence (citation tests guard); a "Bull wins" reading (no-verdict design is the mitigation); building before Skeptic exists (hard dependency).
- **Rollback:** revert; additive.
- **Demo value:** very high — the philosophy as product. **Research value:** medium. **CV value:** very high.
- **Model/effort:** **Opus, high.** **Depends:** R2-SKEPTIC-01 (embedded), R2-AUTOPSY-01 (page patterns), R2-CONTRACT-02. **Commit:** yes. **/clear after:** yes.

### R2-FRICTION-01 — Friction simulator (the inverted backtester)
- **Priority:** P3. **Stage:** 4. **Owner role:** Research/Frontend. **Risk:** **high — the queue's most dangerous claim surface.**
- **Why this matters:** Backtesting discipline is the one classic quant-hygiene topic with no artifact here. Built *around a null signal*, its purpose is inverted: show that even occasionally-lucky per-split baskets do not survive turnover and costs, and teach why gross hypothetical numbers overstate everything.
- **Current evidence:** prerequisite met — `predictions_test_{2023,2024,2025}.csv` exist (ticker, year, model, y_true, y_pred; n=80/split, training universe). Note: `y_pred` scales differ by model (baseline emits 0–1 scores, regressors emit return-scale values) — baskets must be built from **within-year, within-model ranks of y_pred**, never from y_pred magnitudes.
- **Target files:** new `experiments/friction_sim.py` + outputs `experiments/results/friction_report.{json,md}` (+ plot CSV); additive Makefile target; a panel on `AutopsyPage.jsx` (not a standalone page); root tests; contract updates.
- **Design (binding):** top-k basket per model per test year from y_pred ranks; turnover between consecutive years' baskets; parameterized cost bps (defaults labeled as assumptions with a comment citing that they are assumptions, not measured BIST costs); outputs per-year gross vs net basket mean-return bars only — **no cumulative-wealth curves, ever**; every chart canvas carries the stamp rendered *inside* the drawing area (verbatim): "**Hypothetical illustration — not a backtest of a viable strategy; underlying signal IC ≈ 0 and no model survives significance correction.**" Universe label mandatory: "81-ticker training universe, nominal TRY."
- **Steps:** (1) Read the dumps + leaderboard; build the deterministic simulator (seedless — it's arithmetic, no sampling; if any sampling is added, seed it). (2) Tests: turnover arithmetic on a hand-built fixture; cost application; determinism (rerun → identical). (3) Report generation with the stamp text embedded in the markdown too. (4) Autopsy panel with in-canvas stamp; contract registration; lint. (5) Never run `make research` here — the dumps are inputs, not regenerated.
- **Acceptance criteria:** Rerun → byte-identical report; no cumulative-return series anywhere in code, report, or UI (grep for `cumprod`/`cumulative` as a self-check, recorded); stamp present in every rendered chart and in the report; suites + build + lint green.
- **Verification commands:** `PYTHONPATH=. python -m pytest tests/`; new target twice + `shasum`; `cd frontend && npm run build`; `make claims-lint`.
- **Generated artifacts:** `experiments/results/friction_report.{json,md}` (new names only).
- **Claim-safety:** highest in the queue. Forbidden outputs: any net-return figure without its gross companion and stamp; any annualized/compounded number; any sentence where friction results read as achievable performance. The MCC forbidden-vocabulary rules apply to the report markdown as well — human-review it against the do-not-claim register and say so in the report.
- **Failure modes:** a screenshot-safe chart that isn't (the in-canvas stamp is the defense); ranking by y_pred magnitude across models (scale trap above); scope creep into portfolio optimization (out of scope, forever).
- **Rollback:** delete new files; revert panel.
- **Demo value:** medium-high. **Research value:** high. **CV value:** high — "I built the cost machinery to kill my own lucky baskets" is a memorable line.
- **Model/effort:** **Fable, high** — judgment about what must *not* be shown is the core skill here. **Depends:** R2-AUTOPSY-01 (panel host), R2-CONTRACT-02. **Commit:** yes. **/clear after:** yes.

### R2-DEMO-01 — Glass-box demo runbook + reproducibility quickstart
- **Priority:** P3. **Stage:** 4 (last). **Owner role:** Documentation/Product. **Risk:** low.
- **Why this matters:** The demo assets will exist scattered across eight surfaces; a scripted path with fallback branches is the difference between a wow and a fumble. Also owns the last documentation gap: a one-screen reproducibility quickstart (manifest → `make research-verify-run`) currently lives only inside METHODOLOGY.
- **Current evidence:** `make demo-check` (BE-01), `make research-verify-run`, `make claims-lint`, runtime-status endpoint, all Stage-2/3 surfaces per this queue's completion state at execution time.
- **Target files:** new `docs/DEMO_RUNBOOK.md`; `README.md` (short reproducibility quickstart section linking METHODOLOGY — additive prose only).
- **Steps:** (1) Inventory which queue surfaces actually shipped (do not script a demo of an unbuilt page — check `App.jsx` routes). (2) Scripted path: `make demo-check` → runtime-status → frozen-evidence specimen archive → seismograph + Instrumented Null panel → Autopsy → Skeptic/Courtroom (if shipped) → finale: live-edit a page to say "predicts winners," run `make claims-lint`, watch it fail, revert. (3) Fallback branches: LLM down (deterministic modes), backend down (labeled demo data), single-page demo (Autopsy only). (4) Timing notes per segment; the §5/§14 claims-guide answers cross-referenced, not duplicated. (5) README quickstart: three commands, no claims.
- **Acceptance criteria:** Every step names a real route/target that exists at write time; the lint-failure finale is rehearsed and its transcript recorded; no forbidden claim anywhere in the runbook (human-review against the claims guide).
- **Verification commands:** run `make demo-check` + the finale sequence once, honestly recorded; `make claims-lint` (must end green after the revert).
- **Generated artifacts:** none.
- **Claim-safety:** the runbook quotes claim wording only from the claims guide/contract; it never introduces new phrasings.
- **Failure modes:** scripting unbuilt surfaces; the finale mutation accidentally committed (revert is part of the script).
- **Rollback:** revert docs.
- **Demo value:** very high (it *is* the demo). **Research value:** low. **CV value:** medium.
- **Model/effort:** **Terra, medium** (Sol acceptable if all referenced surfaces shipped). **Depends:** whatever has shipped; hard floor: R2-STAT-UI-01. **Commit:** yes. **/clear after:** yes.

---

## Phase 2 remaining order (dependency-sorted)

R2-GOV-01 → R2-STAT-UI-01 → R2-CONTRACT-02 → R2-LINEAGE-01 → R2-SKEPTIC-01 → R2-AUTOPSY-01 → R2-CAL-01 → R2-REAL-01 → R2-REGIME-01 → R2-LOOP-01 → R2-COURT-01 → R2-FRICTION-01 → R2-DEMO-01

Hard dependencies: AUTOPSY needs STAT-UI (endpoint reuse) and CONTRACT-02 (registration procedure); COURT needs SKEPTIC + AUTOPSY; FRICTION needs AUTOPSY + CONTRACT-02; REGIME needs REAL (shared macro CSV); DEMO runs last. Everything else may interleave. CAL-01, REAL-01, and LOOP-01 have no dependents besides REGIME — they can shift later without blocking the demo path (STAT-UI → SKEPTIC → AUTOPSY → COURT → DEMO is the critical demo chain).

**Standing instruction for every executing agent:** on completion, add your task's row to the completion ledger above (ID, DONE, commit SHA + key artifact), add a TASK_STATE.md row, and recommend `/clear`. Do not begin a second queue task in the same context.

---

# Phase 3 — Execution truth & frontier queue (planned 2026-07-13)

## Phase 2 closure — COMPLETE (do not reopen)

**All Phase-2 tasks are done and committed.** Verified 2026-07-13 at HEAD `fbab761f` (clean tree): root suite **168/168 passed**, backend suite **85/85 passed** (both re-run, observed). The ledger rows above that still say "(uncommitted) … no commit by request" predate the owner's commits; the mapping from `git log --oneline` message text is unambiguous (e.g. `2985a86b` R2-STAT-UI-01, `253eedc5` R2-CONTRACT-02, `6af2c5be` R2-LINEAGE-01, `53a92a41` R2-SKEPTIC-01, `a95e1e1c` R2-AUTOPSY-01, `646fdae7` R2-CAL-01, `7124bdd8` R2-REAL-01, `d83741c2` R2-REGIME-01, `57ea8c05` R2-LOOP-01, `ef6a8030` R2-COURT-01, `b9fe263e` R2-FRICTION-01, `97e4fc33` R2-DEMO-01, `fbab761f` post-Phase-2 manifest). R3-GOV-01 owns writing those SHAs into the ledger rows; do not rewrite history entries, append corrections.

Planning provenance: candidate analysis, adversarial-review dispositions, dependency graph, waves, verification matrix, model allocation, and prioritized selections live in `FINANCEIQ_PHASE3_4_FRONTIER_PLAN.md`. Strategic framing lives in `FINANCEIQ_MOONSHOT_ROADMAP.md` §9. This queue holds the execution packets and is the single execution source of truth.

## Phase 3 completion ledger

| Task | Status | Evidence |
|---|---|---|
| R3-GOV-01 post-Phase-2 truth sync | DONE (awaiting owner commit) | 2026-07-13; `docs/VERIFICATION_BASELINE.md`; root/backend suites green, data VALID, claims lint passed; operating docs cite the baseline; Phase-2 commit reconciliation appended; no code, data, metric, generated artifact, MCC, or application behavior changed |
| R3-STAT-02 model disagreement atlas | DONE (awaiting owner commit) | 2026-07-13; isolated seedless rank-only 9×9 per-year Spearman matrices and per-ticker-year nine-model rank spread/IQR in `experiments/results_disagreement/`; source dump checksums embedded and verified; missing ranks yield explicit insufficient-data nulls; no raw cross-model score comparison, retraining, ranking/service change, or predictive-validity claim; registry ownership is `make research-disagreement` |
| R3-INF-01 leave-one-out influence diagnostics | DONE (awaiting owner commit) | 2026-07-13; isolated seedless per-observation ΔIC (`loo_pooled_ic − full_pooled_ic`) for 9 models × 80 tickers × 3 years in `experiments/results_influence/`; pooled IC reuses `experiments/significance.py` `spearman_ic` verbatim, pinned equal to `analyze_model` by a test; both signs + per-model top-5 |Δ| concentration reported; missing/boundary rows yield explicit insufficient-data nulls; run-twice byte-identical; canonical/`results_disagreement`/`data/trusted_clean` byte-unchanged; no retraining, ranking, service, or significance change and no mispriced-stock/opportunity/edge claim; registry ownership is `make research-influence` |

## Universal rules for Phase-3 tasks (in addition to the Phase-2 universal rules above, which all still apply)

- **Never overwrite a canonical artifact.** Every new evaluation writes to a **new, separately named** `experiments/results_<name>/` directory. `experiments/leaderboard.csv`, `experiments/results/significance_report.*`, `experiments/results/predictions_test_*.csv`, `experiments/results/{calibration,friction}_report.*`, `experiments/results_real_terms/`, `experiments/results_regime/`, and everything in `data/trusted_clean/` change only via their owning Makefile targets, and only in a task that explicitly owns regeneration (no Phase-3 task below does).
- **Harness/significance-running tasks (R3-SERV-01, R3-NULL-01, R3-TGT-01) must each run in a fresh context, never together**, and must record `shasum experiments/leaderboard.csv` plus the canonical significance report before and after — any drift = STOP and report.
- **Determinism is mandatory:** every new generator is seeded (or seedless arithmetic), run twice, and checksum-compared in the task's verification.
- **Missing data stays null.** New diagnostics report reduced n rather than filling anything; `insufficient_data` is a valid, expected output state.
- **Every quotable statistic ships paired:** raw p-values never appear without their family-correction context; per-split values are labeled exploratory; every new artifact embeds a `limitations` array and a claim-safety statement in its JSON/MD.
- **MCC discipline:** new user-facing copy is specified verbatim in the packet; new pages/services are registered in `model_confidence_contract.json` per the R2-CONTRACT-02 procedure (version bump + review); only one MCC-bumping task per context.
- **Once R3-REL-01 lands,** every task that creates a generated artifact must also register it in `artifact_registry.json` and keep the registry test green.
- **Model key:** Sol/Terra/Opus/Fable as defined at the top of this file. Current-label mapping and the independent-review rule are in `FINANCEIQ_PHASE3_4_FRONTIER_PLAN.md` §7. Tasks marked "independent review: Fable (mandatory)" may not be self-reviewed by their implementer's model family.
- **Commits:** the owner commits manually (established Phase-2 convention). Finish each task with a clean, reviewable working tree, a proposed commit-message theme, and `git diff --check` clean; never run `git commit`/`git push`.

---

### R3-GOV-01 — Post-Phase-2 truth sync + verification baseline
- **Priority:** P1. **Wave:** 3A (first). **Owner role:** Docs/Verification. **Risk:** low. **Model/effort:** **Sol, low.** Independent review: none (self-verifying).
- **Verified pre-execution state (2026-07-13):** all Phase-2 work committed at `fbab761f`; suites observed root 168/168, backend 85/85; yet `CLAUDE.md`/`AGENTS.md` "Known test-suite state" said 106/55 and 95-97/51 respectively; `PRD.md` Current Reality said 51/51 + 95/97 and carried a stale page inventory. The R3-GOV-01 recount found 23 page files and 27 `<Route>` declarations in `frontend/src/App.jsx` (22 page-rendering, 5 redirects). `FINANCEIQ_DEMO_AND_CLAIMS_GUIDE.md` §2 said "148 automated tests", §15 said 51/51 + 97/97; `FINANCEIQ_SMALL_MODEL_RULES.md` §9/§12 said 51/97; `TASK_STATE.md` + this queue's Phase-2 ledger carried stale "(uncommitted)" notes; `.agent/memory/01-generated-experiment-summary.md` cited a stale sentence that no longer exists in the regenerated `experiments/results/experiment_summary.md` (grep 0 hits).
- **Read first:** this packet; `git log --oneline -20`; the exact sections named above (grep, do not read files whole).
- **Files to modify:** `CLAUDE.md`, `AGENTS.md` (keep byte-identical to each other), `PRD.md`, `FINANCEIQ_DEMO_AND_CLAIMS_GUIDE.md`, `FINANCEIQ_SMALL_MODEL_RULES.md`, `TASK_STATE.md`, this file (ledger SHAs), `.agent/memory/01-…` (evidence note only — the lesson itself stands). New (proposed): `docs/VERIFICATION_BASELINE.md` — a short dated ledger of observed suite counts/data-validate output; after creating it, the docs above should state counts once and cite this file for "current baseline" instead of embedding numbers in multiple places where feasible without restructuring.
- **Do NOT touch:** any source code, generated artifacts, METHODOLOGY numbers, the MCC, or the Phase-2 ledger's existing text (append SHA corrections; never rewrite/delete rows).
- **Steps:** (1) Run both suites, `make data-validate`, `make claims-lint`; record observed output. (2) Write `docs/VERIFICATION_BASELINE.md` with date, git SHA, and observed results. (3) Update each stale section to the observed state, citing the baseline file. (4) Add commit SHAs to the Phase-2 ledger rows and TASK_STATE rows using `git log` message mapping (listed in the Phase-2 closure note above). (5) Update the small-model rules baselines and add the Phase-2 services (`skeptic_service.py`, `courtroom_service.py`, `analyst_verdict_service.py`, `services/research/significance.py`) to its §4 unsafe list. (6) Fix the memory lesson's evidence note.
- **Acceptance criteria:** no operating-layer doc states a count that the fresh run contradicts; every Phase-2 ledger row has a commit SHA; `CLAUDE.md` == `AGENTS.md`; diff is docs-only.
- **Verification:** `PYTHONPATH=. python -m pytest tests/`; `PYTHONPATH=backend python -m pytest backend/tests`; `make data-validate`; `make claims-lint`; `diff CLAUDE.md AGENTS.md`; `git diff --stat` (docs only).
- **Generated artifacts:** none (VERIFICATION_BASELINE.md is hand-written from observed output, dated).
- **Failure modes / misleading success:** editing a count without running the suite (forbidden); "fixing" ledger history instead of appending; letting CLAUDE.md and AGENTS.md drift apart. A green claims-lint does NOT validate these docs (they are outside its scan — say so in the report).
- **Rollback:** revert doc edits. **Stop conditions:** a suite failure you did not cause; any count that cannot be reproduced.
- **Commit scope:** docs only. **Theme:** "Sync operating docs to post-Phase-2 truth". Manual commit by owner: yes. **/clear after:** yes.

### R3-LINT-01 — Docs link & path lint
- **Priority:** P2. **Wave:** 3A. **Owner role:** Ops/Docs. **Risk:** low. **Model/effort:** **Sol, low** (Terra if allowlist design proves fiddly). Review: none.
- **Verified current state:** the dead `unnecessary/README.md` link was a real past incident (OPS-02); `docs/DEMO_RUNBOOK.md`, the planning layer, and METHODOLOGY now carry dozens of relative links and cited paths that nothing checks.
- **Read first:** `scripts/lint_claims.py` (style precedent), `Makefile` (additive-target pattern).
- **Files to modify:** `Makefile` (additive `docs-lint` target only). New (proposed): `scripts/lint_doc_links.py` (stdlib only), optional `tests/test_doc_links.py` wrapper.
- **Do NOT touch:** existing lint script, MCC, any non-doc file.
- **Design (binding):** scan `*.md` at repo root and `docs/` for relative markdown links and backticked repo paths that look like files (heuristic: contains `/` and an extension or a known top-level dir); verify existence; support an explicit in-script allowlist (with reasons) for intentional examples and "(proposed)" paths; exit non-zero with `file:line` diagnostics. Do not fetch URLs (no network).
- **Acceptance criteria:** lint passes on the current tree after any genuinely dead links found are either fixed (docs-only edits, listed in the report) or allowlisted with reasons; deliberately adding a fake link makes it fail (mutation check recorded, then reverted).
- **Verification:** `make docs-lint` ×2; mutation check; root suite if the test wrapper was added.
- **Generated artifacts:** none. **Failure modes:** over-eager path heuristic flagging prose (keep it dumb; allowlist); "fixing" a link by creating its target (forbidden — fix the link). **Rollback:** delete new files, revert Makefile hunk. **Stop:** if >20 dead links appear, report instead of mass-editing.
- **Commit scope:** script + Makefile + doc link fixes. **Theme:** "Add docs link lint". Manual commit: yes. **/clear:** yes.

### R3-MCC-01 — MCC evidence-state consistency guard
- **Priority:** P1. **Wave:** 3A. **Owner role:** Governance/Test. **Risk:** low-medium. **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** `model_confidence_contract.json` v1.7.0 `evidence_basis[].finding` strings and `evidence_state` flags are hand-asserted; the only binding test (`backend/tests/test_confidence_contract.py`) pins `reliable_predictive_edge_observed is False` against the forecasting service. Nothing compares the contract to `experiments/results/significance_report.json` (whose `headline.significant_fwer_0_05` and `headline.conclusion` exist — verified), the regime report, or the friction report. If a future regeneration changed a conclusion, the contract would drift silently.
- **Read first:** `model_confidence_contract.json` (whole), `backend/tests/test_confidence_contract.py`, `tests/test_contract_coverage.py`, the `headline`/`limitations` keys of the three cited reports.
- **Files to modify:** new root test `tests/test_contract_evidence_state.py` (proposed); `model_confidence_contract.json` **only if** a machine-checkable key needs adding (then minor version bump per procedure + review note).
- **Do NOT touch:** `scripts/lint_claims.py` behavior, contract rules/allowlist, any report.
- **Design (binding):** assertions — (a) `evidence_state.ml_family_wise_significant == significance_report.headline.significant_fwer_0_05`; (b) each `evidence_basis[].path` exists; (c) each `evidence_basis[].finding` is consistent with its report via a pinned, documented check (exact-substring against the report's own conclusion/claim-safety text where available; otherwise pin the specific field, e.g. regime's `not_computed_insufficient_regime_diversity`); (d) `evidence_state.conclusion == "no reliable predictive edge"` matches `approved_wording.headline_conclusion` semantics. The test must FAIL if any cited report's conclusion changes — that failure is the feature (forces the documented MCC review flow).
- **Acceptance criteria:** test green on current tree; a temporary mutation of a report copy in a test fixture (never the committed report) demonstrates the failure path; root suite green; `make claims-lint` green.
- **Verification:** `PYTHONPATH=. python -m pytest tests/`; `make claims-lint`; mutation demonstration recorded.
- **Generated artifacts:** none. **Failure modes / misleading success:** asserting on prose so loosely the test can never fail (vacuous guard); mutating committed reports to test the failure path (forbidden — use fixtures). **Rollback:** delete the test; revert contract bump if made. **Stop:** if finding-strings cannot be bound to any machine field, report the specific mismatch rather than weakening the assertion.
- **Commit scope:** one test (+ optional contract bump). **Theme:** "Bind MCC evidence state to committed reports". Manual commit: yes. **/clear:** yes.

### R3-REL-01 — Artifact ownership & regeneration registry + staleness guard
- **Priority:** P1. **Wave:** 3A. **Owner role:** Governance/Backend. **Risk:** medium. **Model/effort:** **Opus, medium.** Review: Terra low.
- **Verified current state:** ~40 generated files across `experiments/results*/` and `data/trusted_clean/` are owned by 15+ Makefile targets, mapped only in prose; `significance_report.json` and `friction_report.json` embed `source_artifacts` checksums that nothing re-verifies; run manifests cover runs, not files.
- **Read first:** `Makefile` (all target definitions), `ls` of `experiments/results*`, `data/trusted_clean/`, the `source_artifacts` keys of the significance/friction reports, METHODOLOGY §Run-directory governance.
- **Files to modify:** new root `artifact_registry.json` (proposed — hand-curated, reviewed; NOT generated), new `tests/test_artifact_registry.py` (proposed), METHODOLOGY (≤8-line "Artifact registry" note under Reproducibility).
- **Do NOT touch:** any generated artifact, any generator, the Makefile (registry cites existing targets; adds none).
- **Design (binding):** registry entries `{path_or_glob, generator_command, inputs[], hand_edit_forbidden: true|false, notes}`. Test asserts: (1) every registry path/glob matches ≥1 existing file; (2) every file under `experiments/results*/` and `data/trusted_clean/` (excluding `runs/**`, covered by one glob entry citing the manifest governance rule) is matched by exactly one entry — an unregistered file fails the suite; (3) for reports embedding `source_artifacts` checksums, recompute and compare — mismatch fails with a "stale evidence: regenerate X via Y or investigate" message; (4) registry wording stays neutral (ownership, not validity certification).
- **Acceptance criteria:** full coverage with zero unmatched files; staleness check green on current tree; deliberately planting an unregistered temp file (removed after) fails the test; root suite green.
- **Verification:** `PYTHONPATH=. python -m pytest tests/` ×2 (determinism of the checksum pass); the planted-file mutation check, recorded.
- **Generated artifacts:** none (the registry is curated input, like the MCC). **Failure modes / misleading success:** globs so broad the coverage check is vacuous (each glob must be justified in `notes`); registering a file with a wrong generator (reviewer spot-checks 5 entries against the Makefile). **Rollback:** delete registry + test, revert METHODOLOGY note. **Stop:** any file whose generator cannot be identified from Makefile/scripts — list it as `"generator": "UNKNOWN — owner decision"` and report, never guess.
- **Commit scope:** registry + test + doc note. **Theme:** "Add generated-artifact ownership registry". Manual commit: yes. **/clear:** yes.

### R3-LEGACY-01 — Legacy DB scoring/sector path demarcation audit (memo only)
- **Priority:** P2. **Wave:** 3A. **Owner role:** Backend/Architecture. **Risk:** low (no code change). **Model/effort:** **Opus, medium.** Review: Terra low.
- **Verified current state:** a second, DB-backed scoring path exists beside the honest CSV path: `backend/app/services/scoring_service.py`, `adaptive_weights_service.py`, `sector_service.py` (z-scores with `MIN_PEERS = 2` over `Company.sector_code` strings whose provenance the trusted pipeline never validates — METHODOLOGY §Sector-label provenance), router `scoring.py` (no prefix), and `frontend/src/pages/ForecastingDetailPage.jsx` calling legacy `/predict/trends` + `/predict/heatmap` with a `sector` param. The trusted loader leaves sector fields null, so these surfaces are only as good as whatever external workflow populated the DB.
- **Read first:** the four backend files above + `routers/scoring.py`, `routers/forecasting.py` (route inventory), `ForecastingDetailPage.jsx`, METHODOLOGY §Sector-label provenance.
- **Files to modify:** new `docs/LEGACY_DB_PATH_AUDIT.md` (proposed) only. **Do NOT touch:** any source file, route, or test — this task changes zero behavior.
- **Deliverable (binding):** the memo maps (a) exactly which routes/pages reach the legacy path and from where; (b) which computations depend on unprovenanced DB fields (sector z-scores, adaptive weights); (c) concrete claim hazards (e.g. a 2-peer sector z-score rendered as if meaningful); (d) three owner options with consequences: runtime "legacy — provenance unvalidated" labeling; quarantine behind an env flag; retirement. It recommends one option but decides nothing — the decision line is explicitly "owner decision required".
- **Acceptance criteria:** every claim in the memo carries a `file:line` citation; backend suite untouched and green; no diff outside the new doc.
- **Verification:** `PYTHONPATH=backend python -m pytest backend/tests`; `git diff --stat` (one new doc).
- **Failure modes:** "helpfully" fixing anything while auditing (forbidden); overstating danger (the path may be unreachable in default deployments — verify and say which). **Rollback:** delete the doc. **Stop:** n/a.
- **Commit scope:** one doc. **Theme:** "Audit legacy DB scoring path ownership". Manual commit: yes. **/clear:** yes.

### R3-E2E-01 — Authenticated E2E/visual verification spike (memo only)
- **Priority:** P1. **Wave:** 3A. **Owner role:** Ops/Frontend. **Risk:** medium (design decision; zero code). **Model/effort:** **Opus, medium.** Review: Terra low.
- **Verified current state:** four Phase-2 reports were blocked on "protected-page visual verification blocked by missing approved Supabase session"; `frontend/tests/e2e-forecasting.spec.js` assumes open email/password signup, inconsistent with Supabase auth + the private lockdown; the demo runbook forbids weakening auth for presentations.
- **Read first:** `frontend/src/App.jsx` `Protected` implementation and the Supabase client setup it relies on; `docs/SUPABASE_AUTH.md`; `backend/tests/test_supabase_jwks.py` (HS256 legacy fallback evidence); `frontend/tests/e2e-forecasting.spec.js`; `frontend/package.json` e2e script.
- **Files to modify:** new `docs/E2E_AUTH_SPIKE.md` (proposed) only. **Do NOT touch:** auth code, env files, deployment configs, the existing spec.
- **Deliverable (binding):** evaluate, with repo-cited feasibility notes, at minimum: (1) Playwright `storageState` reuse after a documented one-time manual login against a local/dev Supabase project; (2) a dedicated local-only Supabase test project with a seeded approved user (env-only credentials, never committed); (3) API-level-only assertion expansion using the backend's existing verification paths, accepting that page visuals stay manual. Hard constraints stated in the memo: no auth weakening, no bypass flags in production code paths, no committed secrets, no CAPTCHA circumvention. Ends with one recommendation + a drafted follow-up implementation task (added to the Later backlog section below by the executing agent, not started).
- **Acceptance criteria:** memo cites real files for every feasibility claim; no diff outside the doc; the recommended approach names its exact env prerequisites.
- **Verification:** `git diff --stat` (one new doc). **Failure modes:** recommending a bypass flag (forbidden); assuming Supabase features not evidenced in config/docs (mark "needs verification"). **Rollback:** delete doc. **Stop:** if no approach satisfies the constraints, that finding IS the deliverable.
- **Commit scope:** one doc. **Theme:** "E2E auth spike". Manual commit: yes. **/clear:** yes.

### R3-STAT-02 — Model disagreement atlas (artifact)
- **Priority:** P2. **Wave:** 3B (safe starter). **Owner role:** Research. **Risk:** low. **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** `experiments/results/predictions_test_{2023,2024,2025}.csv` hold 9 models × 80 rows/split (model list verified: 3 baselines + 6 ML); no artifact measures cross-model agreement; Skeptic check 3 uses rank flips per ticker but no matrix exists.
- **Read first:** one prediction dump (columns: ticker, year, model, y_true, y_pred), `experiments/significance.py` (loading/rank conventions — ranks are within-year, within-model; y_pred scales differ by model and must never be compared by magnitude), `experiments/friction_sim.py` (deterministic-module style).
- **Files to modify:** new `experiments/disagreement_atlas.py` (proposed); new `experiments/results_disagreement/` outputs `disagreement_report.{json,md}` + `disagreement_matrix.csv` (proposed); `Makefile` additive target `research-disagreement`; new `tests/test_disagreement_atlas.py` (proposed).
- **Do NOT touch:** the dumps (inputs only), any canonical artifact, any service.
- **Design (binding):** per year: 9×9 pairwise Spearman between models' within-year y_pred ranks; per ticker-year: cross-model rank spread (max−min and IQR of the 9 within-model ranks). Seedless arithmetic — deterministic. Report embeds `source_artifacts` with checksums, a `limitations` array (81-ticker training universe; three years; disagreement describes model instability, not opportunity), and this claim-safety sentence verbatim: "**Model disagreement measures instability of a signal already indistinguishable from the null; high agreement between models is not evidence of predictive validity.**"
- **Acceptance criteria:** run twice → byte-identical; matrix values hand-checked for one model pair in a unit test fixture; root suite green + new tests; no canonical artifact changed (`shasum` before/after recorded); registry updated if R3-REL-01 landed.
- **Verification:** `PYTHONPATH=. python -m pytest tests/`; `make research-disagreement` ×2 + `shasum` compare; canonical `shasum` gates.
- **Generated artifacts:** `experiments/results_disagreement/*` — owner: `make research-disagreement`; never hand-edit.
- **Failure modes / misleading success:** comparing y_pred magnitudes across models (the documented scale trap); reading "models agree" as robustness (the verbatim sentence is the guard). **Rollback:** delete new files + Makefile hunk. **Stop:** dumps missing/columns differ from spec.
- **Commit scope:** module + outputs + test + Makefile. **Theme:** "Add model disagreement atlas". Manual commit: yes. **/clear:** yes.

### R3-INF-01 — Influence diagnostics (leave-one-out IC influence)
- **Priority:** P2. **Wave:** 3B. **Owner role:** Research. **Risk:** low-medium. **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** dumps permit exact leave-one-out recomputation of within-year Spearman per model; extreme TRY-era observations are a documented concern; no influence measurement exists (`coverage_impact.csv` measures feature coverage, not observation influence).
- **Read first:** `experiments/significance.py` (pooled-IC definition: equal-weighted mean of three within-year Spearman ICs — reuse it exactly), one dump.
- **Files to modify:** new `experiments/influence_map.py` (proposed) → `experiments/results_influence/influence_report.{json,md}` + `influence_by_observation.csv` (proposed); Makefile additive `research-influence`; new root test.
- **Design (binding):** for each model and each ticker-year in the dumps: Δ pooled IC when that observation is removed (recompute the year's Spearman on n−1, re-pool). Deterministic, seedless. Report top-|Δ| observations with the ticker's year and both signs, plus per-model influence concentration (share of |Δ| held by top 5 observations). Claim-safety sentence verbatim: "**Influence values describe the sensitivity of a null-consistent estimate to single observations; they do not identify mispriced stocks or opportunities.**" `limitations` array mandatory.
- **Acceptance criteria:** hand-computed fixture (5 rows) pins the LOO arithmetic; run twice → identical; canonical checksums unchanged; root suite green.
- **Verification:** root pytest; `make research-influence` ×2 + shasum; canonical shasum gates.
- **Generated artifacts:** `experiments/results_influence/*` — owner `make research-influence`.
- **Failure modes:** recomputing pooled IC with a different convention than `significance.py` (numbers would silently disagree — reuse its function or pin equality in a test); narrating influential names as stories. **Rollback:** delete new files + hunk. **Stop:** convention mismatch you cannot resolve by import/reuse.
- **Commit scope:** module + outputs + test + Makefile. **Theme:** "Add leave-one-out influence diagnostics". Manual commit: yes. **/clear:** yes.

### R3-STAT-01 — Ranking & cohort stability under resampling
- **Priority:** P1. **Wave:** 3B. **Owner role:** Research/Statistics. **Risk:** medium (claim surface). **Model/effort:** **Opus, high.** Independent review: **Fable, medium** (design + wording).
- **Verified current state:** dumps committed; nothing measures how fragile per-ticker ranks or the pooled IC are under cohort perturbation; the public-40 subset of the 80-row dumps has never been evaluated separately (universe configs at `data/config/universe_public_40.csv` identify the subset).
- **Read first:** `experiments/significance.py` (bootstrap conventions: tickers resampled with replacement within year, 10k draws, seeded — mirror them), one dump, `data/config/universe_public_40.csv`.
- **Files to modify:** new `experiments/rank_stability.py` (proposed) → `experiments/results_rank_stability/rank_stability_report.{json,md}` + `stability_by_ticker.csv` (proposed); Makefile additive `research-rank-stability`; root tests.
- **Design (binding):** seeded (`numpy.random.default_rng(fixed)`), B≥2000/year: (a) per model/year, bootstrap tickers within year → each ticker's top-10 membership frequency (conditional on being drawn) and rank interval (2.5–97.5 pct of within-sample rank); (b) jackknife leave-k-out (k=1 exact, k=8 sampled) → distribution of pooled IC per model; (c) cohort restriction: recompute per-model pooled IC on dump rows whose tickers are in the public-40 config, reporting per-year n. All outputs carry the universe label ("81-ticker training universe; public-40 subset where stated"). Claim-safety verbatim, in report and any future surface: "**Stability frequencies describe resampling variability of a ranking already indistinguishable from the null; a frequently-top-ranked ticker is not a validated pick.**" Raw/adjusted pairing rules apply to any p-value republished from the significance report.
- **Acceptance criteria:** seeded rerun → byte-identical; fixture test pins bootstrap membership-frequency arithmetic on a 6-row toy; public-40 subset ICs reported with per-year n; canonical checksums unchanged; root suite green.
- **Verification:** root pytest; `make research-rank-stability` ×2 + shasum; canonical shasum gates; reviewer reads the MD against the do-not-claim register.
- **Generated artifacts:** `experiments/results_rank_stability/*` — owner `make research-rank-stability`.
- **Failure modes / misleading success:** presenting membership frequency as pick-confidence (the named trap — verbatim sentence guards); bootstrap that resamples across years (breaks the within-year design); quietly comparing public-40 IC to the 80-row IC as if a "which cohort is better" result (it is a sensitivity description only — the report must say so).
- **Rollback:** delete new files + hunk. **Stop:** if the public-40 subset leaves any year with n<30, report per-year n and label those cells `insufficient_data` rather than deciding alone to publish them.
- **Commit scope:** module + outputs + tests + Makefile. **Theme:** "Add rank and cohort stability diagnostics". Manual commit: yes. **/clear:** yes.

### R3-TGT-01 — Excess-return-basis significance treatment
- **Priority:** P2. **Wave:** 3B (harness-running — fresh context, never with SERV-01/NULL-01). **Owner role:** Research. **Risk:** medium. **Model/effort:** **Opus, medium** (Terra acceptable given the R2-REAL-01 precedent). Review: Opus low.
- **Verified current state:** `next_year_excess_return_vs_bist100` is a dataset column already evaluated at leaderboard level (`experiments/results/leaderboard_by_target.csv` targets verified) but has no prediction dumps and no significance treatment; the currency bases got exactly this treatment in R2-REAL-01 via `build_panel_for_target` + reused significance machinery into an isolated directory.
- **Read first:** `experiments/run_alternative_targets.py` and the R2-REAL-01 pattern (`Makefile` `research-real-terms` target, `experiments/results_real_terms/` layout), METHODOLOGY §Alternative return-basis evaluation.
- **Files to modify:** Makefile additive `research-excess`; possibly a thin runner `experiments/run_excess_basis.py` (proposed) if the real-terms runner cannot be pointed at the excess column without code edits — prefer parameter reuse; outputs to `experiments/results_excess/` (proposed); root test; METHODOLOGY paragraph mirroring the real-terms section's structure and qualifiers.
- **Design (binding):** same nine models, same splits, same seeded significance machinery; **null benchmark coverage shrinks n** — report evaluated n per year explicitly; nulls stay null. Every quoted IC ships with raw AND Bonferroni-adjusted p. METHODOLOGY paragraph must close with the standard sentence pattern: descriptive historical research result; does not establish signal, investment value, or a reliable predictive edge.
- **Acceptance criteria:** `make data-validate` unchanged; canonical leaderboard + significance report checksums identical before/after; new dir self-consistent; two-run checksums identical; root suite green.
- **Verification:** shasum gates on `experiments/leaderboard.csv` + `experiments/results/significance_report.json`; `make research-excess` ×2 + shasum; root pytest; `make data-validate`.
- **Generated artifacts:** `experiments/results_excess/*` — owner `make research-excess`.
- **Failure modes:** promoting an uncorrected per-split excess IC ("signal vs benchmark!" is the misquote to pre-kill — the METHODOLOGY qualifier is mandatory); silently dropping rows without reporting per-year n. **Rollback:** delete new dir + hunks. **Stop:** if the harness cannot target the excess column without touching metric code, report the minimal refactor instead of improvising (same rule as R2-REAL-01 step 6).
- **Commit scope:** runner/Makefile + outputs + test + METHODOLOGY paragraph. **Theme:** "Add excess-return-basis significance". Manual commit: yes. **/clear:** yes.
- **Owner amendment — isolated excess-target fitting authorization (2026-07-22; append-only; supersedes nothing above):** this amendment resolves the contract conflict between the R3-TGT-01 requirement to produce row-level excess-target prediction dumps for significance inference and the earlier blanket "Do not retrain models" constraint. **The "Do not retrain models" constraint is narrowed for this task only**; it remains in full force everywhere else in the queue.
  - **Forbidden (unchanged, and restated so the narrowing cannot be read broadly):** retraining, replacing, overwriting, or reinterpreting the canonical nominal models; changing nominal prediction dumps; changing nominal leaderboards; changing nominal significance artifacts; changing production models or deployment behavior; hyperparameter search of any kind; adding or removing model families; selecting or altering specifications on the basis of favorable excess-target results; and treating nominal predictions as though they were excess-target predictions.
  - **Explicitly authorized:** isolated, leakage-safe walk-forward fitting of the existing frozen nine model specifications against the trusted dataset target `next_year_excess_return_vs_bist100`, using the existing frozen splits, preprocessing, hyperparameters, and seeds; creation of row-level per-ticker excess-target prediction dumps confined to the separate `experiments/results_excess/**` namespace; reconstruction of aggregate excess leaderboards **only** from those new row-level dumps; the prespecified equal-year Spearman analysis, 10,000 within-year permutations, 10,000 ticker bootstraps, and the already-defined six-model Bonferroni correction; recording of target, generator, seed, source artifacts, checksums, regeneration command, and disagreement results; and comparison of the reconstructed leaderboard against the pre-existing read-only excess leaderboard.
  - **Disagreement handling:** any disagreement between the reconstructed leaderboard and the pre-existing aggregate excess leaderboard **must be reported, not patched, reconciled, or silently absorbed.** The pre-existing leaderboard stays read-only.
  - **Scope characterization:** this is isolated research fitting for significance inference, **not production retraining.** It does not alter the committed no-reliable-edge conclusion, and no result obtained under this authorization may be used to revise that conclusion within this task.
  - **Review status:** this authorization covers the fitting activity only. The implementation and its generated outputs still require independent review before the owner considers a commit; the amendment does not approve, accept, or merge anything.
  - **Sequencing note (recorded for audit honesty):** the owner authorization **preceded acceptance of the implementation.** The implementation agent stopped at the conflict, requested permission, and then proceeded under the explicit owner decision; this governance text reconciles the queue after that agent stopped, so the written record post-dates the decision it records while the decision itself came first.
- **Independent repository technical review (2026-07-22; append-only; verdict APPROVED — technical scope only):** a separate-context independent repository technical review of the uncommitted implementation on branch `local/r3-tgt-01-excess-implementation` at base HEAD `26051a35992ea65b789c35f715d7c3aa5cae434c`, conducted in review worktree `local/r3-tgt-01-independent-review`, returned **APPROVED** for the repository-technical scope.
  - **Reviewed scope (complete):** isolated excess-target fitting; row-level prediction dumps; ticker-cluster bootstrap; within-year permutation; the six-model Bonferroni family; family-symmetric reporting; generated-artifact provenance; write isolation; malformed-input refusal; deterministic regeneration; the protected-artifact gate; METHODOLOGY; and claim safety.
  - **Independently observed verification:** focused tests **134/134 passed**; root tests **490/490 passed**; `make data-validate` **VALID** (403 modeling rows, 40 features, 321 target rows, benchmark available); docs lint **PASSED**; claims lint **PASSED** against MCC **v1.8.0**; two isolated successful generations produced all seven outputs **byte-identical to each other and to the implementation checkout**; the protected gate covered **351 files with zero missing, zero added, and zero SHA-256 mismatches**; the 27-row excess leaderboard reproduced with **zero mismatches at rtol=0 and atol=1e-12**.
  - **Substantive findings (unchanged by this review):** **no ML model survives the six-model family-wise correction**, and **no reliable predictive edge is established.**
  - **Technical boundaries (binding):** repository technical review is approved. **Qualified independent human statistical/domain review remains mandatory before COMMIT_READY.** Governance correctness and green tests **do not establish predictive validity.** The task must remain **REVIEW_PENDING** and must **not** be marked APPROVED overall, COMMIT_READY, MERGE_COMPLETE, or CLOSED.
- **Human and advisory statistical review dispositions (2026-07-22; append-only; supersedes nothing above):** three review dispositions were supplied by the owner against the uncommitted implementation on `local/r3-tgt-01-excess-implementation` at base HEAD `26051a35992ea65b789c35f715d7c3aa5cae434c`. They are recorded exactly as supplied. **No identity, institutional affiliation, credential, or human status is asserted for any reviewer beyond what the owner supplied**; where the owner did not supply it, it is not inferred here.
  - **Review A — disposition: CHANGES_REQUIRED (binding).** Two binding concerns:
    1. **Estimand mislabeling.** Within each evaluation year, subtracting the same annual BIST100 return from every stock leaves the cross-sectional outcome ranks unchanged. The within-year Spearman estimand must therefore be described as **ordinal cross-sectional ranking performance**, and **not** as benchmark-relative magnitude prediction, alpha, economic outperformance, or investment value.
    2. **Permutation null does not preserve trajectories.** The original independently-within-year permutation does not preserve repeated-ticker trajectories across years. A **trajectory-preserving shared-ticker permutation sensitivity** must be added and corrected over the same frozen six-model family.
  - **Review B — disposition: APPROVED with non-blocking recommendations.** Recommendations (non-blocking): explicitly disclose multiplicity across the nominal, real-TRY, USD, and excess target bases; document the coincident baseline results without presenting them as independent baseline diversity; disclose the limited scope of the supplied human-review package.
  - **Review C — disposition: ADVISORY ONLY / NOT A HUMAN STATISTICAL SIGN-OFF.** Supportive advisory findings: the ticker-cluster bootstrap is appropriate; the existing family-wise non-rejection is defensible; no outcome-selected headline is present; negative IC values must not be interpreted as inverse alpha or a contrarian signal; and the package did not independently reproduce the complete fitting and feature-construction layer. **Review C does not count toward, and does not satisfy, the qualified-human statistical/domain review gate.**
  - **Net effect:** the conservative disposition governs. The human statistical/domain review gate is **not** satisfied by these dispositions taken together, and the task returns to **HUMAN_REVIEW_REOPENED / TECHNICAL_REREVIEW_REQUIRED**.
- **Owner disposition — narrow statistical correction authorized (2026-07-22; append-only; supersedes nothing above):** the owner **adopts the conservative CHANGES_REQUIRED disposition** and authorizes a **narrow correction pass** whose complete permitted scope is the eight items below. Nothing outside this list is authorized.
  1. Clarify the within-year Spearman estimand as **ordinal cross-sectional ranking** performance.
  2. **Preserve** the existing independently-within-year permutation as the **original primary analysis**, with its exact null hypothesis documented.
  3. Add a **reviewer-requested post-review trajectory-preserving ticker-permutation sensitivity**: draw one ticker permutation per iteration; apply the same ticker mapping across 2023, 2024, and 2025; preserve complete realized-return trajectories; retain year-local Spearman calculations; retain equal-year aggregation; use **10,000 permutations and the frozen seed**; and reapply the **frozen six-model Bonferroni correction**.
  4. Report **primary and sensitivity results side by side**.
  5. Designate the **nominal-return family as the sole confirmatory family**. Real-TRY, USD, and excess-return analyses are **exploratory robustness analyses**; their within-basis corrections **do not** control multiplicity across target bases.
  6. Explain the duplicate/coincident baseline results **only at the level supported by the persisted evidence**.
  7. Add a cautious note that predominantly negative IC signs may reflect sampling variation, feature orientation, or construction effects, and are **not** inverse alpha, a contrarian strategy, or actionable evidence.
  8. Disclose that the compact human-review package **principally supports review of the persisted prediction-to-significance layer**, not standalone reproduction of feature construction and model fitting.
  - **Explicitly prohibited in this correction pass:** modifying the trusted dataset; adding companies or years; changing splits; changing features; changing model specifications, hyperparameters, or seeds; fitting additional models; changing the six-model inferential family; deleting or silently replacing the original permutation analysis; presenting the reviewer-requested sensitivity as preregistered or prespecified; changing prediction dumps or the 27-row leaderboard; and making alpha, investment-value, recommendation, or production-validity claims.
  - **Dataset decision:** **R3-TGT-01 dataset expansion is not required and is not authorized in this correction pass.** Any future expansion must be a **separate preregistered task**, preferably adding genuinely **prospective** evaluation years rather than retrospectively enlarging the current task.
  - **Effect on the prior technical approval:** the 2026-07-22 independent repository technical review applies **only to the pre-amendment implementation**. It must be **rerun after the correction**. The task is **not COMMIT_READY**, **no final human approval exists**, and the task must **not** be marked APPROVED overall, COMMIT_READY, MERGE_COMPLETE, or CLOSED.
- **Independent repository technical rereview (2026-07-23; append-only; verdict APPROVED — repository-technical scope only):** a fresh independent repository technical rereview of the **amended R3-TGT-01 snapshot** (uncommitted working tree on branch `local/r3-tgt-01-excess-implementation` at base HEAD `26051a35992ea65b789c35f715d7c3aa5cae434c`) returned **APPROVED**. **Scope:** the amended snapshot; repository-technical approval only. This rereview **does not satisfy or replace the renewed qualified-human statistical/domain review**, and the task **remains NOT COMMIT_READY**.
  - **Independently verified findings:**
    1. Governance records are **append-only and coherent**.
    2. The human-review amendment remained **within the authorized scope**.
    3. The within-year **nominal-versus-excess rank audit** independently reproduced: **2023 — 40 tickers, 0 mismatches; 2024 — 40 tickers, 0 mismatches; 2025 — 40 tickers, 0 mismatches; total 120 rows, 0 rank mismatches.**
    4. The **original independently-within-year permutation analysis was preserved exactly**: 10,000 draws; seed 42; two-sided absolute-tail rule; Monte Carlo +1 correction; equal-year pooled IC; frozen six-model Bonferroni family; baselines outside the family.
    5. The **trajectory-preserving shared-ticker permutation sensitivity** was technically implemented correctly: one one-to-one ticker mapping per draw; identical mapping across 2023–2025; predictions fixed; complete realized-return trajectories moved; year-local Spearman recomputed; equal-year aggregation; 10,000 draws; seed 42; six-model Bonferroni correction.
    6. **Exact family conclusions:** primary — **0 of 6 reject**; trajectory sensitivity — **0 of 6 reject**; either analysis — **0 of 6 reject**.
    7. An **algorithmically independent 50,000-draw direct-permutation calculation** found all six sensitivity p-values **statistically compatible** with the persisted values and **preserved the 0-of-6 family conclusion**.
    8. **Ticker-cluster bootstrap intervals reproduced.**
    9. **Cross-basis multiplicity, coincident-baseline, negative-IC, package-scope, and scientific-claim disclosures are technically accurate.**
    10. **All seven governed excess artifacts reproduced deterministically.**
    11. **Prediction dumps, leaderboard, canonical nominal artifacts, trusted dataset, and the protected 351-file boundary remained unchanged.**
    12. **Validation results:** focused suite **165 passed**; full root suite **521 passed**; docs-lint **passed**; claims-lint **passed**; data-validation **passed**; two-run generation determinism **passed**.
  - **Non-blocking findings (recorded; not elevated to blockers):**
    - a previous implementation report **inaccurately described `Makefile` and `artifact_registry.json` as governance appends**; they are **pre-existing implementation/build-governance changes**, and **no unauthorized amendment edit was found**;
    - focused tests **do not independently pin the pre-amendment primary authority** or use a **different sensitivity RNG design**, but the independent technical rereview **supplied those checks**;
    - **argsort-based permutation generation** carries a **negligible finite-grid tie caveat**;
    - the study **remains limited by 40 tickers, three evaluation years, retrospective cohort membership, low power, and one unusual macro regime**.
  - **Memory-citation disclosure:** the reviewer used prior review summaries **solely to seed adversarial checks**. **No concern or qualification arises from that use**, because **all material findings were independently reconstructed and verified from repository evidence**. Memory summaries are **not statistical evidence** and are **not a substitute for repository verification**.
  - **Gate status (binding):** **no blocking repository-technical defect remains**; the task **may proceed to renewed qualified-human statistical/domain review**; **final human approval has not been received**; the task is **not COMMIT_READY, not merge-ready, and not closed**, and must **not** be marked HUMAN_REVIEW_APPROVED, COMMIT_READY, MERGE_COMPLETE, or CLOSED. Nothing here establishes a predictive edge, alpha, investment value, or production validity.
- **Owner governance amendment — qualified-human attestation unavailable; human-review gate waived by owner (2026-07-23; append-only; supersedes nothing above; all historical text above is preserved unchanged):** the owner records that a **verifiable qualified-human statistical/domain attestation for R3-TGT-01 could not be obtained**, and that this gate is therefore **explicitly waived by the owner for this internal repository task**. This amendment changes the **governance completion rule only**. It changes **no scientific evidence, no result, no artifact, and no claim**.
  - **No reclassification of any opinion as a human sign-off (binding):** **no AI system, no repository reviewer, no advisory model, no owner statement, and no unsigned methodological opinion is being reclassified as a qualified-human attestation.** No reviewer identity, affiliation, credential, qualification, or signature is asserted or invented anywhere in this record.
  - **Disposition of the prior HUMAN_REVIEW_CHANGES_REQUIRED items (substantively addressed):**
    1. corrected **ordinal cross-sectional ranking** estimand language;
    2. **zero nominal-versus-excess rank mismatches across 120 evaluation rows** (40 tickers × 2023/2024/2025);
    3. **preservation of the original independently-within-year primary permutation**;
    4. addition of the **reviewer-requested trajectory-preserving shared-ticker permutation sensitivity**;
    5. **symmetric six-model reporting**;
    6. a **separate six-model Bonferroni correction for each analysis**;
    7. **cross-basis multiplicity disclosure**;
    8. **coincident-baseline disclosure**;
    9. **negative-IC claim restrictions**;
    10. **package-scope clarification**.
  - **Independent repository technical rereview:** returned **APPROVED with no blocking defect** (recorded above, 2026-07-23; repository-technical scope only).
  - **Additional advisory statistical assessments:** independently reproduced or statistically validated the **pooled IC values**, the **primary permutation p-values**, the **trajectory-preserving sensitivity p-values**, **rank invariance**, **baseline coincidence**, and the **0-of-6 family-level conclusion under both analyses**. **Those advisory assessments explicitly stated that they are not qualified-human attestations.**
  - **Owner acceptance:** the owner **accepts the statistical and methodological corrections as sufficient for the internal project decision**, while **preserving all scientific limitations** stated above and below.
  - **Replacement completion standard (governance only):** in place of the waived qualified-human attestation, R3-TGT-01 closure requires all four of —
    a. **independent repository technical rereview APPROVED**;
    b. **multiple independent advisory statistical assessments supporting the corrected methodology**;
    c. **owner methodological acceptance**;
    d. **a final repository commit-readiness audit**.
  - **Permanent description restrictions (binding):** this task must **never** be described as qualified-human reviewed, human-statistician approved, externally validated, or independently human attested. **Any future genuine qualified-human review must be recorded separately and must never be backdated** onto this record.
  - **What the waiver does not establish:** the waiver establishes **no predictive edge, no alpha, no profitability, no investment value, no tradable-strategy evidence, no deployment validity, and no production validity.**
  - **Limitations that remain active and undiminished:** **low statistical power; 40 tickers; three evaluation years; retrospective cohort membership; cross-basis multiplicity.** **Dataset expansion remains outside R3-TGT-01 and is not required for closure** (any expansion remains a separate preregistered task, per the 2026-07-22 dataset decision above).
- **Advisory review dispositions D and E (2026-07-23; append-only; recorded as supplied; advisory only; neither is a qualified-human attestation; no identity, affiliation, qualification, or signature is asserted for either):**
  - **Advisory assessment D — disposition: STATISTICAL-MERITS APPROVED / NOT HUMAN SIGN-OFF.** Verified independently with a **different implementation, seed 7, and 20,000 draws**: **pooled ICs reproduced**; **all primary and sensitivity p-values compatible within Monte Carlo uncertainty**; **nominal-versus-excess ranks had 0 mismatches**; **baseline prediction values identical**; **both analyses retained the 0-of-6 conclusion**; and **both original binding concerns considered statistically resolved.** This assessment **does not satisfy the qualified-human gate.**
  - **Advisory assessment E — disposition: METHODOLOGICAL APPROVAL / NOT SIGNED QUALIFIED-HUMAN ATTESTATION.** Found: the **revised ordinal estimand explanation resolves the first concern**; the **shared-ticker sensitivity resolves the repeated-ticker concern**; **retaining the original analysis as primary is methodologically sound**; the **new analysis is adequately labeled non-prespecified**; **both six-model families yield 0-of-6 rejections**; and **the formal human gate cannot close on that assessment alone.**
  - **Net effect:** D and E are **advisory inputs to the owner's waiver decision only**. They do **not** constitute, satisfy, or substitute for a qualified-human statistical/domain attestation, and they change no artifact, no result, and no claim.
- **Post-waiver gate status (binding):** R3-TGT-01 is **QUALIFIED_HUMAN_ATTESTATION_UNAVAILABLE**, the human-review gate is **HUMAN_REVIEW_GATE_WAIVED_BY_OWNER**, and the task **may now proceed to the final repository commit-readiness audit** under the amended standard. The task is **not COMMIT_READY until that audit passes**, **no human-review approval exists**, and the task must **not** be marked HUMAN_REVIEW_APPROVED, QUALIFIED_HUMAN_VALIDATED, EXTERNAL_VALIDATION_COMPLETE, COMMIT_READY, MERGE_COMPLETE, or CLOSED.
- **Final repository commit-readiness audit (2026-07-23; append-only; supersedes nothing above; all historical text above is preserved unchanged) — verdict: APPROVED.**
  - **Scope (exact):** the **complete uncommitted R3-TGT-01 change set**; **repository coherence and commit readiness only**; conducted under the **owner-amended internal governance standard**. This audit is **not a qualified-human statistical attestation**, **not external validation**, and **not scientific proof of predictive value**.
  - **Audit snapshot:** detached audit checkout `/tmp/financeiq-r3-tgt-01-final-commit-audit-20260723`; base HEAD `26051a35992ea65b789c35f715d7c3aa5cae434c`; complete change-set digest `67f2f059b4c906ac47d719867aa59580adfd0b762e565f91db979e644e7cf8a4`.
  - **Independently verified findings:**
    1. The **complete task change set is coherent and complete**.
    2. **No missing, unrelated, orphaned, or partially implemented path** was found.
    3. **Governance truthfully records:** the original review history; the human-review **CHANGES_REQUIRED** disposition; the implemented corrections; **technical rereview approval**; **qualified-human attestation unavailability**; the **owner waiver**; the **replacement review standard**; and **no claim that human approval occurred**.
    4. **Scientific claim boundaries are internally consistent:** ordinal within-year cross-sectional ranking; no benchmark-relative magnitude or alpha interpretation; primary permutation retained; trajectory sensitivity labeled post-review and non-prespecified; nominal basis the sole confirmatory family; other target bases exploratory; no cross-basis multiplicity control claimed; no inverse-alpha or contrarian interpretation; non-rejection is not proof of zero true IC; **no reliable predictive edge established**.
    5. **Exact family conclusions:** primary analysis **0 of 6 reject**; trajectory-preserving sensitivity **0 of 6 reject**; either analysis **0 of 6 reject**.
    6. **Rank audit:** 2023 — 40 rows, 0 mismatches; 2024 — 40 rows, 0 mismatches; 2025 — 40 rows, 0 mismatches; **total 120 rows, 0 mismatches**.
    7. **All seven governed excess artifacts and manifest records are coherent.**
    8. The **27-row leaderboard and all three prediction dumps** were independently verified.
    9. The **protected 351-file boundary** had **zero missing files, zero added files, and zero changed files**.
    10. **Two isolated research-excess generations reproduced all seven outputs byte-identically.**
    11. **Validation results:** focused tests **165 passed**; full root suite **521 passed**; docs-lint **passed**; claims-lint **passed**; data-validation **passed**; `git diff --check` **passed**.
    12. **Security and repository hygiene checks found no blocking issue.**
  - **Blocking findings:** **None.**
  - **Non-blocking findings (recorded; not elevated to blockers):**
    1. Focused tests **share the production significance helper** for part of primary preservation and use a **production-equivalent argsort RNG design** for the sensitivity; independent audit calculations supplied distinct checks and **preserved the 0-of-6 conclusion**.
    2. The **argsort permutation generator** has a **negligible finite-grid tie-bias caveat** that does not threaten the one-to-one mappings or the conclusions observed here.
    3. `scripts/lint_doc_links.py` still labels `experiments/results_excess/` as a **future-output allowlist entry** although the path now exists; registry and root tests independently enforce ownership and presence.
    4. The existing **pytest-asyncio default-loop-scope deprecation warning** remains unrelated and non-blocking.
  - **Scientific and governance limitations (preserved explicitly, undiminished):** **no qualified-human attestation was obtained**; the human gate was **waived only by the owner for this internal repository task**; **the waiver changes governance, not evidence**; **40 tickers**; **three evaluation years**; **retrospective cohort membership**; **low statistical power**; **one unusual macro regime**; **cross-basis multiplicity**; **post-review sensitivity status**; **environment-qualified byte reproducibility**; **limited compact-package coverage**; **non-rejection does not prove that true IC is zero**; and **nothing establishes alpha, profitability, investment value, a tradable strategy, deployment validity, or production validity**.
  - **Commit-readiness disposition:** all four requirements of the owner-amended completion standard are now satisfied — (1) **independent repository technical rereview APPROVED**; (2) **multiple independent advisory statistical assessments supported the corrected methodology**; (3) **owner methodological acceptance recorded**; (4) **final repository commit-readiness audit APPROVED**. Therefore **R3-TGT-01 is COMMIT_READY under the owner-amended internal governance standard**.
  - **What COMMIT_READY means here (binding):** the **complete current R3-TGT-01 change set may be staged and committed manually by the owner**. It does **not** mean qualified-human reviewed. It does **not** mean merge-complete or closed. **The commit has not yet occurred.** **Final post-commit verification remains required after the owner creates the commit.** The task must still **not** be marked HUMAN_REVIEW_APPROVED, QUALIFIED_HUMAN_VALIDATED, EXTERNAL_VALIDATION_COMPLETE, MERGE_COMPLETE, or CLOSED, and nothing here establishes a predictive edge, alpha, investment value, or production validity.
- **Post-commit verification record (2026-07-23; append-only; supersedes nothing above; all historical text above is preserved unchanged):** the owner-created implementation commit for R3-TGT-01 was recorded and its post-commit verification passed.
  - **Implementation commit created successfully:**
    - **commit:** `ac4ba8a80d6143fe3fccebc5637fc4d3dab79e43`
    - **parent:** `26051a35992ea65b789c35f715d7c3aa5cae434c`
    - **subject:** `Add R3-TGT-01 excess-return robustness analysis`
    - **committed inventory:** exactly the **14 approved R3-TGT-01 task files**
    - **diff summary:** **11,845 insertions and 1 deletion**
    - **no unrelated file entered the commit.**
  - **Post-commit verification results (all passed):**
    - `git status` was **clean** immediately after the implementation commit;
    - `git diff HEAD^ HEAD --check` **passed**;
    - **docs-lint passed**;
    - **claims-lint passed** against **Model Confidence Contract v1.8.0**;
    - **data validation passed:** **403 modeling rows**, **40 governed features**, **321 target rows**, **82 inference-only rows**, **benchmark available**;
    - **focused excess-basis suite passed: 165 tests**;
    - the existing **pytest-asyncio default-loop-scope deprecation warning** was **non-blocking and unrelated**.
  - **Governance status (binding, unchanged in substance):**
    - the implementation commit is **locally complete and verified**;
    - **no qualified-human attestation exists**;
    - the **owner human-gate waiver remains active**;
    - **COMMIT_READY was applied only under the owner-amended internal standard**;
    - **nothing establishes predictive edge, alpha, profitability, investment value, tradable-strategy validity, deployment validity, or production validity**;
    - the branch has **not yet been pushed or merged**;
    - the task is **not MERGE_COMPLETE or CLOSED**;
    - **owner push or integration remains pending.**
- **Merge and closure record (2026-07-23; append-only; supersedes nothing above; all historical text above is preserved unchanged):** the R3-TGT-01 implementation branch was published and merged into `main` via pull request, and post-merge verification passed. This record changes **no scientific evidence, no result, no artifact, and no claim.**
  - **Pull request:**
    - **PR number:** 1
    - **title:** `Add R3-TGT-01 excess-return robustness analysis`
    - **base branch:** `main`
    - **head branch:** `local/r3-tgt-01-excess-implementation`
    - **state:** MERGED
    - **merge method:** merge commit
    - **merge commit:** `1961decf3955011d7824c7142ccbb4d21c9357f2`
    - **merged timestamp:** `2026-07-22T22:56:06Z`
  - **Pre-merge checks (all successful):** the PR was **MERGEABLE**; the merge state was **CLEAN**; the **Vercel** check passed; the **Vercel Preview Comments** check passed; **0 failing and 0 pending checks**.
  - **Ancestry verification:** implementation commit `ac4ba8a8` (`Add R3-TGT-01 excess-return robustness analysis`) is an **ancestor of `origin/main`**; post-commit governance commit `91408e6c` (`Record R3-TGT-01 post-commit verification`) is an **ancestor of `origin/main`**; **both audited commit hashes were preserved by the merge-commit strategy** (no rebase or squash rewriting).
  - **Merged scope (exact):** **two branch commits**; **fourteen task files**; **no unrelated file**; **no rebase or squash rewriting of the audited commits.**
  - **Post-merge verification (local `main` fast-forwarded to `origin/main`; all passed):**
    - `git diff --check` passed;
    - **docs-lint passed**;
    - **claims-lint passed** against **Model Confidence Contract v1.8.0**;
    - **data validation passed:** **403 modeling rows**, **40 governed features**, **321 target rows**, **82 inference-only rows**, **benchmark available**, **valid T-to-T+1 modeling dataset**;
    - **focused excess-basis suite: 165 passed in 18.55 seconds**;
    - **full root suite: 521 passed in 120.71 seconds**;
    - the existing **pytest-asyncio default-loop-scope deprecation warning** was **unrelated and non-blocking**.
  - **Final scientific and governance boundary (preserved explicitly, undiminished):**
    - **no qualified-human statistical/domain attestation was obtained**;
    - **no human-review approval exists**;
    - the **owner human-gate waiver remains an internal governance decision**;
    - **no AI, repository reviewer, advisory assessment, or owner statement is represented as qualified-human approval**;
    - the **waiver changed the internal completion rule, not the scientific evidence**;
    - the study **remains limited by 40 tickers, three evaluation years, retrospective cohort membership, low statistical power, one unusual macro regime, cross-basis multiplicity, and the post-review status of the trajectory-preserving sensitivity**;
    - **non-rejection does not prove that the true IC is zero**;
    - **nothing establishes reliable predictive edge, alpha, profitability, investment value, a tradable strategy, deployment validity, or production validity.**
  - **Closure disposition:** the implementation was **committed**; **post-commit verification passed**; the **branch was published**; **PR #1 was merged into `main`**; **both audited commits are present on `origin/main`**; **post-merge verification passed**; **no blocking implementation, repository, governance, or integration defect remains for the bounded R3-TGT-01 task**; **R3-TGT-01 is MERGE_COMPLETE and CLOSED under the owner-amended internal governance standard.**
  - **What CLOSED means here (binding):** it means **only** that the **authorized R3-TGT-01 repository task is complete** and that **no further implementation is required within this task**. **Dataset expansion, future prospective years, or genuine qualified-human review must be separately authorized and recorded.** **Closure does not strengthen the statistical evidence or the external-validation status**, and this task must **never** be described as qualified-human reviewed, human-statistician approved, externally validated, or independently human attested.

### R3-NULL-01 — Negative-control / placebo laboratory
- **Priority:** P1. **Wave:** 3B (harness-running — fresh context). **Owner role:** Research/Statistics. **Risk:** medium-high. **Model/effort:** **Opus, high.** Independent review: **Fable, medium (design mandatory before merge).**
- **Verified current state:** the significance machinery has never been demonstrated against known-null inputs; a hostile reviewer's first question ("does your rig reject noise?") has no committed answer.
- **Read first:** `experiments/run_experiments.py` (panel construction + seeding), `experiments/significance.py`, METHODOLOGY §Headline IC significance treatment.
- **Files to modify:** new `experiments/placebo_lab.py` (proposed) → `experiments/results_placebo/placebo_report.{json,md}` (proposed); Makefile additive `research-placebo`; root tests.
- **Design (binding):** R≥20 seeded placebo repetitions: replace all feature columns with `N(0,1)` noise (seeded per repetition; targets, splits, models, significance settings identical to the real run), run harness + the same family-wise gate **in an isolated temp panel — the canonical datasets are never modified**; report the empirical distribution of pooled ICs, raw-p minima, and the family-wise rejection count across repetitions (expected ≈ α·R). Also record per-repetition wall-clock; if the full 20×harness cost is prohibitive, reduce models to the six ML family and say so. Claim-safety verbatim: "**Placebo runs test the evaluation machinery, not the market; the expected outcome is failure to reject known-null inputs in approximately (1 − α) of repetitions, and any placebo ‘significance’ is a false positive at rate α or a numerical artifact — never a signal.**"
- **Acceptance criteria:** seeded rerun → byte-identical report; canonical checksums untouched (shasum gates); the report states the observed placebo rejection rate against its binomial expectation; root suite green + fixture test on the noise-panel builder (columns preserved, values replaced, targets untouched).
- **Verification:** root pytest; `make research-placebo` ×2 + shasum; canonical shasum gates; Fable design review recorded before merge.
- **Generated artifacts:** `experiments/results_placebo/*` — owner `make research-placebo`.
- **Failure modes / misleading success:** a placebo generator that accidentally preserves signal-bearing structure (e.g. noise seeded per-ticker constant across years → spurious cross-year structure) making "rejection" meaningless — the fixture test must pin independence; hiding a placebo repetition that rejected (forbidden — the rate IS the result); letting placebo artifacts get quoted as evidence about markets.
- **Rollback:** delete new files + hunk. **Stop:** if isolation from canonical data cannot be proven (temp-dir panel), stop and report.
- **Commit scope:** module + outputs + tests + Makefile. **Theme:** "Add placebo negative-control laboratory". Manual commit: yes. **/clear:** yes.
- **Independent Fable 5 mandatory-fix amendment (2026-07-14; append-only):** fixes 1–4 completed without redesigning the approved experiment: claim safety now correctly expects non-rejection in approximately `(1 − α)` of repetitions; an end-to-end planted-signal test drives the real prediction-dump writer and six-model significance gate (`elasticnet`, raw p `0.000999001`, Bonferroni p `0.005994006`, rejected); nondeterministic timing moved to the explicitly ignored local runtime path and is absent from governed evidence; and the report identifies `R=25` as a low-resolution smoke test with exact two-sided 95% Clopper-Pearson upper bound `0.1371851715` for observed `0/25`. Focused tests: 44/44 passed; root tests: 270/270 passed; two production reruns were byte-identical (`placebo_report.json` `7fc1ebddff9d85b7a879b7fa0b5f3b559f257798195a75470540dfd4324fc0bd`, `placebo_report.md` `5b3376f04d7a622b5baed3819966501c37420b7609ac2f19734450338ec6c58d`); claims lint passed; docs lint still reports eight pre-existing missing-path violations outside R3-NULL-01; protected canonical artifacts and trusted data remained unchanged.

### R3-SERV-01 — Serving-heuristic walk-forward significance parity
- **Priority:** P1. **Wave:** 3B (harness-running — fresh context; the queue's highest-judgment task). **Owner role:** Research/Statistics. **Risk:** **high — new quotable numbers about the surface users actually see.** **Model/effort:** **Fable, high** (or Opus high with **mandatory Fable review** of design + wording before merge).
- **Verified current state:** the dumps/leaderboard contain 3 baselines + 6 ML models (verified); the user-facing heuristic (`backend/app/services/forecasting_csv_service.py` `train_parameters()`/`run_forecast()`) is not among them; METHODOLOGY §Serving-side heuristic documents the distinction in prose with no numbers. The calibration bench (R2-CAL-01) already established the precedent of replaying backend services offline from `experiments/`.
- **Read first:** `forecasting_csv_service.py` (fully — especially which universe/years `train_parameters` consumes and what panel `run_forecast` expects), `experiments/calibration_bench.py` (service-replay precedent incl. import mechanics), `experiments/run_experiments.py` (split definitions), `experiments/significance.py`.
- **Files to modify:** new `experiments/serving_eval.py` (proposed) → `experiments/results_serving_eval/serving_eval_report.{json,md}` + `predictions_serving_*.csv` (proposed); Makefile additive `research-serving-eval`; root tests; METHODOLOGY paragraph (§Serving-side heuristic gains a "now measured" subsection).
- **Do NOT touch:** `forecasting_csv_service.py` (read-only import — zero edits), canonical dumps/leaderboard/significance artifacts, backend tests.
- **Design (binding):** replicate the walk-forward protocol exactly: for each test year T+1 ∈ {2023,2024,2025}, call `train_parameters` restricted to training years ≤ T, apply `run_forecast`-equivalent scoring to year-T feature rows of the same 80-ticker evaluation panel the other models saw, rank within year, compute Spearman vs realized. Then the existing significance treatment: within-year permutation (10k, seeded) + bootstrap CI. **Family framing is fixed in advance:** the heuristic is ONE prespecified model — report its raw permutation p unadjusted, clearly labeled "single prespecified test, outside the six-model Bonferroni family", AND alongside the family context so no one reads it as having survived a correction it was never subject to. Wording for every outcome is pre-committed in the packet: whatever the IC, the report's conclusion sentence is "**The user-facing serving heuristic's walk-forward IC is [value] (95% CI [a,b], permutation p=[p]); this [is/is not] distinguishable from the within-year null, and in either case does not establish investment value, implementability, or a reliable predictive edge.**" If the service cannot score the training-universe panel without code changes (e.g. hardwired public-universe filtering), **STOP** and report the minimal seam instead of forking the logic — a reimplemented "approximation" of the heuristic is explicitly forbidden (it would measure the wrong thing).
- **Acceptance criteria:** service file byte-identical (`git diff` empty for `backend/`); a pin test proves the harness-invoked scoring equals the service's own output on a fixture panel; seeded rerun → byte-identical; canonical checksums untouched; root + backend suites green; METHODOLOGY paragraph reviewed against the do-not-claim register (human review recorded — claims-lint does not scan METHODOLOGY).
- **Verification:** root + backend pytest; `make research-serving-eval` ×2 + shasum; canonical shasum gates; `git diff --stat backend/` empty; Fable review recorded.
- **Generated artifacts:** `experiments/results_serving_eval/*` — owner `make research-serving-eval`.
- **Failure modes / misleading success:** an approximated reimplementation drifting from the real service (the pin test is the guard); the single-model raw p being quoted as family-corrected evidence in either direction; a negative IC being spun as "the heuristic is contrarian gold" (pre-committed sentence kills it); accidentally training on year-T+1 rows (walk-forward discipline test required).
- **Rollback:** delete new files + hunk; METHODOLOGY revert. **Stop conditions:** service seam issue above; any need to modify backend code.
- **Commit scope:** experiments module + outputs + tests + Makefile + METHODOLOGY. **Theme:** "Measure the serving heuristic under walk-forward significance". Manual commit: yes. **/clear:** yes.
- **Implementation evidence (2026-07-15; append-only; independent review PENDING):** the harness invokes the unchanged `train_parameters()` and `run_forecast()` functions through the documented `RESEARCH_REPO_ROOT` seam on the exact canonical eligible panels (n=80 in 2023/2024/2025), with prior-year training windows and held-out outcomes blanked before scoring. Pooled IC is 0.050 (95% CI [-0.075,0.174], raw permutation p=0.4427), labeled as one prespecified test outside the six-model Bonferroni family; the pre-committed no-investment-value/no-reliable-edge conclusion is unchanged. Focused + registry tests passed 32/32, root passed 286/286, backend passed 85/85, claims lint passed, two generator runs were byte-identical, and all 291 protected service/canonical/trusted files were unchanged. Docs lint reports the same eight pre-existing missing-path violations outside R3-SERV-01. Review handoff: `docs/R3_SERV_01_FABLE5_REVIEW_HANDOFF.md`; status remains **PENDING**, task is **not merge-ready**, and no commit was made.
- **Independent review evidence (2026-07-18; append-only; APPROVED):** a separate-context independent Fable 5 review (REV-01 session) of implementation commit bd9aa71a39e33e62d43197e034e8db86b82df0a5 at repository HEAD 18514ac5 returned APPROVED with no required fixes. Verified: service-path parity (pin test + inspect.getsourcefile guard; forecasting_csv_service.py sha 7438ab40… unchanged), walk-forward boundaries and cohort parity (81/161/241 training, 80 eligible per year, RGYAS exclusions), canonical statistical parity (10k/10k, seed 42, significance.analyze_model), outside-family raw-p framing with no serving Bonferroni field, exact pre-committed conclusion in JSON/MD/METHODOLOGY, six-model context byte-equal to significance_report.json, all five generated and all protected checksums byte-identical at HEAD, claim-safety term scan clean, no backend/frontend consumer. Fresh verification: root 356/356, backend 99/99, claims lint green (MCC v1.8.0); docs lint's 9 violations are all pre-existing/ledger drift outside this task. Generator two-run byte-identity was not re-executed (read-only review); committed artifacts match all recorded checksums. Task is merge-complete: the implementation is committed at bd9aa71a and the review-PENDING statements in the 2026-07-15 evidence bullet and the generated reports are superseded by this entry (implementation date 2026-07-15; review date 2026-07-18).

### R3-PREREG-01 — Pre-registered 2026 forward-outcome evaluation protocol
- **Priority:** P1. **Wave:** 3B. **Time gate: must land before any 2026 outcome data exists.** **Owner role:** Research governance. **Risk:** medium. **Model/effort:** **Opus, medium-high.** Independent review: **Fable, medium (interpretation grid mandatory).**
- **Verified current state:** the 2026 forward ranking is computed at request time (`GET /forecasting/inference?year=2025`, `unevaluated_forward_forecast`) and is not frozen anywhere; when 2026 outcomes mature (~early 2027), nothing prevents post-hoc analytic choices. Power context is committed: one 40-ticker year detects only |IC| ≥ ~0.431 (`significance_report.md`).
- **Read first:** `forecasting_csv_service.py` inference path, METHODOLOGY §Forward forecast, §Power and detectability limits, the manual-CSV ingestion pattern (`data/trusted_raw/macro/` sidecars from R2-REAL-01).
- **Files to modify:** new `experiments/results_forward_2026/forward_ranking_2026.csv` (proposed — the frozen ranking, generated once by a new deterministic script `experiments/freeze_forward_ranking.py` (proposed) that replays the inference path offline, stamped with git SHA + input checksums); new `docs/PREREGISTERED_2026_EVALUATION.md` (proposed — the protocol); new `experiments/evaluate_preregistered_2026.py` (proposed — inert without data); Makefile additive targets `freeze-forward-2026` + `evaluate-forward-2026`; root tests; METHODOLOGY cross-reference (≤6 lines).
- **Design (binding):** the protocol pre-specifies EVERYTHING: the frozen artifact + checksum; the future outcome source (sourced year-end 2026 prices via the existing manual/Yahoo pattern → nominal TRY T+1 returns; nulls stay null); exactly ONE test (Spearman IC of frozen ranking vs realized 2026 returns over available public-40 rows, within-year permutation p, seeded); and a **pre-written interpretation grid covering every cell** — including the "significant positive" cell, whose pre-committed wording states that a single ~40-row year (detectable |IC| ≥ ~0.431 at 80% power) cannot establish a reliable predictive edge, is one draw, and changes no product claim without the full documented evidence flow (experiments → audit → claims guide → MCC bump). The protocol leads with its own power limitation: **the pre-registered test is nearly powerless by design, and the artifact's value is the discipline, not the answer.** The evaluation script refuses to run (structured `outcome_data_absent`) unless the sourced outcomes CSV exists and matches its declared schema.
- **Acceptance criteria:** frozen CSV generated twice → identical; a pin test proves the frozen ranking equals the live inference output at freeze time; evaluation script's absent-data path tested; protocol doc reviewed by Fable against the do-not-claim register; canonical artifacts untouched; root + backend suites green.
- **Verification:** root + backend pytest; `make freeze-forward-2026` ×2 + shasum; canonical shasum gates; recorded Fable review.
- **Generated artifacts:** `experiments/results_forward_2026/forward_ranking_2026.csv` — owner `make freeze-forward-2026`; **after freezing, this file becomes a protected checksum: regenerating it later invalidates the pre-registration and is forbidden without an owner decision recorded in the protocol doc.**
- **Failure modes / misleading success:** freezing a ranking that differs from what the API serves (pin test); wording any grid cell so a lucky year could be quoted as validation; "improving" the protocol after outcomes exist (the doc states it is immutable once outcome data exists — later edits require an appended, dated amendment section, never rewrites).
- **Rollback:** before owner commit only — delete new files. **Stop conditions:** any evidence that partial 2026 outcome data already exists in-repo (then the pre-registration window may be compromised — report to owner).
- **Commit scope:** protocol + frozen artifact + scripts + tests + Makefile. **Theme:** "Pre-register the 2026 forward evaluation". Manual commit: yes. **/clear:** yes.
- **Implementation evidence (2026-07-15; append-only; independent review PENDING):** verified before generation that no 2026 realized-outcome file existed (`data/trusted_raw/realized_2026_returns.csv` and the optional `partial_2026_ytd_returns.csv` both absent) — genuine pre-registration window intact. `experiments/freeze_forward_ranking.py` replays the unchanged production path `forecasting_csv_service.inference_forecast(input_year=2025, top_n=12)` (the `GET /forecasting/inference?year=2025` entry point) through `RESEARCH_REPO_ROOT`, asserting function-source identity, and froze 40 public-universe rows to `experiments/results_forward_2026/forward_ranking_2026.csv` (sha256 `a8a8c39cb8956b13c388d6d0be83470678a1b5c2395476d87d849b05b5b5518f`) plus a deterministic `freeze_manifest.json`; both are byte-identical across two runs and across two Python interpreters. `docs/PREREGISTERED_2026_EVALUATION.md` leads with the power boundary, pre-specifies one Spearman-IC + seeded within-year permutation test (α=0.05), and pre-writes every interpretation cell (the positive-significant cell still denies a reliable edge). `experiments/evaluate_preregistered_2026.py` is inert: it returns `outcome_data_absent` with no metric, and refuses on checksum/protocol/schema/duplicate/target-year/provenance/insufficient-row/tamper faults. Additive Makefile targets `freeze-forward-2026`/`evaluate-forward-2026`; both frozen artifacts registered with one owner under new governed root `experiments/results_forward_2026`. Focused tests 28/28, registry 16/16, root 314/314, backend 85/85, claims lint green (MCC v1.7.0, no bump), two-run byte identity, all protected service/canonical/trusted/manifest/MCC checksums unchanged, `git diff --check` clean. Docs lint has the same eight pre-existing violations outside this task. Review handoff: `docs/R3_PREREG_01_FABLE5_REVIEW_HANDOFF.md`; status remains **PENDING**, task is **not merge-ready**, no commit was made.
- **Mandatory review-fix evidence (2026-07-16; append-only; independent re-review PENDING):** RF-1 through RF-6 only were applied after the independent review returned changes required. Freeze-once now validates both existing artifacts and pinned checksums, computes candidates in isolation, returns write-free `already_frozen_identical`, and refuses Git/service/data/universe/ranking/artifact drift before canonical writes. The evaluator floor is 30; absent, insufficient, and estimated states disclose the complete frozen cohort. The outcome contract is pinned to nominal-TRY Yahoo adjusted closes on the final valid December 20–31 quotation on/before each year end, retained start/end snapshot checksums, row-level provenance, and independent return recomputation; unexpected, duplicate, non-finite, malformed, or under-provenanced rows refuse before statistics. A frozen n=30–40 Fisher-z table supplies descriptive power context only. Non-canonical manifest checksums now follow the requested results path without machine paths. Focused 70/70, registry 16/16, root 356/356, backend 85/85, claims lint green; docs lint remains the same eight pre-existing violations, and 315 protected service/trusted/canonical/freeze-manifest/MCC files stayed byte-identical. `make evaluate-forward-2026` remains deterministic `outcome_data_absent` with no metric/report; ranking SHA-256 remains `a8a8c39cb8956b13c388d6d0be83470678a1b5c2395476d87d849b05b5b5518f`. Not merge-ready; no commit or push.
- **Independent re-review evidence (2026-07-18; append-only; APPROVED):** the separate-context independent Fable 5 re-review of `local/r3-prereg-01-execution-e7299d` returned **APPROVED**, with RF-1 through RF-6 **resolved**. The frozen ranking checksum remains `a8a8c39cb8956b13c388d6d0be83470678a1b5c2395476d87d849b05b5b5518f` and the freeze-manifest checksum remains `6a96408c55789646ce8f5b66fa8be243ac6ac8a2292e1783ecb60c88b87f54ea`, both unchanged. No 2026 outcome data existed during implementation or review. This task is **merge-ready after owner commit**; no commit or push was made.

### R3-MISS-01 — Serving-heuristic missingness sensitivity
- **Priority:** P2. **Wave:** 3B. **Owner role:** Research. **Risk:** medium. **Model/effort:** **Opus, medium.** Review: Terra low.
- **Verified current state:** `run_forecast()` omits missing features and reduces confidence (documented + tested at DATA-02/03), but no artifact measures how *ranks* move under controlled feature-group masking; audit §9 names sparse-column silent-ranking as a residual risk.
- **Read first:** `forecasting_csv_service.py` (feature-selection + percentile mechanics), `experiments/calibration_bench.py` (replay precedent), the feature-category mapping used by the service or dictionary.
- **Files to modify:** new `experiments/missingness_sensitivity.py` (proposed) → `experiments/results_missingness/missingness_report.{json,md}` + `rank_deltas.csv` (proposed); Makefile additive `research-missingness`; root tests.
- **Do NOT touch:** the service (read-only import; pin test like R3-SERV-01), canonical artifacts.
- **Design (binding):** offline replay on the latest public-universe input year: baseline ranks; then for each feature category (and each single feature in the selected-weight set), mask it dataset-wide and per-ticker → record rank deltas and confidence deltas. Deterministic (no sampling). Labels mandatory everywhere: "**Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.**"
- **Acceptance criteria:** rerun → byte-identical; pin test (unmasked replay == service output on fixture); root + backend green; canonical checksums untouched.
- **Verification:** root + backend pytest; new target ×2 + shasum; canonical shasum gates.
- **Generated artifacts:** `experiments/results_missingness/*` — owner `make research-missingness`.
- **Failure modes:** masking implemented differently from real missingness handling (pin against the service's own null path); reading "rank robust to missingness" as reliability. **Rollback:** delete files + hunk. **Stop:** service seam requires edits.
- **Commit scope:** module + outputs + tests + Makefile. **Theme:** "Measure serving-ranking sensitivity to missing inputs". Manual commit: yes. **/clear:** yes.
- **Implementation evidence (2026-07-24; append-only; independent review PENDING; ROOT-SUITE-BLOCKED; NOT commit-ready):** the deterministic serving-heuristic missingness sensitivity implementation is **complete**. The implementation lives in worktree `r3-miss-01-missingness-sensitivity-ad3e40` on branch `local/r3-miss-01-missingness-sensitivity-ad3e40` (the packet-named branch) at commit `4fc1136a`, as the uncommitted change set `M Makefile`, `M artifact_registry.json`, `?? experiments/missingness_sensitivity.py`, `?? experiments/results_missingness/`, `?? tests/test_missingness_sensitivity.py`. This governance/amendment session was placed by the harness in the sibling worktree `r3-tgt-01-excess-basis-2e6992` on branch `local/r3-miss-01-amendment-fbc80d` at the same commit `4fc1136a`; the naming discrepancy (governance branch vs packet-named implementation branch) is recorded honestly per the R3-UI-02 precedent, switching was not required because this amendment only appends to `FINANCEIQ_AGENT_TASK_QUEUE.md` and `TASK_STATE.md`, and every figure below was independently re-verified read-only in the implementation worktree during this pass (it was not merely transcribed). The **unchanged serving seam is replayed read-only** against an isolated temporary data root via the documented `RESEARCH_REPO_ROOT` override; **unmasked replay matches the service output exactly** (`True`, byte-compared, fails closed on any difference). Analysis universe: **input year 2025, forecast year 2026, public cohort 40 tickers**; the **selected serving set contains 12 features across four governed source classes** (`derived`: enterprise_value, ev_ebitda, price_drawdown_from_3y_high_pct, price_momentum_2y_pct; `unknown`: gross_margin, gross_profit, net_debt, net_income, revenue; `vendor_xlsx`: ebitda_growth_pct, net_income_growth_pct; `yahoo_fetch`: price_adjclose_t). **Four exhaustive scenario families (A dataset-wide category 4, B per-ticker category 160, C dataset-wide single-feature 12, D per-ticker single-feature 480) contain 656 scenarios and 26,240 row-level observations** (deterministic, no sampling). The **generated namespace `experiments/results_missingness/` contains exactly three files**: `missingness_report.json`, `missingness_report.md`, `rank_deltas.csv`. Verification independently re-run this pass in the implementation worktree: **focused suite 37 passed**; **backend suite 99 passed**; **docs-lint PASSED**, **claims-lint PASSED** (Model Confidence Contract v1.8.0), and **data validation VALID** (403 modeling rows, 40 features, 321 target rows, benchmark available). Two-run byte-identical artifact generation is attested by the implementation session and is consistent with the three artifacts present; it was **not** independently re-executed in this pass because this governance/amendment pass regenerates nothing. Service, backend, frontend, datasets, models, weights, feature definitions, canonical artifacts, and all other result namespaces were untouched by the implementation (working set limited to the module, its outputs, its test, and the additive Makefile/registry entries).
- **Mandatory interpretation boundary (recorded verbatim):** *Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.*
- **Root-suite blocker (2026-07-24; independently observed this pass):** the full root suite returned **554 passed and 4 failed** (`PYTHONPATH=. python -m pytest tests/`). The four failures are cross-task provenance/test compatibility defects belonging to the already-closed R3-TGT-01, not evidence that the R3-MISS-01 serving replay or generated results are incorrect. Independently observed failure classes: **(1) Frozen protected-boundary count vs. dynamic namespace discovery** — `test_protected_boundary_and_determinism_survive_the_corrections` (in `tests/test_excess_basis.py`) asserts `protected_count == 351` while its helper dynamically discovers every `experiments/results_*` namespace, and the three new authorized missingness artifacts raise that discovery count (observed `assert 354 == 351`); the companion `test_protected_artifacts_are_byte_identical_across_regeneration` and `test_generated_artifacts_are_isolated_complete_and_claim_safe` fail from the same boundary/discovery drift. **(2) Stale whole-Makefile-SHA provenance** — `test_embedded_source_artifact_checksums_are_current` (in `tests/test_artifact_registry.py`) (and the `test_excess_basis.py` embedded-source checksum assertion) fail because R3-TGT-01 excess provenance embeds the SHA-256 of the entire `Makefile`; adding the mandatory additive `research-missingness` target marks the historical excess provenance stale even though the research-excess recipe, statistical implementation, predictions, leaderboard, and results did not change.
- **Owner authorization — narrow R3-TGT-01 compatibility repair (2026-07-24; append-only):** a narrow compatibility repair is authorized. **Permitted repair paths only:** `experiments/run_excess_basis.py` (only if the production provenance or protected-boundary implementation shares the defect); `tests/test_excess_basis.py`; `experiments/results_excess/artifact_manifest.json`; `experiments/results_excess/significance_report.json`; `experiments/results_excess/significance_report.md`. **No other prior-task file is authorized.** Any necessary R3-TGT-01 report/provenance files must be **regenerated through the governed runner, never hand-edited**. The repair must establish these **durable invariants**: (1) adding an unrelated governed result namespace must not invalidate the frozen R3-TGT-01 task boundary; (2) adding an unrelated Makefile target must not stale R3-TGT-01 provenance; (3) changing the research-excess target recipe or its actual statistical source inputs must still be detected; (4) mutating any file belonging to the original R3-TGT-01 protected boundary must still be detected; (5) the repair must not special-case the name `results_missingness` or merely change a hardcoded count from 351 to 354; (6) the repair must not weaken artifact-registry ownership, stale-artifact detection, output confinement, or protected-file mutation detection; (7) R3-TGT-01 prediction dumps, leaderboard, pooled ICs, p-values, intervals, conclusions, and claim boundaries must remain unchanged; (8) all three R3-MISS-01 generated artifacts must remain byte-identical; (9) any necessary R3-TGT-01 report/provenance files must be regenerated through the governed runner, never hand-edited. **Explicitly prohibited:** service changes; dataset changes; model, feature, weight, split, seed, company, or year changes; changing R3-MISS-01 scenario results; weakening tests merely to ignore the failures; hardcoding the new repository file count; adding a one-off exclusion for `results_missingness`; modifying prediction dumps or leaderboards; altering statistical conclusions; staging, committing, or pushing. **Independent review must wait until the full root suite passes.** This is an owner-authorized cross-task compatibility amendment; it does not mark R3-MISS-01 APPROVED, COMMIT_READY, MERGE_COMPLETE, or CLOSED.
- **R3-MISS-01 cross-task compatibility repair (2026-07-24; append-only): COMPLETE / INDEPENDENT REVIEW REQUIRED.** All figures below were independently re-verified in this pass (this worktree, `r3-miss-01-missingness-sensitivity-ad3e40`, branch `local/r3-miss-01-missingness-sensitivity-ad3e40`, at `4fc1136a`), not transcribed from a prior claim. **Original reproduced failure state:** full root suite before repair returned **554 passed, 4 failed**; two failures were caused by R3-TGT-01's dynamically discovered protected boundary expanding from 351 to 354 when the new governed missingness namespace appeared; two failures were caused by the R3-TGT-01 provenance embedding the SHA-256 of the entire `Makefile`, which changed when the unrelated `research-missingness` target was added. These are classified as **cross-task provenance/test compatibility defects**, not failures of the R3-MISS-01 serving replay or scenario results.
  - **Repair design:** (1) `experiments/run_excess_basis.py` now uses an explicit historical frozen-boundary authority (`FROZEN_BOUNDARY_DATA_DIRS`, `FROZEN_BOUNDARY_EXPERIMENT_DIRS`, `FROZEN_BOUNDARY_EXTRA_FILES`) rather than a live filesystem-wide `results_*` discovery count — confirmed present at `run_excess_basis.py:125-159, 698-738`. (2) The frozen boundary is represented by its exact member set and a pinned content digest (`FROZEN_PROTECTED_BOUNDARY_SHA256`, `_frozen_boundary_digest`). (3) Future unrelated result namespaces do not enter the historical boundary. (4) Modification or removal of an original boundary member still fails closed (`ExcessFrozenBoundaryError`). (5) Independently grepped: **no literal `results_missingness` exclusion and no 351-to-354 replacement** exists anywhere in `experiments/run_excess_basis.py` or `tests/test_excess_basis.py`. (6) R3-TGT-01 Makefile provenance now records the normalized `research-excess` target recipe and prerequisites (`_parse_makefile_target`, `ExcessMakefileProvenanceError`) rather than the SHA-256 of the complete Makefile. (7) Adding an unrelated Makefile target does not stale excess artifacts. (8) Editing or removing `research-excess`, changing its prerequisites, changing `run_excess_basis.py`, or changing a true statistical input remains detectable (unchanged failure-closed test coverage in `tests/test_excess_basis.py`).
  - **Exact authorized repair files touched (git diff, uncommitted):** `experiments/run_excess_basis.py`; `tests/test_excess_basis.py`; `experiments/results_excess/artifact_manifest.json`; `experiments/results_excess/significance_report.json`; `experiments/results_excess/significance_report.md`. (The working tree also carries the unrelated, already-authorized `Makefile` and `artifact_registry.json` edits and the untracked `experiments/missingness_sensitivity.py`, `experiments/results_missingness/`, `tests/test_missingness_sensitivity.py` from the underlying R3-MISS-01 implementation — none of those are part of this repair pass.)
  - **Preservation evidence (independently hashed this pass):** the excess leaderboard and three prediction dumps remained byte-identical (unchanged by this repair, not touched by the permitted-file list); all pooled ICs, permutation p-values, Bonferroni values, bootstrap intervals, rank-invariance results, and 0-of-6 conclusions remained unchanged. All three R3-MISS-01 artifacts remained byte-identical, independently re-hashed this pass: `missingness_report.json` sha256 `4973813689aad13a0be279acfad0d4315c5d2788379d946e0962644992159958`; `missingness_report.md` sha256 `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` sha256 `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`.
  - **Generated excess report hash changes (independently confirmed this pass, HEAD vs. working tree):** `artifact_manifest.json` `c4dabc0347a9515857799d85fb340d1d09140dc8767a0d2ee0c528a13131ee24` → `9305a6db40bb12ebb5865b02e713ddcc0cd37e956b6834cbfcb2e0caf3299ec1`; `significance_report.json` `c9d7e4a3c90587c8de4b097988ee964600c60ab10561f96d7ee49182818470fb` → `532e4bff8b0ed0c12fd8697108149202a184ff8d510df8a8fa83172da6887062`; `significance_report.md` `6fabb3587f1420c6880e7bef8f5fb90235b29d19dcc1e5eee14c30e9841c3145` → `1806e846d38b11d427c4b98c7ab9f67e6aeaf59b8b571021a70570c9c47963cc`. Only provenance and frozen-boundary metadata changed in those reports; statistical evidence and conclusions did not change.
  - **Mandatory interpretation boundary (recorded verbatim):** *Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.* The repair does **not** establish predictive robustness, reliability, stability, edge, alpha, profitability, investment value, tradable-strategy validity, deployment validity, or production validity.
  - **Verification (all independently re-run this pass, not transcribed):** focused excess suite `PYTHONPATH=. python -m pytest tests/test_excess_basis.py -q` → **177 passed**; focused missingness suite `PYTHONPATH=. python -m pytest tests/test_missingness_sensitivity.py -q` → **37 passed**; artifact-registry suite `PYTHONPATH=. python -m pytest tests/test_artifact_registry.py -q` → **16 passed**; full root suite `PYTHONPATH=. python -m pytest tests/ -q` → **570 passed, 0 failed** (188.46s); backend suite `cd backend && python -m pytest tests/ -q` → **99 passed**; `make docs-lint` → **PASSED**; `make claims-lint` → **PASSED** against Model Confidence Contract v1.8.0; `make data-validate` → **VALID** (403 modeling rows, 40 features, 321 target rows, 82 inference-only rows, benchmark available). Two-run byte-identical isolated/working-tree excess generation and the two-run missingness-hash preservation are attested by the implementation/repair session; deterministic generation code paths were inspected but full two-run regeneration was **not** independently re-executed in this recording pass (this pass appends records only and regenerates nothing). `git diff --check` passed. Nothing was staged, committed, or pushed.
  - **Remaining limitations:** the historical frozen-boundary member authority and pinned digest require explicit reviewed amendment if an original boundary member is intentionally changed; the research-excess recipe hash similarly requires reviewed regeneration after an intentional recipe change; this fail-closed behavior is deliberate. Missingness analysis covers one serving input year and a retrospective public cohort. `source_class` is a provenance grouping, not a financial taxonomy. Byte reproducibility is environment-qualified.
- **R3-MISS-01 independent technical review (2026-07-24; append-only): VERDICT — `CHANGES_REQUIRED`.** An independent reviewer examined the full uncommitted R3-MISS-01 change set in worktree `r3-miss-01-missingness-sensitivity-ad3e40`, branch `local/r3-miss-01-missingness-sensitivity-ad3e40`, at `4fc1136a`. Branch and HEAD confirmed matching; the complete uncommitted change set present; nothing staged. The review confirmed many positive findings but records **three material blocking defects** that make the task **not commit-ready**.
  - **Independently confirmed positive findings:** the serving-seam replay is correct and uses the real, unchanged service; the unmasked replay matches the complete service response exactly; input year 2025, forecast year 2026, cohort 40; all 12 selected serving features are covered; all four scenario families are exhaustive; the 656 scenarios and 26,240 row-level observations are coherent; the JSON and Markdown aggregates reconstruct exactly from the CSV; the mandatory scientific label appears in the JSON, the Markdown, and every CSV row; missingness generation is deterministic; the original four root failures were independently reconstructed; the R3-TGT-01 historical boundary reconstructs to the genuine original 351-member set and pinned digest; R3-TGT-01 prediction dumps, leaderboard, statistics, multiplicity results, intervals, rank audit, and 0-of-6 conclusions remain unchanged; the full repaired suite currently passes 570 tests; backend, registry, lints, and data validation pass.
  - **None of these positive findings makes the task commit-ready** because three material blocking defects remain (below).
  - **BLOCKING DEFECT 1 — Missingness output confinement.** `experiments/missingness_sensitivity.py` accepts an arbitrary caller-supplied `--results-dir`; its current checks confine filenames only relative to that caller-selected directory. The reviewer successfully wrote all three artifacts under a `backend/` subdirectory outside `experiments/results_missingness`. Canonical output must be restricted to the governed namespace; isolated execution may use only an explicitly authorized and bounded temporary root; traversal, unrelated absolute paths, and symlink escapes must fail closed.
  - **BLOCKING DEFECT 2 — Makefile provenance authority.** The custom research-excess Makefile parser is not semantically equivalent to GNU Make; it mishandles valid constructs including duplicate rules, continuations, comments, blank lines, and some prerequisite forms. In the blocking adversarial case, GNU Make executed the later duplicate research-excess recipe while provenance hashed the earlier recipe, so changing the effective executed recipe went undetected. The repair must use GNU Make itself as the effective rule authority, or another mechanism demonstrably equivalent for the repository contract; another partial Makefile parser is not acceptable.
  - **BLOCKING DEFECT 3 — Frozen-boundary symlink safety.** Replacing a genuine protected member with a symlink to an identical external file preserved the path/content digest and was incorrectly accepted. All protected members and relevant path components must be genuine, repository-contained, non-symlink regular files; symlink members, symlinked ancestors, path escapes, missing members, and modified members must fail closed.
  - **NON-BLOCKING hardening defect — Fractional-year validation.** `load_public_frame()` casts numeric years before proving mathematical integrality; values such as `2025.5` must be rejected rather than truncated. This issue is non-blocking in the committed dataset but is **authorized for correction in the same repair pass**.
  - **Owner authorization — mandatory narrow repair pass (2026-07-24; append-only).** New edits are authorized **only** to: `experiments/missingness_sensitivity.py`; `tests/test_missingness_sensitivity.py`; `experiments/results_missingness/missingness_report.json`; `experiments/results_missingness/missingness_report.md`; `experiments/results_missingness/rank_deltas.csv`; `experiments/run_excess_basis.py`; `tests/test_excess_basis.py`; `experiments/results_excess/artifact_manifest.json`; `experiments/results_excess/significance_report.json`; `experiments/results_excess/significance_report.md`. Generated files must be regenerated **only** through their governed owners `make research-missingness` and `make research-excess` — never hand-edited. **New edits are NOT authorized** to: the `Makefile`; `artifact_registry.json`; service or backend code; frontend; datasets; feature passports; models; weights; splits; seeds; companies; years; canonical nominal artifacts; excess prediction dumps; excess leaderboard; MCC; or any other path. If Makefile or registry changes appear necessary, **stop and request a separate owner amendment**.
  - **Required durable invariants (for the repair):** (1) missingness output defaults to exactly `experiments/results_missingness`; (2) a noncanonical output directory is accepted only when it is a descendant of an explicit bounded temporary-root authority intended for isolated tests or validation; (3) the resolved output directory and every relevant ancestor must remain inside that bounded authority; (4) traversal, arbitrary absolute paths, backend/frontend/data paths, symlinked output directories, and symlink escapes fail closed; (5) the three expected filenames remain fixed and no additional output is allowed; (6) Makefile provenance must reflect the effective research-excess rule GNU Make would execute; (7) duplicate rules, continuations, comments, blank lines, prerequisites, target removal, and the repository's actual Makefile syntax must be handled correctly; (8) adding an unrelated target must not change research-excess provenance; (9) changing the effective research-excess recipe or prerequisites must be detected; (10) no new hand-written partial GNU Make parser is acceptable unless its equivalence is independently demonstrated over all supported syntax; (11) every frozen-boundary member must pass lstat-based non-symlink, repository-containment, and regular-file checks before hashing; (12) symlinked ancestor components must also be rejected; (13) the pinned frozen set and digest must remain the genuine historical R3-TGT-01 authority; (14) fractional, non-finite, malformed, or non-integral years fail before integer conversion; (15) missingness scenario rows and scientific aggregates must remain numerically identical unless a genuine previously hidden implementation error is found; (16) R3-TGT-01 prediction dumps, leaderboard, statistics, conclusions, and claim boundaries remain unchanged; (17) any generated-artifact hash change caused only by truthful source/provenance updates must be explicitly reported and independently compared.
  - **Scientific claim boundary (preserved verbatim):** *Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.* Green tests and correct sensitivity measurements do **not** establish predictive robustness, predictive reliability, edge, alpha, profitability, investment value, tradable-strategy validity, deployment validity, or production validity.
  - **Status after this review:** R3-MISS-01 is `REPAIR_REQUIRED / REVIEW_PENDING / NOT_COMMIT_READY`. It is **not** APPROVED, **not** COMMIT_READY, **not** MERGE_COMPLETE, and **not** CLOSED. This recording pass appended to `FINANCEIQ_AGENT_TASK_QUEUE.md` and `TASK_STATE.md` only; it changed no implementation, test, Makefile, artifact-registry, generated-artifact, dataset, backend, frontend, or MCC file, regenerated nothing, and performed no staging, commit, or push. All prior text in this section is preserved byte-for-byte.
- **R3-MISS-01 mandatory review repair (2026-07-24; append-only): COMPLETE / FRESH INDEPENDENT RE-REVIEW REQUIRED.** All figures below were independently re-run and re-hashed in this recording pass against the target worktree `r3-miss-01-missingness-sensitivity-ad3e40`, branch `local/r3-miss-01-missingness-sensitivity-ad3e40`, at HEAD `4fc1136a3b0b65a547799a1c475ef4c4598572ce`; branch and HEAD confirmed matching, nothing staged, `git diff --check` clean, the complete R3-MISS-01 implementation, the first R3-TGT-01 compatibility repair, the `CHANGES_REQUIRED` review record, and the mandatory repair implementation all present, and no unrelated changed path. All prior text in this section is preserved byte-for-byte. **All three blocking defects from the `CHANGES_REQUIRED` review are repaired, and the authorized non-blocking hardening was applied.**
  - **BLOCKER 1 REPAIRED — Missingness output confinement.** Canonical output is restricted to exactly `experiments/results_missingness`; a noncanonical output directory is accepted only together with a separate explicit bounded temporary-root authority (`--temp-root`), beneath which the destination must strictly resolve. Arbitrary repository destinations, unrelated absolute paths, `..` traversal, symlinked destinations, symlinked ancestors, and post-resolution symlink escapes all fail closed (confirmed present at `experiments/missingness_sensitivity.py:1245-1470`, incl. `_reject_symlinked_components_below`, lstat/`O_NOFOLLOW` write guards, and the reviewer's former `--results-dir backend/…` escape now refused). Output writes remain limited to exactly the three governed filenames; failed writes roll back newly created partial output files. `make research-missingness` is unchanged and writes only to the canonical governed namespace.
  - **BLOCKER 2 REPAIRED — Effective GNU Make provenance.** The former ad hoc Makefile text parser was removed; GNU Make itself is now the authority for the effective `research-excess` rule, queried through controlled read-only `--print-data-base --dry-run` / `--dry-run` subprocesses (confirmed at `experiments/run_excess_basis.py:619-760`). Target existence, effective (merged) prerequisites, the expanded effective recipe, duplicate-rule resolution, line continuations, comments, blank lines, and variable expansion are obtained from GNU Make's evaluated database, not from file text. Adding an unrelated target or variable does not change the authority; changing or removing the effective `research-excess` rule or its effective prerequisites is detected. True statistical sources remain separately hashed; the whole-Makefile SHA-256 was not reintroduced.
  - **BLOCKER 3 REPAIRED — Frozen-boundary symlink and path safety.** Frozen authority paths are normalized and validated; absolute paths and `..` traversal are rejected; every member and ancestor component is inspected with lstat (`follow_symlinks=False`) without following symlinks (confirmed at `experiments/run_excess_basis.py:894-1000`). Symlinked members, symlinked ancestors, non-regular files, duplicate normalized members, missing members, modified members, and path escapes fail closed. The genuine historical boundary remains **351 members** and the pinned digest remains `634d7151e75f0ec7a85e412f748ac81499a4fad5e9eac71ab1a5c920f0137dd9` (`FROZEN_PROTECTED_BOUNDARY_SHA256`).
  - **HARDENING REPAIRED — Fractional-year validation.** Year values are validated for finiteness and mathematical integrality before integer conversion (in `experiments/run_excess_basis.py`, a `math.isfinite` + `float.is_integer` gate around lines 1402-1430). `2025.5`, NaN, infinities, empty strings, malformed strings, and values requiring truncation or rounding are rejected; valid integer years and the permitted canonical integer strings remain accepted.
  - **Files changed by the mandatory repair (uncommitted; new repair edits confined to):** `experiments/missingness_sensitivity.py`; `tests/test_missingness_sensitivity.py`; `experiments/run_excess_basis.py`; `tests/test_excess_basis.py`. The following generated artifacts were regenerated **only** through their governed owners (`make research-missingness`, `make research-excess`), never hand-edited: `experiments/results_missingness/missingness_report.json`, `experiments/results_missingness/missingness_report.md`, `experiments/results_missingness/rank_deltas.csv`, `experiments/results_excess/artifact_manifest.json`, `experiments/results_excess/significance_report.json`, `experiments/results_excess/significance_report.md`.
  - **Scientific preservation (R3-MISS-01).** The R3-MISS-01 scientific evidence is unchanged: input year 2025; forecast year 2026; cohort 40; 12 selected features; four governed source classes; 656 exhaustive scenarios; 26,240 row-level observations; all rank and confidence deltas; all grouped aggregates and percentiles; largest movements; zero cohort membership changes; the mandatory scientific label. Preserved verbatim: *Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.* Missingness artifact changes: `missingness_report.md` remained byte-identical (`80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`); `rank_deltas.csv` remained byte-identical (`cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`); `missingness_report.json` changed only because the edited generator source checksum and size are truthfully embedded (its embedded `row_level_evidence.csv_sha256` still equals the unchanged `cb7c60…` CSV, and its `generation_provenance.timestamp_policy` is `omitted for byte-deterministic regeneration`), and all scientific fields remained identical. **Independently recomputed complete `missingness_report.json` SHA-256 (from the current on-disk artifact this pass): `e351dbf5911da301fb2baafbd23ea7d2d7453791522eefc05db92204410e1676`** — the same digest an isolated `make research-missingness` regeneration produced this pass, not an abbreviated implementation-report value.
  - **Scientific preservation (R3-TGT-01).** R3-TGT-01 evidence is unchanged: leaderboard and three prediction dumps byte-identical (independently re-hashed this pass — `leaderboard.csv` `2c2a7ac8fbfe98da7c1baab079bca7d2462987bae6d7ad9a530d3b0d11fed03c`, `predictions_test_2023.csv` `4159ff988ee4ab0a11fef33988f6a66deea98486ba5b4243aa164efbe6b97d6b`, `predictions_test_2024.csv` `fcbd508361a922fab09acc8b1a5656f1bb88b861fa641cef020c16df2e25e4a1`, `predictions_test_2025.csv` `52b4b3a7eaa2c99bc67bd62106399824b1c0696b22ab81af97e3fa4a6aa392c1`); all 27 leaderboard rows; pooled ICs; primary permutation p-values; trajectory-preserving sensitivity p-values; Bonferroni values; ticker-cluster bootstrap intervals; rank-invariance results; primary 0 of 6; sensitivity 0 of 6; either 0 of 6; 120 nominal-versus-excess rows and zero rank mismatches; all scientific conclusions and claim boundaries. `significance_report.md` remained byte-identical (`1806e846d38b11d427c4b98c7ab9f67e6aeaf59b8b571021a70570c9c47963cc`); `significance_report.json` and `artifact_manifest.json` changed only through truthful GNU Make provenance and edited-generator source checksums (current working-tree hashes `significance_report.json` `b7c5dc22d9afb7a0b549cf19e15cd257f03746c7a85e53b886c7377f0e4a7779`, `artifact_manifest.json` `65e5578ba8c5b26ca13108860d071053c8b5ae33275061fbbd572569cd349793`); the immutable excess CSV outputs remained byte-identical.
  - **Verification (all independently re-run in this recording pass, not transcribed):** focused missingness `PYTHONPATH=. python -m pytest tests/test_missingness_sensitivity.py -q` → **78 passed** (78.79s); focused excess `tests/test_excess_basis.py -q` → **204 passed** (23.68s); artifact-registry `tests/test_artifact_registry.py -q` → **16 passed**; full root suite `PYTHONPATH=. python -m pytest tests/ -q` → **638 passed, 0 failed** (221.91s); backend `cd backend && python -m pytest tests/ -q` → **99 passed**; `make docs-lint` → **PASSED**; `make claims-lint` → **PASSED** against Model Confidence Contract **v1.8.0**; `make data-validate` → **VALID** (403 modeling rows, 40 features, 321 target rows, 82 inference-only rows, benchmark available). **Determinism:** isolated two-run missingness regeneration (two separate out-of-repository temporary roots) produced byte-identical `missingness_report.json` / `.md` / `rank_deltas.csv`, and those bytes equal the committed target artifacts (`.md`/`.csv` matched, and the JSON reproduced to `e351dbf5…`); isolated two-run excess regeneration was byte-identical except for the embedded absolute output-directory path (normalized diff = 0 lines), and two excess runs into the **same** temporary path were byte-identical, so excess generation is deterministic and only path-dependent, and its path-independent outputs (`significance_report.md`, `leaderboard.csv`, all three prediction dumps) reproduced to the committed target bytes. **Target in-place two-run regeneration was intentionally NOT performed** because the governing override for this recording pass forbids regenerating artifacts; target artifact reproducibility was instead confirmed non-destructively by the isolated regenerations above (equal to the committed target bytes for every path-independent artifact). `git diff --check` clean; nothing was staged, committed, or pushed.
  - **Remaining limitations.** GNU Make provenance is qualified to the controlled GNU Make version and locale used by the repository environment. Generated-artifact byte identity remains numerical-environment-qualified. The frozen historical authority (351-member set + pinned digest) requires explicit reviewed amendment for an intentional original-member change. R3-MISS-01 measures deterministic serving-recipe sensitivity only; green tests do not establish predictive validity; R3-TGT-01 still establishes no reliable predictive edge. No qualified-human statistical or domain attestation exists.
  - **Status after this repair-completion recording:** R3-MISS-01 is `REREVIEW_PENDING / NOT_COMMIT_READY`; a fresh independent technical re-review is required. It is **not** APPROVED, **not** COMMIT_READY, **not** MERGE_COMPLETE, and **not** CLOSED. This recording pass appended to `FINANCEIQ_AGENT_TASK_QUEUE.md` and `TASK_STATE.md` only; it changed no implementation, test, Makefile, artifact-registry, generated-artifact, dataset, backend, frontend, or MCC file, regenerated no committed artifact, and performed no staging, commit, or push. All prior text in this section is preserved byte-for-byte.


- **Fresh independent technical re-review evidence (2026-07-24; append-only; CHANGES_REQUIRED — five blockers):** the fresh detached technical re-review independently reconfirmed that the real unchanged serving seam is used, the unmasked replay exactly matches the complete service response, the 2025→2026 public cohort contains 40 tickers, all 12 selected serving features and four masking families are covered, all 656 scenarios and 26,240 row-level observations reconstruct correctly, the generated scientific aggregates and mandatory claim boundary remain coherent, and the R3-TGT-01 prediction dumps, leaderboard, statistical evidence, multiplicity conclusions, intervals, and rank-invariance results remain unchanged. The review nevertheless independently reproduced five blocking repository-integrity defects. Verdict: **CHANGES_REQUIRED**. R3-MISS-01 remains **NOT_COMMIT_READY**.

- **Blocking defect 1 — canonical output symlink escape (2026-07-24; append-only):** canonical missingness execution accepted a symlinked `experiments/results_missingness` directory and wrote the three governed files into the symlink target outside the governed namespace. Canonical and temporary publication must be anchored to validated non-symlink directory descriptors, and neither the destination nor any relevant ancestor may be followed through a symlink.

- **Blocking defect 2 — mixed-generation artifact set (2026-07-24; append-only):** a controlled failure during replacement of the second artifact left the first artifact from the new generation and the remaining artifacts from the old generation. Publication must be transactional: either the complete prior three-file set remains byte-identical or the complete new three-file set becomes visible. Every replaced prior file must be restored on failure.

- **Blocking defect 3 — incomplete GNU Make command semantics (2026-07-24; append-only):** the effective Make provenance produced the same authority for `false` and `-false`, although GNU Make changes from a failing command to an error-ignored successful target. Execution-significant command-control semantics, including `-`, `+`, and other relevant prefixes, must be preserved or the authority must fail closed.

- **Blocking defect 4 — side effects during purported read-only Make inspection (2026-07-24; append-only):** a recursive or force-executed Make recipe produced a filesystem side effect while provenance was being inspected through a purported dry-run path. Provenance inspection must be demonstrably non-executing. Recursive, force-executed, or otherwise unsafe recipes must be represented without execution or rejected before any subprocess capable of executing them is launched.

- **Blocking defect 5 — frozen-boundary validation-to-hash race (2026-07-24; append-only):** a protected member passed initial path and file-type validation, was replaced before hashing with a symlink to an identical external file, and was accepted because hashing reopened and followed the pathname. Each frozen member must be opened without following symlinks, validated through `fstat`, and hashed through the same already-validated file descriptor while directory ancestry remains anchored.

- **Owner-authorized mandatory repair (2026-07-24; append-only):** authorize a narrow repair limited to `experiments/missingness_sensitivity.py`, `tests/test_missingness_sensitivity.py`, `experiments/results_missingness/{missingness_report.json,missingness_report.md,rank_deltas.csv}`, `experiments/run_excess_basis.py`, `tests/test_excess_basis.py`, and `experiments/results_excess/{artifact_manifest.json,significance_report.json,significance_report.md}`. Generated artifacts must be regenerated only through their governed owners. No new edit is authorized to the Makefile, artifact registry, service/backend/frontend code, datasets, feature passports, models, weights, splits, seeds, companies, years, canonical nominal artifacts, excess prediction dumps, excess leaderboard, MCC, governance history outside this append-only record, or unrelated paths.

- **Binding mandatory-repair invariants (2026-07-24; append-only):** missingness publication must reject canonical and temporary symlink escapes and must publish the three artifacts as one recoverable transaction; Make provenance must preserve execution-significant semantics without executing recipes during inspection; frozen-boundary members must be validated and hashed through the same non-following file descriptors; all R3-MISS-01 scientific rows, aggregates, scenario counts, cohort membership, and claim boundaries must remain unchanged; all R3-TGT-01 prediction dumps, leaderboard rows, ICs, p-values, multiplicity values, intervals, rank audit, conclusions, and claim boundaries must remain unchanged.

- **Scientific boundary (2026-07-24; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Current state (2026-07-24; append-only):** `FIVE_BLOCKING_DEFECTS / MANDATORY_REPAIR_AUTHORIZED / REREVIEW_REQUIRED / NOT_COMMIT_READY`.



- **Five-blocker mandatory repair completion (2026-07-24; append-only; FRESH RE-REVIEW REQUIRED):** the owner-authorized repair reports all five independently reproduced repository-integrity blockers addressed. Canonical and bounded-temporary output publication is now descriptor-anchored and rejects symlinked destinations, authorities, and ancestors; the three missingness artifacts publish as one recoverable directory transaction so a failed publication preserves either the complete prior set or the complete new set; Make provenance no longer invokes GNU Make and instead enforces a narrow non-executing static contract that preserves execution-significant `@` and `-` prefixes while rejecting duplicate, recursive, `+`, shell-evaluating, dynamic, and unsupported rules; frozen-boundary members are opened without following symlinks and hashed through the same validated descriptors with pre/post identity checks.

- **Five-blocker repair verification (2026-07-24; append-only):** focused missingness tests passed `95`; focused excess tests passed `215`; artifact-registry tests passed `16`; the full root suite passed `666`; backend tests passed `99`; docs lint, claims lint under MCC v1.8.0, and data validation passed. Isolated and TARGET two-run missingness and excess generations were byte-identical. Nothing was staged, committed, or pushed.

- **Scientific preservation after five-blocker repair (2026-07-24; append-only):** R3-MISS-01 remains a 2025→2026, 40-ticker, 12-feature, four-category deterministic serving-recipe sensitivity analysis with 656 scenarios and 26,240 row-level observations. The Markdown report and rank-delta CSV remained byte-identical. The JSON changed only through truthful generator-source provenance. R3-TGT-01 leaderboard, prediction dumps, ICs, p-values, multiplicity values, intervals, rank audit, conclusions, and claim boundaries remained unchanged.

- **Current artifact hashes after five-blocker repair (2026-07-24; append-only):** `missingness_report.json` SHA-256 `2e6f543f0573bf65e889f63468b5fcf512cf510a938193c66650b67a6f7d50ce`; `missingness_report.md` SHA-256 `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` SHA-256 `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` SHA-256 `62237cbc08400891c4813e1b4b32377a880227f89fc9ddc9ec3d39834598ef98`; excess `significance_report.json` SHA-256 `1a64c7252852a45b0817d9a3297b4b046ea4203979aabf5577d9e3a250008e44`; excess `significance_report.md` SHA-256 `1806e846d38b11d427c4b98c7ab9f67e6aeaf59b8b571021a70570c9c47963cc`.

- **Scientific boundary (2026-07-24; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Five-blocker repair state (2026-07-24; append-only):** `MANDATORY_REPAIR_COMPLETE / ROOT_SUITE_GREEN / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Post-five-blocker fresh independent technical re-review (2026-07-29; append-only; CHANGES_REQUIRED):** the fresh detached review independently reconfirmed the real forecasting-service seam, complete 2025→2026 scientific reconstruction, 40-ticker cohort, 12 selected features, four source groups, 656 scenarios, 26,240 row-level observations, deterministic artifact regeneration, immutable R3-TGT-01 prediction and leaderboard outputs, primary `0/6`, sensitivity `0/6`, either `0/6`, and the mandatory claim boundary. Focused missingness passed `95`, focused excess passed `215`, registry passed `16`, the full root suite passed `666`, backend passed `99`, and docs lint, claims lint, and data validation passed. Verdict nevertheless remains **CHANGES_REQUIRED** because five material repository-integrity defects remain. R3-MISS-01 is **NOT_COMMIT_READY**.

- **Blocking defect 1 — authority-to-publication temporary-ancestor race (2026-07-29; append-only):** after a valid bounded temporary destination was authorized while an intermediate child did not yet exist, replacing that child with an external symlink before publication allowed all three artifacts to be written outside the authorized temporary root. Authorization and publication must remain anchored to the same validated descriptor chain; a pathname component created or replaced after authorization must not be followed.

- **Blocking defect 2 — cleanup-time transaction corruption (2026-07-29; append-only):** after the complete new artifact directory had been installed and validated, a partial failure while deleting the old backup damaged that backup. The exception path then discarded the complete new generation and restored the already-damaged backup, leaving only two old files. Once the new set has reached the durable commit point, backup-cleanup failure must never roll back to a possibly modified backup; the complete new set must remain visible and cleanup residue may remain for later recovery.

- **Blocking defect 3 — incomplete static Make semantics (2026-07-29; append-only):** the non-executing parser correctly rejects several dynamic constructs but still accepts or misrepresents execution-affecting syntax. `.IGNORE: research-excess` and `.ONESHELL:` do not change recorded provenance, a tabbed command after a blank line is ignored, and inline prerequisite comments are parsed as prerequisite tokens. The accepted subset must reject or truthfully represent all execution-affecting global directives, ambiguous recipe boundaries, and comment syntax. No unsupported Make syntax may be silently accepted.

- **Blocking defect 4 — frozen-boundary ancestor replacement race (2026-07-29; append-only):** final-member descriptor hashing is strong, but the anchored ancestor chain is not revalidated after hashing. Replacing `data/trusted` during hashing allowed the old now-unreachable file descriptor to be accepted while the current repository path identified different content. Every anchored ancestor and the final member must be revalidated from the repository-root descriptor after hashing, with stable device and inode identity.

- **Blocking defect 5 — overstated complete-service replay guard (2026-07-29; append-only):** an independent external replay matched the full service output, but the durable internal baseline comparison normalizes away top-parameter details, warnings, contribution details, and inference-related fields. Mutating omitted fields did not break the internal guard. The persisted provenance claim must accurately describe the enforced comparison, and the regression guard must compare the complete relevant service response rather than a reduced representation.

- **Owner-authorized second mandatory repair (2026-07-29; append-only):** authorize a narrow follow-up repair limited to `experiments/missingness_sensitivity.py`, `tests/test_missingness_sensitivity.py`, `experiments/results_missingness/{missingness_report.json,missingness_report.md,rank_deltas.csv}`, `experiments/run_excess_basis.py`, `tests/test_excess_basis.py`, and `experiments/results_excess/{artifact_manifest.json,significance_report.json,significance_report.md}`. Generated artifacts must be regenerated only through their governed runners. No new edit is authorized to the Makefile, artifact registry, governance files outside this append-only record, backend, frontend, datasets, feature passports, models, weights, splits, seeds, companies, years, canonical nominal artifacts, excess prediction dumps, excess leaderboard, MCC, or unrelated paths.

- **Binding follow-up repair invariants (2026-07-29; append-only):** bounded temporary publication must retain descriptor authority continuously from authorization through staging, backup, publication, and validation; transaction state must have an explicit durable commit point after which cleanup failures preserve the complete new set; Make provenance must fail closed on `.IGNORE`, `.ONESHELL`, relevant special targets, ambiguous recipe boundaries, and unsupported comment syntax; frozen-boundary hashing must post-validate the complete ancestor identity chain from the repository-root descriptor; the baseline replay guard must enforce complete relevant service-output equality and tests must prove that mutations to warnings, top-parameter details, contribution details, and inference fields are detected.

- **Scientific preservation requirement (2026-07-29; append-only):** all R3-MISS-01 rows, ranks, score and confidence deltas, aggregates, scenario counts, cohort membership, and claim boundaries must remain scientifically identical. All R3-TGT-01 prediction dumps, leaderboard rows, ICs, p-values, multiplicity values, confidence intervals, rank audit, conclusions, and claim boundaries must remain unchanged.

- **Scientific boundary (2026-07-29; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Current state (2026-07-29; append-only):** `SECOND_FIVE_DEFECT_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Second mandatory repair completion (2026-07-29; append-only; FRESH INDEPENDENT RE-REVIEW REQUIRED):** the owner-authorized follow-up repair reports all five defects from the post-five-blocker review addressed. Bounded temporary publication now retains descriptor-relative authority from the validated temporary root through component creation and final publication; the three-artifact transaction now has an explicit commit point after which cleanup failures preserve the complete new generation; Make provenance enforces a narrower non-executing fail-closed subset covering execution-affecting special targets, global directives, ambiguous recipe boundaries, and inline comments; frozen-boundary hashing revalidates every retained ancestor from the repository-root descriptor after hashing; and the unmasked baseline replay guard compares the complete public forecasting-service response through deterministic canonical serialization.

- **Second mandatory repair verification (2026-07-29; append-only):** focused missingness tests passed `133`; focused excess tests passed `253`; artifact-registry tests passed `16`; the full root suite passed `742`; backend tests passed `99`; docs lint, claims lint under MCC v1.8.0, and data validation passed. Isolated and TARGET missingness and excess generations were each byte-identical across two runs. Nothing was staged, committed, or pushed.

- **Second mandatory repair scientific preservation (2026-07-29; append-only):** R3-MISS-01 remains a 2025→2026, 40-ticker, 12-feature, four-group serving-recipe sensitivity analysis with A=`4`, B=`160`, C=`12`, D=`480`, 656 total scenarios, 26,240 row-level observations, and zero cohort changes. The rank-delta CSV and Markdown remained byte-identical. The JSON changed only through complete-service replay-audit metadata and truthful generator-source provenance. R3-TGT-01 leaderboard, three prediction dumps, all 27 leaderboard rows, ICs, p-values, Bonferroni values, intervals, primary `0/6`, sensitivity `0/6`, either `0/6`, 120-row rank audit, zero rank mismatches, conclusions, and claim boundaries remained unchanged.

- **Current generated artifact hashes after second mandatory repair (2026-07-29; append-only):** `missingness_report.json` SHA-256 `2228951b2f3121b713a0e5c3c190d56dbeea0727d4442c9bbcefd280a1cc20e3`; `missingness_report.md` SHA-256 `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` SHA-256 `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` SHA-256 `fa863b0996d878d8321eda87a9a722946bce81ac99f19fed89ca3b36d22b0fd9`; excess `significance_report.json` SHA-256 `e42dc045e5e773bf227dba890b1ac7ffab92c135e633fd0cd13eb2cae3012af2`; excess `significance_report.md` SHA-256 `1806e846d38b11d427c4b98c7ab9f67e6aeaf59b8b571021a70570c9c47963cc`.

- **Independent re-review focus (2026-07-29; append-only):** the next detached review must independently challenge post-authorization temporary ancestor replacement, post-commit partial backup-cleanup failure, unsupported Make special/global syntax, repository-root-to-member ancestor replacement during hashing, and complete forecasting-response mutation detection. It must also determine whether the retained `temp_root=None` direct-caller fallback is safely bounded and whether monkeypatched transaction faults provide sufficient evidence or require additional real-filesystem failure checks.

- **Scientific boundary (2026-07-29; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Second mandatory repair state (2026-07-29; append-only):** `SECOND_MANDATORY_REPAIR_COMPLETE / ROOT_SUITE_GREEN / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Second-repair fresh independent re-review (2026-07-29; append-only; CHANGES_REQUIRED — two blockers):** the detached independent review reconfirmed all R3-MISS-01 and R3-TGT-01 scientific evidence, deterministic regeneration, immutable prediction and leaderboard artifacts, focused missingness `133`, focused excess `253`, registry `16`, full root `742`, backend `99`, and passing docs lint, claims lint, and data validation. Three follow-up repairs passed: transaction commit-point safety, frozen-boundary ancestor revalidation, and complete forecasting-service replay comparison. Verdict remains **CHANGES_REQUIRED** because two repository-integrity blockers remain.

- **Blocking defect — authorized real-directory replacement (2026-07-29; append-only):** bounded-temporary publication rejects symlink replacement but accepts replacement of an already-authorized intermediate directory with a different genuine directory. Publication then follows the replacement inode. The retained descriptor authority must verify that every current path component still identifies the originally authorized device/inode before component creation and publication; directory replacement must fail closed.

- **Blocking defect — incomplete Make global semantics (2026-07-29; append-only):** whitespace-prefixed `.IGNORE` and `.SILENT`, `.PHONY` membership affecting `research-excess`, and global `SHELL` semantics can change GNU Make behaviour while leaving provenance unchanged. The static non-executing subset must either represent these semantics truthfully or reject them, including leading-whitespace variants.

- **Owner-authorized narrow repair (2026-07-29; append-only):** authorize changes only to `experiments/missingness_sensitivity.py`, `tests/test_missingness_sensitivity.py`, `experiments/results_missingness/{missingness_report.json,missingness_report.md,rank_deltas.csv}`, `experiments/run_excess_basis.py`, `tests/test_excess_basis.py`, and `experiments/results_excess/{artifact_manifest.json,significance_report.json,significance_report.md}`. Generated artifacts must be regenerated only through their governed runners. All other files and scientific behaviour remain protected.

- **Current state (2026-07-29; append-only):** `TWO_BLOCKING_DEFECTS / NARROW_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Two-blocker narrow repair completion (2026-07-29; append-only):** the authorized repair reports continuous descriptor/device/inode authority for bounded temporary publication, including refusal of real-directory replacement, rename-away, deletion, symlink replacement, and stale authority use. Static Make provenance now handles leading-whitespace directives, records `research-excess` `.PHONY` membership truthfully, and fails closed on `SHELL`, `.SHELLFLAGS`, `MAKESHELL`, and `MAKEFLAGS` semantics without executing Make, a shell, or subprocesses.

- **Verification (2026-07-29; append-only):** focused missingness `146`, focused excess `292`, registry `16`, full root `794`, backend `99`; docs lint, claims lint, and data validation passed. Missingness and excess generation were byte-identical across two runs. Nothing was staged, committed, or pushed.

- **Scientific preservation (2026-07-29; append-only):** 2025→2026, 40 tickers, 12 features, four source groups, A=`4`, B=`160`, C=`12`, D=`480`, 656 scenarios, 26,240 rows, zero cohort changes. R3-TGT-01 remains primary `0/6`, sensitivity `0/6`, either `0/6`, with 120 rank-audit rows and zero mismatches.

- **Current artifact hashes (2026-07-29; append-only):** `missingness_report.json` `2b9892bd4dde0a72bf7206706710efa22c178a91fac5b1ca69a778526bcac742`; `missingness_report.md` `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` `4405ab26abef7893c900cee979f9a039387b5f8643b3faa484428ca295344dbe`; excess `significance_report.json` `090c6aadb5000d8045605b7d5966a318ea8b4bc8aea7b5bd7215a1cd35582d06`; excess `significance_report.md` `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Scientific boundary (2026-07-29; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Current state (2026-07-29; append-only):** `TWO_BLOCKER_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Final fresh independent re-review (2026-07-29; append-only; CHANGES_REQUIRED — two blockers):** the review independently reconfirmed deterministic artifacts, unchanged scientific evidence, missingness `146`, excess `292`, registry `16`, full root `794`, backend `99`, and passing documentation lint, claims lint, and data validation. Transaction commit-point safety, frozen ancestor-chain revalidation, and complete service-response replay remain repaired. Verdict is **CHANGES_REQUIRED** because two repository-integrity blockers remain.

- **Blocking defect — final authorized directory identity (2026-07-29; append-only):** both publication APIs reject intermediate-component attacks but accept replacement, rename-away, or deletion of an existing final authorized directory and continue publication. When the final directory exists at authorization, its device/inode identity must remain bound and must be revalidated before publication. Replacement, deletion, rename-away, symlink substitution, or identity mismatch must fail closed.

- **Blocking defect — ambiguous Make target forms (2026-07-29; append-only):** Make provenance accepts multi-target rules and variable-derived target definitions or duplicates without changing authority. The static subset must fail closed on multi-target rules involving `research-excess`, variable-derived target names, derived duplicates, and every ambiguous target form that could affect the governed target.

- **Owner-authorized final narrow repair (2026-07-29; append-only):** authorize edits only to the existing R3-MISS-01 missingness/excess generators, their tests, and their governed generated reports. Governance, Makefile, registry, backend, frontend, data, scientific logic, leaderboard, and prediction dumps remain protected.

- **Current state (2026-07-29; append-only):** `TWO_BLOCKERS_REMAIN / FINAL_NARROW_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Final two-blocker repair completion (2026-07-29; append-only):** the authorized repair reports that existing final output-directory identity is now bound at authorization and revalidated before staging and publication across both structured and legacy APIs. Replacement, rename-away, deletion, recreation, symlink substitution, and unexpected appearance of an absent final directory fail closed. Static Make provenance now rejects multi-target, continued multi-target, variable-derived, pattern-derived, duplicate, static-pattern, and inline-recipe target definitions that could ambiguously affect `research-excess`, without invoking Make, a shell, or subprocesses.

- **Verification (2026-07-29; append-only):** missingness `170`, excess `335`, registry `16`, full root `861`, backend `99`; docs lint, claims lint, and data validation passed. Both governed runners were executed twice with byte-identical outputs. Nothing was staged, committed, or pushed.

- **Scientific preservation (2026-07-29; append-only):** 2025→2026, 40 tickers, 12 features, four groups, A=`4`, B=`160`, C=`12`, D=`480`, 656 scenarios, 26,240 rows. R3-TGT-01 remains primary `0/6`, sensitivity `0/6`, either `0/6`, with 120 rank-audit rows and zero mismatches.

- **Current artifact hashes (2026-07-29; append-only):** `missingness_report.json` `60b6ed1cc429e46df9d96e9a8c3942bdc426857c3e94db682c50de580aef0617`; `missingness_report.md` `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` `a39c55977fe767458961fafb072d5b9fc8bb3bde2fee6832b01797ee4b067b33`; excess `significance_report.json` `737ce290240a180c6af3f5a8b95e3dc3705d2240e087890045fa7551bf6bd054`; excess `significance_report.md` `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Previous artifact hashes for independent comparison (2026-07-29; append-only):** prior `missingness_report.json` `2b9892bd4dde0a72bf7206706710efa22c178a91fac5b1ca69a778526bcac742`; prior excess `artifact_manifest.json` `4405ab26abef7893c900cee979f9a039387b5f8643b3faa484428ca295344dbe`; prior excess `significance_report.json` `090c6aadb5000d8045605b7d5966a318ea8b4bc8aea7b5bd7215a1cd35582d06`; prior excess Markdown already matched the current hash.

- **Scientific boundary (2026-07-29; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Current state (2026-07-29; append-only):** `FINAL_TWO_BLOCKER_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.

- **Final independent re-review — legacy publication blocker (2026-07-29; append-only):** the detached review reconfirmed deterministic artifacts, unchanged R3-MISS-01 and R3-TGT-01 scientific evidence, missingness `170`, excess `335`, registry `16`, full root `861`, backend `99`, and passing docs lint, claims lint, and data validation. Structured publication and all Make-provenance repairs passed. Verdict remains **CHANGES_REQUIRED** because one legacy publication blocker remains.

- **Blocking defect — legacy final-directory symlink substitution (2026-07-29; append-only):** the legacy `resolve_output_authority` plus retained-ledger publication flow accepts in-root symlink substitution of the final governed directory. Both replacement of an existing authorized directory and unexpected symlink appearance when the directory was absent caused all three missingness artifacts to be written into the replacement target. The legacy flow must preserve and revalidate the same final-directory identity or absence contract enforced by the structured authority API.

- **Owner-authorized final legacy repair (2026-07-29; append-only):** authorize new edits only to `experiments/missingness_sensitivity.py`, `tests/test_missingness_sensitivity.py`, and the three governed `experiments/results_missingness` artifacts. Make provenance, excess implementation and reports, governance outside this append-only record, Makefile, registry, backend, frontend, data, models, scientific logic, leaderboard, and prediction dumps remain protected.

- **Current state (2026-07-29; append-only):** `ONE_BLOCKER_REMAINS / LEGACY_PUBLICATION_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.



- **Final legacy publication repair completion (2026-07-30; append-only):** the authorized repair reports that the retained-ledger legacy publication flow now preserves the final-component authorization contract without dereferencing the final path component. Parent-only normalization is used, durable `_LEGACY_CLAIMS` retain the authority claim, the private path-only fallback was removed, publication explicitly invokes `PublicationAuthority.assert_authorizes`, and canonical comparison no longer resolves the final component. Existing-directory replacement and unexpected final-directory symlink appearance now fail closed.

- **Verification (2026-07-30; append-only):** focused missingness `202`, artifact registry `16`, full root `893`, backend `99`; documentation lint, claims lint, and data validation passed. Governed missingness artifacts were regenerated. Nothing was staged, committed, or pushed.

- **Scientific preservation (2026-07-30; append-only):** 2025→2026, 40 tickers, 12 features, four source groups, A=`4`, B=`160`, C=`12`, D=`480`, 656 scenarios, 26,240 rows. R3-TGT-01 remains primary `0/6`, sensitivity `0/6`, either `0/6`, with 120 rank-audit rows and zero mismatches.

- **Current artifact hashes (2026-07-30; append-only):** `missingness_report.json` `5e9b6dff8720c5633cd4213fdbdc84dd9b6791671ede1fcf868b40271b7a61c8`; `missingness_report.md` `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`. Excess artifacts remain `artifact_manifest.json` `a39c55977fe767458961fafb072d5b9fc8bb3bde2fee6832b01797ee4b067b33`; `significance_report.json` `737ce290240a180c6af3f5a8b95e3dc3705d2240e087890045fa7551bf6bd054`; `significance_report.md` `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Scientific boundary (2026-07-30; append-only):** Serving-heuristic sensitivity only — describes how this deterministic ranking recipe responds to missing inputs; it does not measure predictive skill, which remains indistinguishable from the null.

- **Current state (2026-07-30; append-only):** `FINAL_LEGACY_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **Final legacy publication independent re-review (2026-07-30; append-only; APPROVED):** a fresh detached review inspected the complete 14-path R3-MISS-01 change set at base `4fc1136a3b0b65a547799a1c475ef4c4598572ce`. The reviewer independently exercised 15 legacy and structured publication attacks covering existing and absent final-directory symlink substitution, real-directory replacement, rename-away, deletion/recreation, consumed or evicted claims, resolution aliases, mismatched explicit authority, canonical namespace attacks, and unauthorized in-repository output. All attacks failed closed with zero governed-file leakage.

- **Independent verification (2026-07-30; append-only):** focused missingness `202`, focused excess `335`, artifact registry `16`, full root `893`, and backend `99` passed with zero failures. Documentation lint, claims lint, and data validation passed. Two isolated governed missingness regenerations were byte-identical to each other and to the recorded artifacts.

- **Scientific and artifact verification (2026-07-30; append-only):** 2025→2026, 40 tickers, 12 features, four source groups, A=`4`, B=`160`, C=`12`, D=`480`, 656 scenarios, and 26,240 rows were independently reconstructed. R3-TGT-01 remains primary `0/6`, sensitivity `0/6`, either `0/6`; the rank audit remains 120 rows with zero mismatches. Leaderboard and prediction artifacts remain byte-identical to HEAD.

- **Approved artifact hashes (2026-07-30; append-only):** `missingness_report.json` `5e9b6dff8720c5633cd4213fdbdc84dd9b6791671ede1fcf868b40271b7a61c8`; `missingness_report.md` `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` `a39c55977fe767458961fafb072d5b9fc8bb3bde2fee6832b01797ee4b067b33`; excess `significance_report.json` `737ce290240a180c6af3f5a8b95e3dc3705d2240e087890045fa7551bf6bd054`; excess `significance_report.md` `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Review limitations (2026-07-30; append-only):** byte reproducibility was verified in the reviewer's macOS/Python environment rather than claimed universally; excess regeneration was not repeated because the final repair did not alter excess files and their hashes remained unchanged; commit-point fault injection was covered by the passing repository tests rather than a second independent real-filesystem harness. These were judged non-blocking.

- **Final independent verdict (2026-07-30; append-only):** `APPROVED`.

- **Current state (2026-07-30; append-only):** `INDEPENDENT_REVIEW_APPROVED / COMMIT_READY / OWNER_MANUAL_COMMIT_PENDING`.


- **R3-MISS-01 post-merge closure (2026-07-30; append-only):** PR `#2` was merged into `main` through merge commit `fa0999577a9e0509bf3f2c53077734f7bcd5a201`. The merged change set preserves the independently approved 14-path scope.

- **Post-merge verification (2026-07-30; append-only):** focused missingness, excess, and artifact-registry suites passed `553/553`; the complete root suite passed `893/893`; backend passed `99/99`. Documentation lint, claims lint, and data validation passed.

- **Post-merge artifact verification (2026-07-30; append-only):** `missingness_report.json` `5e9b6dff8720c5633cd4213fdbdc84dd9b6791671ede1fcf868b40271b7a61c8`; `missingness_report.md` `80b52934c6cac69f7b267f2f8b3a11abbaa48437a884b4527ed2e613b597200f`; `rank_deltas.csv` `cb7c60064d0f25ce67304e1527d3399057c47f9606e3acad88e1d5215866c9cd`; excess `artifact_manifest.json` `a39c55977fe767458961fafb072d5b9fc8bb3bde2fee6832b01797ee4b067b33`; excess `significance_report.json` `737ce290240a180c6af3f5a8b95e3dc3705d2240e087890045fa7551bf6bd054`; excess `significance_report.md` `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Scientific standing at closure (2026-07-30; append-only):** R3-TGT-01 remains primary `0/6`, sensitivity `0/6`, either `0/6`; no reliable predictive edge is established. Missingness analysis remains a serving-heuristic sensitivity measurement, not evidence of predictive skill.

- **Final state (2026-07-30; append-only):** `MERGED / POST_MERGE_VERIFIED / R3_MISS_01_CLOSED`.

### R3-UI-01 — Skeptic challenge panel (per-ticker UI)
- **Priority:** P1. **Wave:** 3D (first UI task). **Owner role:** Frontend. **Risk:** medium (claim surface). **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** `GET /research/skeptic/{ticker}` shipped + tested (R2-SKEPTIC-01), returns `{ticker, checks[], footer}` with verdict/evidence/severity per check; grep confirms no frontend surface besides the Courtroom embed; R2-SKEPTIC-01 explicitly deferred the panel. `CompanyResearchDetailPage.jsx` (route `/research/companies/:ticker`) is already in the MCC `required_disclaimer.pages`.
- **Read first:** `backend/app/services/skeptic_service.py` response shape (or its backend test), `CompanyResearchDetailPage.jsx` fully (fetch/fallback/caveat patterns), `frontend/src/api/researchApi.js`, `docs/DEMO_RUNBOOK.md` §5 (the curl the demo already uses).
- **Files to modify:** `CompanyResearchDetailPage.jsx` (add the panel), `frontend/src/api/researchApi.js` (one fetch function). **No new page, no route change, no backend change.**
- **Design (binding):** prosecution-style panel in the Research Terminal language: one row per check (check name, verdict chip pass/warn/fail/insufficient_data, expandable evidence items with `source_file` shown), the service's fixed footer rendered verbatim and non-collapsible: "**Surviving these checks means *not obviously broken*, not *predictive* — walk-forward IC remains ≈ 0 and no model survives family-wise correction.**" Labeled fallback state per page conventions when the API fails. No new claim copy beyond the service's own strings + neutral labels.
- **Acceptance criteria:** renders real ASELS + one coverage-gap ticker (e.g. ASTOR) with backend up (recorded curl/screenshot or, if visual verification is blocked by auth, the recorded API payloads + build green — state which); `npm run build` green; `make claims-lint` green; backend suite untouched.
- **Verification:** `cd frontend && npm run build`; `make claims-lint`; `PYTHONPATH=backend python -m pytest backend/tests` (unchanged); manual check recorded.
- **Generated artifacts:** none. **Failure modes:** styling "pass" verdicts as endorsements (green-glow trap — use neutral terminal chips); hiding the footer below the fold. **Rollback:** revert two files. **Stop:** response shape differs from the backend test's pin (report, don't adapt silently).
- **Commit scope:** two frontend files. **Theme:** "Surface Skeptic challenge report per ticker". Manual commit: yes. **/clear:** yes.


- **R3-UI-01 implementation and review closure (2026-07-30; append-only):** the company research detail page now surfaces the existing `GET /research/skeptic/{ticker}` report through one new `researchApi.skeptic` fetch function. The panel renders one expandable row per backend check, neutral verdict chips, evidence facts with `source_file`, and the backend-authored footer verbatim in an always-visible non-collapsible block. Skeptic-fetch failures remain isolated from the existing company score and feature panels.

- **Independent review (2026-07-30; append-only):** the first review returned `CHANGES_REQUIRED` solely because evidence rows used array-index React keys. The repair replaced them with deterministic keys derived from `check_id`, `source_file`, and `fact`. Fresh independent re-review returned `APPROVED`, confirming the exact two-file scope, stable keys, semantic toggle buttons, correct `aria-expanded`, neutral PASS presentation, claim safety, and the pinned ASELS and ASTOR backend behavior.

- **Merge and post-merge verification (2026-07-30; append-only):** PR `#3` merged into `main` through merge commit `9d8622d16b697284b0930e42d969340ce016c59d`. On merged `main`, the frontend production build passed, claims lint passed under Model Confidence Contract `v1.8.0`, backend passed `99/99`, and `git diff --check` passed. The merged API function and deterministic evidence key were confirmed directly in source.

- **Verification limitation (2026-07-30; append-only):** no live authenticated Supabase browser session was recorded. UI behavior was verified through the complete source diff, production build, pinned backend tests, real ASELS and ASTOR fixture reports, and independent review. This limitation was judged non-blocking.

- **Final state (2026-07-30; append-only):** `MERGED / POST_MERGE_VERIFIED / R3_UI_01_CLOSED`.

### R3-UI-03 — Calibration audit surfaced
- **Priority:** P2. **Wave:** 3D. **Owner role:** Backend+Frontend. **Risk:** low-medium. **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** `experiments/results/calibration_report.{json,md}` committed (R2-CAL-01): hybrid confidence constant 0.25 across 240 audited ticker-year outcomes → calibration not estimable; the UI displays confidence numbers on multiple pages while this audit result is invisible; no backend endpoint serves the report.
- **Read first:** `calibration_report.json` keys (verified present: `calibration`, `claim_safety`, `confidence_quantity`, `replay_provenance`, `sample`, `limitations`), `backend/app/services/research/significance.py` (the passthrough-loader pattern to copy), `routers/research.py` mounting.
- **Files to modify:** new `backend/app/services/research/calibration.py` (proposed — filtered read-only passthrough); route in `routers/research.py`; backend test; `frontend/src/pages/ValidationLabPage.jsx` panel (page already MCC-registered); `researchApi.js`; `model_confidence_contract.json` (`scan.backend_response_files` += the new service; minor version bump per procedure).
- **Design (binding):** panel copy (verbatim; changes require editing this packet): "**Confidence audited (R2-CAL-01, replay of git `a95e1e1c`): the hybrid confidence component was constant at 0.25 across all 240 audited ticker-year outcomes, so calibration against rank error is not estimable at this scale. Confidence is not a probability of return or recommendation strength.**" Replay-SHA framing mandatory; no tuning, no service change; the endpoint serves the committed report's fields only.
- **Acceptance criteria:** endpoint shape test; panel renders real data / labeled fallback; suites + build + `make claims-lint` green after contract bump.
- **Verification:** backend pytest; `cd frontend && npm run build`; `make claims-lint`.
- **Generated artifacts:** none. **Failure modes:** paraphrasing the finding into "confidence is unreliable" (wrong — it is *unaudited-able at this scale*; use the verbatim copy); recomputing anything server-side. **Rollback:** revert; additive. **Stop:** report keys differ from spec.
- **Commit scope:** service + route + test + page + contract bump. **Theme:** "Surface the confidence calibration audit". Manual commit: yes. **/clear:** yes.

### R3-UI-02 — Return-basis lens (nominal / real-TRY / USD)
- **Priority:** P1. **Wave:** 3C. **Owner role:** Backend+Frontend. **Risk:** **high (three quotable IC families side by side).** **Model/effort:** **Opus, medium-high.** Independent review: **Fable, low (copy check mandatory).**
- **Verified current state:** `experiments/results_real_terms/` significance artifacts + `data/trusted_clean/alternative_targets_report.*` committed with zero frontend surface (grep verified); METHODOLOGY pins the quotable numbers (real-TRY RF pooled IC −0.156, Bonferroni p=0.0984; USD −0.150, p=0.1278; 2022 illustration 185.94% nominal → 74.07% real) and their mandatory qualifiers.
- **Read first:** METHODOLOGY §Alternative return-basis evaluation (the wording authority), the actual artifact layout — `experiments/results_real_terms/comparison_report.{json,md}` at top level plus per-basis subdirectories `real_try/` and `usd/` each holding `significance_report.{json,md}`, `leaderboard.csv`, and prediction dumps (verified 2026-07-13); `services/research/regime.py` + `significance.py` (loader patterns); `BenchmarkPage.jsx` fully.
- **Files to modify:** new `backend/app/services/research/real_terms.py` (proposed — read-only passthrough composing the top-level `comparison_report.json` with the two per-basis `significance_report.json` files + the `data/trusted_clean/alternative_targets_report.json` summary); route in `routers/research.py`; backend test; `BenchmarkPage.jsx` panel; `researchApi.js`; MCC (`scan.backend_response_files` += service; version bump).
- **Design (binding):** a display panel, NOT a data toggle — chart series are never recomputed or re-based client-side; the panel shows, per basis (nominal/real/USD): pooled IC of the family-selected model with raw AND adjusted p always paired, plus the 2022 nominal-vs-real illustration with its METHODOLOGY qualifier verbatim: "**an inflation-basis illustration only, not a strategy-performance or investment-value statement.**" Panel header caveat (verbatim): "**The no-reliable-edge conclusion was re-evaluated separately on CPI-deflated TRY and USD bases; neither survives family-wise correction. Basis changes the unit of measurement, not the conclusion.**"
- **Acceptance criteria:** every number on the panel byte-matches its source artifact; raw p never renders without adjusted p; suites + build + lint green; Fable copy review recorded.
- **Verification:** backend pytest; `cd frontend && npm run build`; `make claims-lint`; number-by-number source check recorded in the report.
- **Generated artifacts:** none. **Failure modes / misleading success:** a "toggle" that implies the charts themselves are re-based (they are not — display panel only); quoting the real-terms raw p=0.0164 without its adjusted 0.0984 (the exact trap the pairing rule kills); presenting the negative ICs as a contrarian finding. **Rollback:** revert; additive. **Stop:** any pressure to recompute returns client-side.
- **Commit scope:** service + route + test + page + contract bump. **Theme:** "Surface per-basis significance lens". Manual commit: yes. **/clear:** yes.
- **Implementation evidence (2026-07-18; append-only; independent Fable 5 copy review PENDING; NOT merge-ready):** worktree `return-basis-lens-ui-f7c7bf` on branch `local/return-basis-lens-ui-f7c7bf` (the packet named `r3-ui-02-return-basis`; the harness placed this session in a sibling worktree at the same commit `68d2681a` and forbade switching — work was done here and the naming discrepancy is recorded honestly; both worktrees clean/identical at start). New read-only passthrough `backend/app/services/research/real_terms.py` composes the canonical nominal report (`experiments/results/significance_report.json`), the two alternative-basis reports (`experiments/results_real_terms/{real_try,usd}/significance_report.json`), the top-level `comparison_report.json` (claim-safety conclusion + cross-check of the alt bases), and the `alternative_targets_report.json` design block; it performs no arithmetic, refuses (`503`) when any headline lacks a raw **or** adjusted p-value, and cross-verifies the alt bases against the comparison report before composing. `GET /research/return-basis` added to `routers/research.py` (existing `require_access`, no auth change). Each of the three basis objects carries raw and adjusted p in the same structure (inseparable by design). The 2022 illustration (nominal 185.94% / real 74.07% / CPI 64.27%) is METHODOLOGY-authored (the task's wording authority); nominal 185.94% and CPI 64.27% are byte-identical to the committed 2022 `bist100_return_pct` / `cpi_december_yoy_pct` in `experiments/results_regime/regime_context_report.json` (pinned in tests). Backend-owned mandatory copy (panel caveat + illustration qualifier) rendered verbatim. Frontend: `BenchmarkPage.jsx` gains a **display-only** `ReturnBasisLens` panel (three neutral basis cards + the 2022 illustration + always-visible caveat) fetched via new `researchApi.returnBasis()`; the tide chart's data and behavior are untouched — no basis selector, no client-side return/IC/p recomputation, no rebasing, neutral styling for negative ICs (no green/arrows/good-bad). MCC bumped **v1.7.0 → v1.8.0** (effective 2026-07-18): `real_terms.py` added to `scan.backend_response_files`; BenchmarkPage remains registered in `required_disclaimer.pages`; the five version-pin tests updated to 1.8.0. New `backend/tests/test_return_basis_api.py` 14/14; root MCC/contract tests 15/15; full backend suite 99/99; `cd frontend && npm run build` succeeded; `make claims-lint` PASSED (MCC v1.8.0). `git diff --check` clean; `frontend/dist` gitignored (no dist changes); no `experiments/**`, `data/trusted_clean/alternative_targets_report.*`, METHODOLOGY, chart-dataset, or canonical-result file changed. Visual browser verification not performed (protected-page Supabase auth gate / full-stack env, per prior tasks) — recorded endpoint payload + successful build stand in; no fabricated screenshot claim. Review handoff: `docs/R3_UI_02_FABLE5_REVIEW_HANDOFF.md`. Status **PENDING INDEPENDENT FABLE 5 COPY REVIEW**; not merge-ready; no commit or push.
- **Independent review evidence (2026-07-18; append-only; APPROVED):** the mandatory independent Fable 5 copy review was performed outside the repository record and returned APPROVED with no required fixes; the REV-01 governance session then re-verified it against committed HEAD 18514ac51f7e7912caf5a04b3b6526e77ce53f98 (the implementation commit): both mandatory sentences byte-exact; all nine per-basis values full-precision matches (nominal −0.15328380688030444 / 0.0182981701829817 / 0.10978902109789021; real-TRY −0.15562044542255335 / 0.0163983601639836 / 0.0983901609839016; USD −0.1504698753771919 / 0.0212978702129787 / 0.1277872212778722); 2022 illustration 185.94% / 74.07% / 64.27% (METHODOLOGY authority; regime-report byte-match pinned); raw/adjusted structurally inseparable with 503 semantics; no recomputation, no chart rebase, no toggle, no contrarian framing; MCC v1.8.0 scoped; no protected artifact changed. Re-verified: test_return_basis_api.py 14/14, backend 99/99, claims lint green, frontend build green. The PENDING status in the 2026-07-18 implementation bullet and the handoff is superseded; the task is committed at HEAD and closed. The worktree-name discrepancy recorded in the handoff (return-basis-lens-ui-f7c7bf vs packet r3-ui-02-return-basis) is accepted.


- **R3-UI-03 cross-task frozen-boundary decision (2026-07-30; append-only):** the required Model Confidence Contract minor bump from `v1.8.0` to `v1.9.0` legitimately changes `model_confidence_contract.json`, which remains a member of the R3-MISS-01 excess-basis protected boundary. The resulting frozen-boundary digest mismatch is an expected cross-task provenance update, not a scientific-result change.

- **Owner decision (2026-07-30; append-only):** retain `model_confidence_contract.json` inside the protected boundary. Do not exempt, remove, weaken, or special-case it. Authorize a narrow compatibility repair limited to `experiments/run_excess_basis.py`, `tests/test_excess_basis.py`, and governed regeneration of `experiments/results_excess/{artifact_manifest.json,significance_report.json,significance_report.md}`. Only digest, provenance, manifest, and directly dependent pinned values may change; scientific results must remain unchanged.

- **Pre-commit root-suite condition (2026-07-30; append-only):** the staged service-source guard test `test_service_source_unchanged_and_recorded` observes unstaged backend changes and therefore cannot represent the final committed tree directly. Full-root validation may be performed in a disposable snapshot with the complete reviewed change set staged there, while the source worktree remains unstaged.

- **Current state (2026-07-30; append-only):** `IMPLEMENTATION_COMPLETE / CROSS_TASK_REPIN_AUTHORIZED / FRESH_INDEPENDENT_REVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 frozen-boundary compatibility repair completion (2026-07-30; append-only):** the required MCC `v1.8.0 → v1.9.0` update remains inside the R3-MISS-01 protected boundary. The boundary retains 351 members. Its pinned digest moved from `634d7151e75f0ec7a85e412f748ac81499a4fad5e9eac71ab1a5c920f0137dd9` to `0b0083a458ff24e9414ed23c12fb58f40ebe22c94539e6979b0c7affcf6d76ba`; independent per-member comparison found that only `model_confidence_contract.json` changed within the boundary. No exemption, carve-out, or special case was introduced.

- **Governed regeneration (2026-07-30; append-only):** excess artifacts were regenerated through `make research-excess`. `artifact_manifest.json` moved from `a39c55977fe767458961fafb072d5b9fc8bb3bde2fee6832b01797ee4b067b33` to `034559139a884dd2fe7a5ba8799a8ba0e5cc2860136fec0389feaeff6d310b3d`; `significance_report.json` moved from `737ce290240a180c6af3f5a8b95e3dc3705d2240e087890045fa7551bf6bd054` to `a8729cf2429444a273e5f8e10a355681279a5f20574cf83eed3b256775e8a0fa`; `significance_report.md` remained `5b74228fe6aeae49cca9545b5d133e25d7f73491f48561176993b446871404af`.

- **Scientific preservation (2026-07-30; append-only):** regenerated excess reports retain identical scientific and numerical findings. Of 2,596 JSON leaves, only four significance-report leaves changed, all provenance fields; of 1,593 numeric leaves, only the generator source-file size changed. Every IC, p-value, confidence interval, family conclusion, and count remains bit-identical. Leaderboard and prediction artifacts remain unchanged. The conclusion remains `0/6` surviving family-wise correction and no reliable predictive edge.

- **Verification (2026-07-30; append-only):** focused excess `335`, focused calibration API `9`, backend `108`, MCC/claim-safety pins `17`, and disposable staged full-root `893` passed. Frontend production build, documentation lint, claims lint under MCC `v1.9.0`, data validation, and `git diff --check` passed.

- **Verification limitation (2026-07-30; append-only):** no live authenticated Supabase browser session was recorded. Calibration-panel behavior is supported by the filtered endpoint tests, fixed-copy pins, source review, claim lint, and production build.

- **Current state (2026-07-30; append-only):** `COMPATIBILITY_REPAIR_COMPLETE / FRESH_INDEPENDENT_REVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 first independent review (2026-07-30; append-only; CHANGES_REQUIRED):** the fresh review confirmed the exact 17-path scope, byte-verbatim fixed copy, filtered read-only calibration passthrough, correct MCC `v1.9.0` registration, retained 351-member frozen boundary, truthful governed regeneration, unchanged scientific findings, and immutable leaderboard and prediction artifacts. Verdict remains `CHANGES_REQUIRED` because two release blockers remain.

- **Blocking defect — incomplete calibration fail-closed behavior (2026-07-30; append-only):** contradictory artifact states such as `calibration.status="estimable"`, `confidence_unique_values=2`, or estimable monotonicity currently return `200`, while malformed nested calibration content can escape as `500`. Missing, malformed, wrong-task, and scientifically contradictory artifacts must consistently fail through the public route with the intended `503`.

- **Blocking defect — documentation lint citation (2026-07-30; append-only):** the append-only record cited a pytest node ID as though it were a repository path. The citation is authorized to be rewritten as a test name without changing the recorded evidence.

- **Owner-authorized narrow repair (2026-07-30; append-only):** authorize changes only to `backend/app/services/research/calibration.py`, `backend/app/routers/research.py` if required for correct exception translation, `backend/tests/test_calibration_api.py`, and this append-only governance record. Do not alter the MCC, frozen-boundary implementation or pins, governed excess artifacts, calibration artifact, frontend behavior, fixed copy, scientific logic, leaderboard, or prediction dumps.

- **Current state (2026-07-30; append-only):** `TWO_BLOCKERS_REMAIN / NARROW_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 blocking-findings repair completion (2026-07-31; append-only):** the calibration loader now explicitly validates the required top-level and nested object types before indexing and verifies every artifact fact needed to support the fixed panel copy. Missing, invalid-JSON, invalid-UTF-8, unreadable, wrong-task, malformed, and scientifically contradictory artifacts are translated through `CalibrationReportMissing` to the public route's intended `503`. Unrelated programming errors are not hidden by a broad exception handler.

- **Adversarial route verification (2026-07-31; append-only):** public-route tests now cover contradictory calibration status, altered confidence cardinality or values, inconsistent confidence quantity or score, estimable monotonicity, unexpected coefficients or intervals, altered outcome count, replay-SHA mismatch, wrong task, malformed containers and nested values, missing fields, wrong scalar types, invalid JSON, invalid UTF-8, unreadable artifacts, and missing files. The valid committed artifact remains a `200` control.

- **Documentation repair (2026-07-31; append-only):** the pytest node-ID citation previously interpreted as a repository path was rewritten as a test-name reference. Documentation lint now passes.

- **Updated verification totals (2026-07-31; append-only):** focused calibration API `160`, complete backend `259`, MCC/claim-safety pins `17`, frontend production build, documentation lint, claims lint under MCC `v1.9.0`, data validation, and `git diff --check` passed.

- **Root-suite environment evidence (2026-07-31; append-only):** a disposable staged snapshot collected 893 root tests and passed `885/893` under Python `3.11.0`, NumPy `2.4.6`, pandas `2.2.2`, SciPy `1.17.1`, and scikit-learn `1.9.0`. The same eight excess-basis numerical-replay failures reproduced on untouched committed HEAD in the identical environment, including constructor and single-digit floating-point drift. No scientific code or frozen authority was changed to accommodate the environment. Fresh review must attempt the previously green `/opt/anaconda3` Python environment and independently adjudicate this evidence.

- **Preservation (2026-07-31; append-only):** the fixed backend/frontend copy, calibration artifact, MCC `v1.9.0`, 351-member frozen boundary, governed excess artifacts and pins, leaderboard, prediction dumps, frontend behavior, and all scientific findings remain unchanged by this repair.

- **Current state (2026-07-31; append-only):** `TWO_BLOCKER_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 second independent re-review (2026-08-01; append-only; CHANGES_REQUIRED):** the fresh review confirmed the previous two blockers are repaired, the exact 17-path scope is preserved, `/opt/anaconda3` reproduces `893/893` on both the staged change set and clean HEAD, and all frozen-boundary, generated-artifact, claim-safety, and scientific-preservation checks pass. Verdict remains `CHANGES_REQUIRED` because one fail-closed schema blocker remains.

- **Blocking defect — incomplete nested-value validation (2026-08-01; append-only):** some nested values are container-checked but passed through without element-level validation. Numeric `limitations` items, numeric `confidence_reasons` items, malformed `sample.target_years`, malformed `replay_provenance.code_version` contents, and contradictory verdict/reason/bin/seed values can still return `200`. Every response field must be validated to the exact type and allowed semantic contract before passthrough; malformed or contradictory values must return the intended public-route `503`.

- **Owner-authorized final narrow repair (2026-08-01; append-only):** authorize edits only to `backend/app/services/research/calibration.py` and `backend/tests/test_calibration_api.py`. The router may change only if exception translation is demonstrably required. Do not modify governance outside this append-only record, frontend, MCC, frozen-boundary implementation or pins, generated excess artifacts, calibration artifact, scientific logic, leaderboard, or prediction dumps.

- **Current state (2026-08-01; append-only):** `ONE_BLOCKER_REMAINS / FINAL_NARROW_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 final nested-validation repair completion (2026-08-01; append-only):** every field returned by `GET /research/calibration` is now validated before passthrough. Element-level validation was added for `limitations`, `confidence_reasons`, `sample.target_years`, model and row-count fields, `replay_provenance.code_version`, calibration verdict/reason/bin fields, random seeds, and the complete returned claim-safety block. Integer validators explicitly reject booleans.

- **Semantic fail-closed contract (2026-08-01; append-only):** altered verdict or reason text, invalid or inconsistent bins, malformed target years, malformed code-version values, invalid seeds, altered claim-safety conclusions, malformed list elements, and all other tested type or semantic substitutions now raise `CalibrationReportMissing` and return public-route `503`. The valid committed artifact remains the `200` control, and unrelated programming errors continue to propagate rather than being disguised as artifact failures.

- **Adversarial expansion (2026-08-01; append-only):** `71` additional mutation cases were added. Together with the existing `63`, the matrix now contains `134` cases. Each mutation is exercised through both route-level status and service-level exception tests, producing `302` focused calibration test executions.

- **Updated verification totals (2026-08-01; append-only):** focused calibration API `302`, complete backend `401`, excess-basis `335`, disposable staged full-root `893`, frontend production build, documentation lint, claims lint under MCC `v1.9.0`, and `git diff --check` passed using `/opt/anaconda3/bin/python`.

- **Preservation (2026-08-01; append-only):** this repair changed only `backend/app/services/research/calibration.py` and `backend/tests/test_calibration_api.py` relative to the prior reviewed change set. The router, frontend, fixed panel copy, MCC, 351-member frozen boundary, boundary pins, generated excess artifacts, committed calibration artifact, leaderboard, prediction dumps, and scientific findings were not changed.

- **Current state (2026-08-01; append-only):** `FINAL_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 third independent re-review (2026-08-02; append-only; CHANGES_REQUIRED):** the complete 17-path change set, MCC `v1.9.0`, 351-member protected boundary, governed excess regeneration, scientific preservation, frontend behavior, fixed copy, and all standard test gates were independently reconfirmed. The disposable staged root suite passed `893/893`. Verdict remains `CHANGES_REQUIRED` because three fail-closed defects remain inside the calibration service.

- **Blocking defect — whole-block passthrough (2026-08-02; append-only):** `sample` and `claim_safety` are validated only for known fields and then returned as their original complete artifact objects. Unknown injected keys and arbitrary nested values are therefore served with `200`, including unsupported predictive-skill assertions. Both blocks must use exact allowed-key contracts, reject unknown keys, and be reconstructed field-by-field before response serialization.

- **Blocking defect — non-finite numbers (2026-08-02; append-only):** Python JSON parsing accepts bare `NaN`, `Infinity`, and `-Infinity`. Non-finite values can pass the current numeric validator and later fail during HTTP serialization, producing `500`. Non-standard JSON constants and all non-finite numeric values must fail through `CalibrationReportMissing` and return `503`.

- **Blocking defect — bootstrap request count (2026-08-02; append-only):** `bootstrap_samples_requested` currently accepts zero and negative integers. It must be a strictly positive integer, and bootstrap-count relationships must be internally consistent.

- **Test-count correction (2026-08-02; append-only):** the earlier record correctly reported `302` focused test executions but incorrectly described the parametrized matrix as `134` cases. The actual matrix before this repair contains `144` parametrized cases: `73` pre-existing plus `71` newly added. Each runs through two parametrized tests, with `14` additional non-parametrized tests, giving `144 × 2 + 14 = 302`.

- **Owner-authorized narrow repair (2026-08-02; append-only):** authorize edits only to `backend/app/services/research/calibration.py` and `backend/tests/test_calibration_api.py`. The router may change only if exception translation is demonstrably required. Also authorize closing directly related hardening observations in the same service: reconstruct returned objects rather than exposing cached mutable references, require semantically mandatory strings to be non-empty, require finite/range-valid numbers, and reject duplicate list values where duplicates would make the response contract ambiguous. Do not modify frontend, MCC, frozen-boundary code or pins, generated artifacts, calibration artifact, scientific files, leaderboard, prediction dumps, or other governance text.

- **Current state (2026-08-02; append-only):** `THREE_BLOCKERS_REMAIN / NARROW_REPAIR_AUTHORIZED / FRESH_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 third-blocker repair completion (2026-08-02; append-only):** the calibration service now enforces exact allowed-key contracts for every returned object and reconstructs all response objects and lists from validated values. `sample`, `claim_safety`, calibration, monotonicity, confidence quantity, replay provenance, and code-version blocks no longer expose original cached dictionaries or unknown artifact keys.

- **Non-finite-number fail-closed repair (2026-08-02; append-only):** JSON parsing now rejects bare `NaN`, `Infinity`, and `-Infinity`, while numeric validators reject every non-finite value, including exponent-overflow values. Such malformed artifacts now raise `CalibrationReportMissing` and return public-route `503` rather than escaping during response serialization as `500`.

- **Bootstrap-count repair (2026-08-02; append-only):** `bootstrap_samples_requested` must now be a strictly positive integer, `bootstrap_samples_usable` must be non-negative, booleans are rejected as integers, and usable samples may not exceed requested samples.

- **Additional hardening (2026-08-02; append-only):** semantically mandatory strings must be non-empty, `hybrid_weight` must be finite and within `[0.0, 1.0]`, duplicate limitations and confidence reasons are rejected, and all returned mutable collections are newly constructed rather than shared with the cached artifact parse.

- **Adversarial verification (2026-08-02; append-only):** `41` additional parametrized mutations and `12` additional dedicated tests were added. The focused suite now contains `185` parametrized cases exercised through two tests plus `26` non-parametrized tests, giving `185 × 2 + 26 = 396` passing focused executions.

- **Updated verification totals (2026-08-02; append-only):** focused calibration API `396`, complete backend `495`, excess-basis `335`, and disposable staged full-root `893` passed under `/opt/anaconda3/bin/python`. Documentation lint, claims lint under MCC `v1.9.0`, frontend production build, and `git diff --check` also passed.

- **Verification-process correction (2026-08-02; append-only):** an initial disposable-copy procedure retained the source worktree's `.git` pointer, causing temporary staging in the source index. The condition was detected immediately, reversed with index-only `git reset`, and verified to leave no staged, committed, pushed, or working-tree content change. Future disposable verification must use an independent clone and must not copy a worktree `.git` pointer.

- **Preservation (2026-08-02; append-only):** this repair changed only `backend/app/services/research/calibration.py` and `backend/tests/test_calibration_api.py` relative to the prior reviewed change set. The router, fixed copy, frontend, MCC, protected boundary and pins, generated excess artifacts, committed calibration artifact, leaderboard, prediction dumps, and scientific findings were not changed.

- **Current state (2026-08-02; append-only):** `THIRD_BLOCKER_REPAIR_COMPLETE / FRESH_INDEPENDENT_REREVIEW_REQUIRED / NOT_COMMIT_READY`.


- **R3-UI-03 final independent approval (2026-08-02; append-only):** fresh independent review returned `APPROVED` after confirming the exact 17-path scope, strict filtered read-only calibration passthrough, byte-verbatim fixed copy, complete route-level fail-closed behavior, exact response-key whitelists, non-aliasing of cached objects, and preservation of frontend loading and fallback behavior.

- **Final adversarial result (2026-08-02; append-only):** unknown scalar, list, and nested-object keys; unsupported claim-safety assertions; malformed bootstrap counts; `NaN`, positive and negative infinity, and exponent-overflow numeric values all return public-route `503`. The valid committed artifact remains `200`, while unrelated programming errors are not disguised as artifact failures.

- **Final verification (2026-08-02; append-only):** focused calibration API `396`, complete backend `495`, excess-basis `335`, six-file MCC/claim-safety suite `348`, and disposable staged root suite `893` passed under `/opt/anaconda3/bin/python`. Documentation lint, claims lint under MCC `v1.9.0`, data validation, frontend production build in an independent disposable clone, and `git diff --check` passed.

- **Boundary and scientific preservation (2026-08-02; append-only):** the protected boundary retains `351` members with digest `0b0083a458ff24e9414ed23c12fb58f40ebe22c94539e6979b0c7affcf6d76ba`. The MCC remains inside the boundary. Governed excess findings, leaderboard, prediction dumps, all ICs, p-values, intervals, counts, and the `0/6` family-wise conclusion remain unchanged.

- **Review-environment limitation (2026-08-02; append-only):** the standalone review clone exposed two unrelated APFS/Git Unicode-normalization deletion artifacts for tracked XLSX filenames. They were independently diagnosed, excluded only from the procedural scope gate, and were not included in staging, testing, hashes, or the verdict. No live authenticated Supabase browser session was recorded.

- **Final state (2026-08-02; append-only):** `INDEPENDENTLY_APPROVED / COMMIT_READY`.


- **R3-UI-03 post-merge closure (2026-08-02; append-only):** PR `#4` merged into `main` through merge commit `663f2825c8835fbf4fd059ca98e859357929b52a`. The Validation Lab now surfaces the committed R2-CAL-01 calibration audit through the authenticated `GET /research/calibration` endpoint with byte-verbatim claim-safe copy, explicit loading and fallback states, strict response whitelists, and fail-closed artifact validation.

- **Post-merge verification (2026-08-02; append-only):** on merged `main`, the root suite passed `893/893`, the complete backend suite passed `495/495`, the frontend production build completed successfully across `2,445` modules, documentation lint passed, claims lint passed under Model Confidence Contract `v1.9.0`, data validation passed with `403` modeling rows, `40` features, and `321` target rows, and `git diff --check` passed.

- **Boundary and scientific preservation (2026-08-02; append-only):** `model_confidence_contract.json` remains inside the `351`-member R3-MISS-01 protected boundary with digest `0b0083a458ff24e9414ed23c12fb58f40ebe22c94539e6979b0c7affcf6d76ba`. The governed excess findings, leaderboard, prediction dumps, ICs, p-values, intervals, counts, and `0/6` family-wise conclusion remain unchanged. No reliable predictive edge is established.

- **Verification limitations (2026-08-02; append-only):** no live authenticated Supabase browser session was recorded. The frontend build emitted only the existing non-blocking large-chunk warning. Python dependencies emitted existing deprecation warnings; no test failed because of them.

- **Final state (2026-08-02; append-only):** `MERGED / POST_MERGE_VERIFIED / R3_UI_03_CLOSED`.

### R3-LIMITS-01 — Automated limitations register
- **Priority:** P1. **Wave:** 3D. **Owner role:** Research/Docs. **Risk:** low-medium. **Model/effort:** **Terra, medium.** Review: Opus low.
- **Verified current state:** machine-readable `limitations` arrays exist in the significance, calibration, friction, regime, alternative-targets, and disagreement/stability (once landed) reports; more limitations live in METHODOLOGY prose, `docs/universe_audit.md`, and the audit doc; no aggregate exists — examiner-grade "list ALL known weaknesses" answers are assembled by hand.
- **Read first:** the `limitations` keys of each committed report (grep `"limitations"` across `experiments/results*/` and `data/trusted_clean/*.json`); METHODOLOGY §Limitations; `docs/universe_audit.md` §Missing evidence.
- **Files to modify:** new `scripts/build_limitations_register.py` (proposed) → `docs/limitations_register.md` (proposed, generated — banner: "GENERATED — regenerate via make limitations-register; do not hand-edit"); Makefile additive `limitations-register`; root test; registry entry (R3-REL-01 landed by then).
- **Design (binding):** two sections: (1) auto-extracted — every `limitations[]` entry from every registered report JSON, grouped by artifact, verbatim, with source path; (2) curated seed list — doc-sourced limitations (retrospective cohort, sector unpopulated, one regime, environment-qualified reproduction, manual shares, deployment unverified) maintained INSIDE the script as a reviewed constant with source citations, so the register is fully regenerable. Deterministic ordering (path-sorted). No summarization, no paraphrase — verbatim extraction only (paraphrase is where claims drift).
- **Acceptance criteria:** run twice → byte-identical; test pins that every report JSON with a `limitations` key contributes ≥1 entry and that the file carries the generated banner; root suite green.
- **Verification:** root pytest; `make limitations-register` ×2 + shasum.
- **Generated artifacts:** `docs/limitations_register.md` — owner `make limitations-register`; never hand-edit.
- **Failure modes:** paraphrasing entries (forbidden — verbatim); the curated list drifting from its cited sources (each entry carries its citation; reviewer spot-checks). **Rollback:** delete files + hunk. **Stop:** a report's limitations key has an unexpected non-list shape.
- **Commit scope:** script + generated doc + test + Makefile. **Theme:** "Generate the aggregated limitations register". Manual commit: yes. **/clear:** yes.

### R3-MEMO-01 — Claim-aware research memo compiler
- **Priority:** P1. **Wave:** 3D (after UI-01; the queue's flagship product task). **Owner role:** Backend/Agent. **Risk:** **high — a composed memo is the closest this product ever comes to looking like advice.** **Model/effort:** **Opus, high.** Independent review: **Fable, medium (mandatory before merge).**
- **Verified current state:** citation-complete building blocks all exist — courtroom lenses (`courtroom_service.py`, per-sentence citation chips, no-verdict schema), skeptic checks, feature passports, significance/calibration/regime passthroughs, company contexts; no composed per-ticker memo exists; ad-hoc prose exports are invisible to claims-lint today.
- **Read first:** `courtroom_service.py` fully (citation mechanics + `insufficient_data` handling — the pattern to reuse), `skeptic_service.py` response shape, `services/research/feature_passports.py`, `model_confidence_contract.json` scan section, one company-context JSON.
- **Files to modify:** new `backend/app/services/memo_service.py` (proposed) + `POST /research/memo/{ticker}` route (router placement per Skeptic precedent); backend tests; MCC (`scan.backend_response_files` += service; version bump); optional thin frontend trigger deferred to a later task — **no new page here**.
- **Design (binding):** deterministic (LLM-off) composition with fixed section order: Identity & data coverage → Evidence quality (passports + quality reports) → Skeptic challenge results (embedded verbatim) → Significance & power context (raw+adjusted paired) → Limitations (from the register once it exists, else per-report arrays) → Provenance stamp (git SHA, source-artifact checksums, generation timestamp, MCC version). **Structural guarantees, each pinned by a test:** every sentence carries a citation resolving to an artifact field (reuse/extract the courtroom's resolution helper into a shared module both services import); the response schema contains **no recommendation, verdict, rating, target, or outlook field** (key-absence test, like courtroom); the primary disclaimer and this closing line (verbatim) are non-optional: "**Composed from committed research artifacts. No part of this memo is a recommendation; the underlying evaluation found no reliable predictive edge.**" Optional LLM mode may only rephrase grounded bullets (courtroom constraint, same rejection rule). Memos are responses, not committed files; if an export path is added, exported markdown embeds the same stamp + disclaimer and the export dir is claims-lint-scanned (add glob to MCC).
- **Acceptance criteria:** LLM-off memo for ASELS + one sparse ticker recorded; citation-completeness test; key-absence test; `insufficient_data` path test; suites green; `make claims-lint` green after contract bump; Fable review recorded.
- **Verification:** backend pytest; root pytest (if shared helper moves); `make claims-lint`; recorded LLM-off transcripts.
- **Generated artifacts:** none by default (see export rule above). **Failure modes / misleading success:** composition upgrading claims ("three checks passed" + "top decile" adjacent sentences reading as endorsement — section order and closing line are the mitigation, and the Fable review must read the full rendered memo, not the code); citation chips resolving to a file but not the quoted value (resolution test must check values, not paths).
- **Rollback:** revert; additive. **Stop:** if any section cannot be built citation-complete, emit `insufficient_data` for that section — never fill with prose.
- **Commit scope:** service + route + shared citation helper + tests + contract bump. **Theme:** "Add citation-complete research memo compiler". Manual commit: yes. **/clear:** yes.

### R3-SPIKE-01 — Point-in-time universe history sourcing spike (memo only)
- **Priority:** P2. **Wave:** 3C. **Owner role:** Research/Data. **Risk:** low (no data, no code). **Model/effort:** **Opus, medium.** Review: Fable low.
- **Verified current state:** `docs/universe_audit.md` §Missing evidence — no constituent history, membership dates, delisting/suspension/symbol-change records exist; several frontier ideas (point-in-time universe, security-status history, survivorship remediation) are blocked on whether free, official, dated sources exist at all.
- **Read first:** `docs/universe_audit.md` fully; `data/config/*.csv` headers; the manual-CSV pattern (`data/trusted_raw/macro/` sidecars).
- **Files to modify:** new `docs/UNIVERSE_HISTORY_SOURCING_SPIKE.md` (proposed) only. **Do NOT touch:** any data or config file; no fetching into the repo.
- **Deliverable (binding):** for each candidate source (Borsa İstanbul index announcements/bulletins, KAP disclosures, any other free/official archive): what it actually publishes, dated how, coverage of 2020–2025, retrieval effort, licensing/citation terms — with links and access dates. Then either a proposed manual-CSV schema (`effective_from`, `effective_to`, `event_type`, `source_url`, `retrieved_at` — following the shares/macro sidecar pattern) + a follow-up ingestion task draft, or the honest conclusion that no free source suffices (which permanently gates the dependent ideas and must then be recorded in `docs/universe_audit.md` by a follow-up docs task). **No inference, no reconstruction from memory, no LLM-recalled "constituent lists" — a source is a document, not a recollection.**
- **Acceptance criteria:** every claim carries a link + access date; no repo data changed; explicit go/no-go recommendation.
- **Verification:** `git diff --stat` (one new doc). **Failure modes:** citing recalled index membership as if sourced (forbidden); scraping (forbidden — manual retrieval documentation only). **Rollback:** delete doc. **Stop:** n/a.
- **Commit scope:** one doc. **Theme:** "Universe-history sourcing spike". Manual commit: yes. **/clear:** yes.

### R3-AGENT-01 — Research-assistant grounded intents for Phase-2/3 artifacts (compact spec)
- **Priority:** P3. **Wave:** 3E. **Gate:** R3-UI-01 + R3-UI-03 landed. **Model/effort:** **Terra, medium.** Review: Opus low.
- `/research/ask` has five grounded intents predating Phase 2; add deterministic intents that quote the significance headline (raw+adjusted paired), the Skeptic report, the calibration finding, and the friction stamp sentence — each with source citations and existing fallback behavior. Full packet to be written when the gate clears (pattern: R2-SKEPTIC-01's evidence-loader reuse). New response copy goes through MCC registration.

## Later accepted backlog (packets written after their gates clear — specs in `FINANCEIQ_PHASE3_4_FRONTIER_PLAN.md` §2)

| ID | Title | Wave | Gate | Model |
|---|---|---|---|---|
| R4-DIM-01 | Feature redundancy & effective dimensionality | 4A | 3B core landed | Terra M |
| R4-ROBUST-01 | Cellwise contamination stress laboratory | 4A | R3-NULL-01 (isolation discipline) | Opus/Fable H |
| R4-PROV-01 | Per-cell provenance (passports v2) | 4A | R3-REL-01 | Opus H |
| R4-EXAM-01 | Examiner question bank + defense pack | 4B | R3-LIMITS-01 | Terra M, Opus review |
| R4-THESIS-01 | Thesis appendix compiler | 4B | R3-REL-01 | Terra/Opus M |
| R4-RELEASE-01 | Public research release package & checklist | 4B | R4-THESIS-01 | Opus M, **Fable review** |
| R4-UI-01 | Evidence registry & freshness page | 4C | R3-REL-01 | Terra M |
| R4-CF-01 | Counterfactual ranking explorer | 4C | R3-MISS-01 + R3-UI-02 | Opus H, **Fable review** |
| R4-A11Y-01 | Plain-language explainers + accessibility pass | 4C | Phase 3D landed | Terra M |
| (follow-up) | Authenticated E2E implementation | 3E | R3-E2E-01 recommendation | per spike memo |

## Phase 3 order (dependency-sorted)

**3A:** R3-GOV-01 → R3-LINT-01 → R3-MCC-01 → R3-REL-01 → R3-LEGACY-01 → R3-E2E-01.
**3B:** R3-STAT-02 → R3-INF-01 → R3-STAT-01 → R3-TGT-01 → R3-NULL-01 → R3-SERV-01 → R3-PREREG-01 (PREREG may move earlier; it is time-gated, not dependency-gated).
**3C:** R3-UI-02 → R3-SPIKE-01. **3D:** R3-UI-01 → R3-UI-03 → R3-LIMITS-01 → R3-MEMO-01. **3E:** R3-AGENT-01 → E2E follow-up.

Hard rules: R3-GOV-01 first (everything else cites its baseline); harness-running tasks (TGT/NULL/SERV) in separate fresh contexts; only one MCC-bumping task per context; R3-MEMO-01 after R3-UI-01 (shared skeptic surface conventions); R3-PREREG-01 before any 2026 outcome data exists. 3A/3B/3D may interleave across sessions where files are disjoint. The demo-critical chain is GOV-01 → UI-01 → SERV-01 → MEMO-01.

**Standing instruction (unchanged):** one task per context; on completion add the ledger row here + a TASK_STATE.md row, leave the tree clean for the owner's manual commit, and recommend `/clear`.

- **Research Courtroom Evidence Lens overlap post-merge closure (2026-08-02; append-only):** PR `#5` merged into `main` through merge commit `ab01dc9f58ded173465f59b561de35b59922a91b`. The fix removes sticky positioning from the Risk evidence panel and adds Playwright regression coverage for the overlap condition.

- **Verification (2026-08-02; append-only):** the rebased two-file scope passed the frontend production build, claims lint under MCC `v1.9.0`, and `git diff --check`. The Vercel deployment was manually inspected in the browser and the Evidence Lens panels no longer overlap during scrolling or responsive layout checks.

- **Final state (2026-08-02; append-only):** `MERGED / MANUALLY_VISUALLY_VERIFIED / CLOSED`.


